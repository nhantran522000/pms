---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-PLAN-04
files_modified:
  - libs/shared-types/src/auth/auth.schema.ts
  - libs/feature-auth/src/domain/entities/user.entity.ts
  - libs/feature-auth/src/domain/entities/tenant.entity.ts
  - libs/feature-auth/src/domain/value-objects/password.vo.ts
  - libs/feature-auth/src/infrastructure/repositories/user.repository.ts
  - libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts
  - libs/feature-auth/src/infrastructure/index.ts
  - libs/feature-auth/src/application/dto/index.ts
  - libs/feature-auth/src/domain/index.ts
  - libs/feature-auth/project.json
autonomous: true
requirements:
  - AUTH-01
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "User can sign up with email and password"
    - "User can log in with verified email and password"
    - "Session persists across browser refresh via httpOnly cookie"
    - "User can log out from any page"
  artifacts:
    - path: "libs/shared-types/src/auth/auth.schema.ts"
      provides: "Auth DTOs and Zod schemas"
      contains: "signupSchema"
    - path: "libs/feature-auth/src/domain/entities/user.entity.ts"
      provides: "User domain entity"
      contains: "UserEntity"
    - path: "libs/feature-auth/src/domain/value-objects/password.vo.ts"
      provides: "Password value object with Zxcvbn validation"
      contains: "Password"
    - path: "libs/feature-auth/src/infrastructure/repositories/user.repository.ts"
      provides: "User data persistence"
      contains: "UserRepository"
  key_links:
    - from: "user.repository.ts"
      to: "PrismaService"
      via: "dependency injection"
      pattern: "this.prisma.user"
---

<objective>
Create auth domain entities, value objects, and repositories for the core authentication flow.

Purpose: Establish the DDD foundation for authentication with proper domain entities, value objects (including Zxcvbn password validation), and repository abstractions.

Output: Zod schemas for auth DTOs, User and Tenant entities, Password value object with strength validation, User and Tenant repositories.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PLAN-04-SUMMARY.md (from prior plan)

<interfaces>
<!-- From Plan 04: -->
<!-- - getTenantContext at libs/data-access/tenant-context/src/async-local-storage.ts -->
<!-- - PrismaService at libs/data-access/prisma/src/prisma.service.ts -->

<!-- From CONTEXT.md decisions: -->
<!-- - Password strength: Zxcvbn score 3+ -->
<!-- - Session duration: 7 days -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth Zod schemas in shared-types</name>
  <files>libs/shared-types/src/auth/auth.schema.ts, libs/shared-types/src/auth/index.ts, libs/shared-types/src/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (for password strength decision)
    - .planning/phases/01-foundation/01-RESEARCH.md (for Zod patterns)
  </read_first>
  <behavior>
    - signupSchema validates email, password, name
    - loginSchema validates email, password
    - forgotPasswordSchema validates email
    - resetPasswordSchema validates token, password
    - verifyEmailSchema validates token
  </behavior>
  <action>
    Create auth Zod schemas:

    1. Create `libs/shared-types/src/auth/auth.schema.ts`:
       ```typescript
       import { z } from 'zod';

       // Per CONTEXT.md: Password strength Zxcvbn score 3+
       export const passwordSchema = z.string()
         .min(8, 'Password must be at least 8 characters')
         .max(100, 'Password must be less than 100 characters');

       export const signupSchema = z.object({
         email: z.string().email('Invalid email address'),
         password: passwordSchema,
         name: z.string().min(1).max(100).optional(),
       });

       export const loginSchema = z.object({
         email: z.string().email('Invalid email address'),
         password: z.string().min(1),
       });

       export const forgotPasswordSchema = z.object({
         email: z.string().email('Invalid email address'),
       });

       export const resetPasswordSchema = z.object({
         token: z.string().min(1, 'Token is required'),
         password: passwordSchema,
       });

       export const verifyEmailSchema = z.object({
         token: z.string().min(1, 'Token is required'),
       });

       // Export types
       export type SignupDto = z.infer<typeof signupSchema>;
       export type LoginDto = z.infer<typeof loginSchema>;
       export type ForgotPasswordDto = z.infer<typeof forgotPasswordSchema>;
       export type ResetPasswordDto = z.infer<typeof resetPasswordSchema>;
       export type VerifyEmailDto = z.infer<typeof verifyEmailSchema>;
       ```

    2. Create `libs/shared-types/src/auth/index.ts`:
       ```typescript
       export * from './auth.schema';
       ```

    3. Update `libs/shared-types/src/index.ts`:
       ```typescript
       export * from './common';
       export * from './auth';
       ```
  </action>
  <verify>
    <automated>test -f libs/shared-types/src/auth/auth.schema.ts && grep -q "signupSchema" libs/shared-types/src/auth/auth.schema.ts && grep -q "LoginDto" libs/shared-types/src/auth/auth.schema.ts && echo "Auth schemas created"</automated>
  </verify>
  <done>
    - libs/shared-types/src/auth/auth.schema.ts exists with all schemas
    - signupSchema, loginSchema, forgotPasswordSchema, resetPasswordSchema, verifyEmailSchema defined
    - Types exported (SignupDto, LoginDto, etc.)
    - Auth schemas re-exported from shared-types
  </done>
</task>

<task type="auto">
  <name>Task 2: Create domain entities</name>
  <files>libs/feature-auth/src/domain/entities/user.entity.ts, libs/feature-auth/src/domain/entities/tenant.entity.ts, libs/feature-auth/src/domain/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (for entity patterns)
    - libs/data-access/prisma/schema.prisma (for Prisma types)
  </read_first>
  <behavior>
    - UserEntity encapsulates user domain logic
    - TenantEntity encapsulates tenant domain logic
    - Entities provide fromPrisma factory methods
    - Entities expose business methods (isEmailVerified, etc.)
  </behavior>
  <action>
    Create domain entities:

    1. Create `libs/feature-auth/src/domain/entities/user.entity.ts`:
       ```typescript
       import { User } from '@prisma/client';

       export class UserEntity {
         private constructor(
           public readonly id: string,
           public readonly email: string,
           public readonly passwordHash: string,
           public readonly name: string | null,
           public readonly emailVerified: boolean,
           public readonly tenantId: string,
           public readonly createdAt: Date,
           public readonly updatedAt: Date,
         ) {}

         static fromPrisma(user: User): UserEntity {
           return new UserEntity(
             user.id,
             user.email,
             user.passwordHash,
             user.name,
             user.emailVerified,
             user.tenantId,
             user.createdAt,
             user.updatedAt,
           );
         }

         isEmailVerified(): boolean {
           return this.emailVerified;
         }

         toJSON() {
           return {
             id: this.id,
             email: this.email,
             name: this.name,
             emailVerified: this.emailVerified,
             tenantId: this.tenantId,
             createdAt: this.createdAt,
             updatedAt: this.updatedAt,
           };
         }
       }
       ```

    2. Create `libs/feature-auth/src/domain/entities/tenant.entity.ts`:
       ```typescript
       import { Tenant } from '@prisma/client';

       export class TenantEntity {
         private constructor(
           public readonly id: string,
           public readonly name: string,
           public readonly createdAt: Date,
           public readonly updatedAt: Date,
         ) {}

         static fromPrisma(tenant: Tenant): TenantEntity {
           return new TenantEntity(
             tenant.id,
             tenant.name,
             tenant.createdAt,
             tenant.updatedAt,
           );
         }
       }
       ```

    3. Create `libs/feature-auth/src/domain/index.ts`:
       ```typescript
       export * from './entities/user.entity';
       export * from './entities/tenant.entity';
       export * from './value-objects/password.vo';
       ```
  </action>
  <verify>
    <automated>test -f libs/feature-auth/src/domain/entities/user.entity.ts && test -f libs/feature-auth/src/domain/entities/tenant.entity.ts && grep -q "UserEntity" libs/feature-auth/src/domain/entities/user.entity.ts && grep -q "fromPrisma" libs/feature-auth/src/domain/entities/user.entity.ts && echo "Domain entities created"</automated>
  </verify>
  <done>
    - libs/feature-auth/src/domain/entities/user.entity.ts exists
    - libs/feature-auth/src/domain/entities/tenant.entity.ts exists
    - UserEntity has fromPrisma factory method
    - UserEntity exposes isEmailVerified() method
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Password value object with Zxcvbn validation</name>
  <files>libs/feature-auth/src/domain/value-objects/password.vo.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (for password strength decision: Zxcvbn score 3+)
    - .planning/phases/01-foundation/01-RESEARCH.md (for value object patterns)
  </read_first>
  <behavior>
    - Password value object encapsulates password hashing and validation
    - Zxcvbn score 3+ enforced for password strength
    - bcrypt used for password hashing
  </behavior>
  <action>
    Create Password value object:

    1. Install dependencies:
       ```bash
       pnpm add zxcvbn bcrypt
       pnpm add -D @types/zxcvbn @types/bcrypt
       ```

    2. Create `libs/feature-auth/src/domain/value-objects/password.vo.ts`:
       ```typescript
       import * as bcrypt from 'bcrypt';
       import zxcvbn from 'zxcvbn';

       export interface PasswordValidationResult {
         valid: boolean;
         score: number;
         warnings: string[];
         suggestions: string[];
       }

       export class Password {
         private constructor(private readonly hashedPassword: string) {}

         static async create(plainPassword: string): Promise<Password> {
           const validation = Password.validate(plainPassword);
           if (!validation.valid) {
             throw new Error(`Password validation failed: ${validation.warnings.join(', ')}`);
           }

           const hashedPassword = await bcrypt.hash(plainPassword, 10);
           return new Password(hashedPassword);
         }

         static fromHash(hashedPassword: string): Password {
           return new Password(hashedPassword);
         }

         static validate(password: string): PasswordValidationResult {
           const result = zxcvbn(password);

           // Per CONTEXT.md: Zxcvbn score 3+ required
           const valid = result.score >= 3;
           const warnings: string[] = [];
           const suggestions: string[] = [];

           if (result.feedback.warning) {
             warnings.push(result.feedback.warning);
           }
           if (result.feedback.suggestions) {
             suggestions.push(...result.feedback.suggestions);
           }

           // Additional basic checks
           if (password.length < 8) {
             warnings.push('Password must be at least 8 characters');
           }

           return {
             valid,
             score: result.score,
             warnings,
             suggestions,
           };
         }

         get hash(): string {
           return this.hashedPassword;
         }

         async compare(plainPassword: string): Promise<boolean> {
           return bcrypt.compare(plainPassword, this.hashedPassword);
         }
       }
       ```
  </action>
  <verify>
    <automated>test -f libs/feature-auth/src/domain/value-objects/password.vo.ts && grep -q "zxcvbn" libs/feature-auth/src/domain/value-objects/password.vo.ts && grep -q "score >= 3" libs/feature-auth/src/domain/value-objects/password.vo.ts && grep -q "bcrypt" libs/feature-auth/src/domain/value-objects/password.vo.ts && echo "Password value object created"</automated>
  </verify>
  <done>
    - libs/feature-auth/src/domain/value-objects/password.vo.ts exists
    - Password.create() validates with Zxcvbn score 3+
    - Password.compare() uses bcrypt for verification
    - zxcvbn and bcrypt packages installed
  </done>
</task>

<task type="auto">
  <name>Task 4: Create infrastructure repositories</name>
  <files>libs/feature-auth/src/infrastructure/repositories/user.repository.ts, libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts, libs/feature-auth/src/infrastructure/index.ts</files>
  <read_first>
    - libs/data-access/prisma/src/prisma.service.ts (for PrismaService)
    - libs/feature-auth/src/domain/entities/user.entity.ts (for UserEntity)
  </read_first>
  <behavior>
    - UserRepository provides CRUD operations for users
    - TenantRepository provides CRUD operations for tenants
    - Repositories use PrismaService
  </behavior>
  <action>
    Create infrastructure repositories:

    1. Create `libs/feature-auth/src/infrastructure/repositories/user.repository.ts`:
       ```typescript
       import { Injectable } from '@nestjs/common';
       import { PrismaService } from '@pms/data-access';
       import { UserEntity } from '../../domain/entities/user.entity';
       import { User } from '@prisma/client';

       export interface CreateUserInput {
         email: string;
         passwordHash: string;
         name?: string;
         tenantId: string;
       }

       @Injectable()
       export class UserRepository {
         constructor(private readonly prisma: PrismaService) {}

         async create(input: CreateUserInput): Promise<UserEntity> {
           const user = await this.prisma.user.create({
             data: {
               email: input.email,
               passwordHash: input.passwordHash,
               name: input.name,
               tenantId: input.tenantId,
               emailVerified: false,
             },
           });
           return UserEntity.fromPrisma(user);
         }

         async findByEmail(email: string): Promise<UserEntity | null> {
           const user = await this.prisma.user.findUnique({
             where: { email },
           });
           return user ? UserEntity.fromPrisma(user) : null;
         }

         async findById(id: string): Promise<UserEntity | null> {
           const user = await this.prisma.user.findUnique({
             where: { id },
           });
           return user ? UserEntity.fromPrisma(user) : null;
         }

         async updateEmailVerified(id: string, verified: boolean): Promise<UserEntity> {
           const user = await this.prisma.user.update({
             where: { id },
             data: { emailVerified: verified },
           });
           return UserEntity.fromPrisma(user);
         }

         async updatePassword(id: string, passwordHash: string): Promise<UserEntity> {
           const user = await this.prisma.user.update({
             where: { id },
             data: { passwordHash },
           });
           return UserEntity.fromPrisma(user);
         }

         async updateVerificationToken(id: string, token: string | null, expiresAt: Date | null): Promise<UserEntity> {
           const user = await this.prisma.user.update({
             where: { id },
             data: {
               verificationToken: token,
               verificationTokenExpires: expiresAt,
             },
           });
           return UserEntity.fromPrisma(user);
         }

         async findByVerificationToken(token: string): Promise<UserEntity | null> {
           const user = await this.prisma.user.findFirst({
             where: {
               verificationToken: token,
               verificationTokenExpires: { gte: new Date() },
             },
           });
           return user ? UserEntity.fromPrisma(user) : null;
         }

         async updatePasswordResetToken(id: string, token: string | null, expiresAt: Date | null): Promise<UserEntity> {
           const user = await this.prisma.user.update({
             where: { id },
             data: {
                 passwordResetToken: token,
                 passwordResetTokenExpires: expiresAt,
               },
             });
           return UserEntity.fromPrisma(user);
         }

         async findByPasswordResetToken(token: string): Promise<UserEntity | null> {
           const user = await this.prisma.user.findFirst({
             where: {
               passwordResetToken: token,
               passwordResetTokenExpires: { gte: new Date() },
             },
           });
           return user ? UserEntity.fromPrisma(user) : null;
         }
       }
       ```

    2. Create `libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts`:
       ```typescript
       import { Injectable } from '@nestjs/common';
       import { PrismaService } from '@pms/data-access';
       import { TenantEntity } from '../../domain/entities/tenant.entity';

       @Injectable()
       export class TenantRepository {
         constructor(private readonly prisma: PrismaService) {}

         async findById(id: string): Promise<TenantEntity | null> {
           const tenant = await this.prisma.tenant.findUnique({
             where: { id },
           });
           return tenant ? TenantEntity.fromPrisma(tenant) : null;
         }

         async create(id: string, name: string): Promise<TenantEntity> {
           const tenant = await this.prisma.tenant.create({
             data: { id, name },
           });
           return TenantEntity.fromPrisma(tenant);
         }
       }
       ```

    3. Create `libs/feature-auth/src/infrastructure/index.ts`:
       ```typescript
       export * from './repositories/user.repository';
       export * from './repositories/tenant.repository';
       ```
  </action>
  <verify>
    <automated>test -f libs/feature-auth/src/infrastructure/repositories/user.repository.ts && test -f libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts && grep -q "UserRepository" libs/feature-auth/src/infrastructure/repositories/user.repository.ts && grep -q "TenantRepository" libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts && echo "Repositories created"</automated>
  </verify>
  <done>
    - libs/feature-auth/src/infrastructure/repositories/user.repository.ts exists
    - libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts exists
    - UserRepository has create, findByEmail, findById, update methods
    - TenantRepository has findById, create methods
  </done>
</task>

<task type="auto">
  <name>Task 5: Create feature-auth library structure</name>
  <files>libs/feature-auth/project.json, libs/feature-auth/src/index.ts, libs/feature-auth/tsconfig.json, libs/feature-auth/tsconfig.lib.json</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (for Nx library patterns)
    - libs/shared-kernel/project.json (for reference)
  </read_first>
  <behavior>
    - feature-auth library created with proper Nx configuration
    - Tags set for DDD layers
    - Library exports from barrel file
  </behavior>
  <action>
    Create feature-auth library structure:

    1. Create library directory structure:
       ```bash
       mkdir -p libs/feature-auth/src/{domain/{entities,value-objects},application/dto,infrastructure/{repositories,services},presentation/{controllers,guards,strategies,decorators}}
       ```

    2. Create `libs/feature-auth/project.json`:
       ```json
       {
         "name": "feature-auth",
         "tags": ["type:lib", "domain:auth", "layer:domain", "layer:application", "layer:infrastructure", "layer:presentation"],
         "sourceRoot": "libs/feature-auth/src"
       }
       ```

    3. Create `libs/feature-auth/tsconfig.json`:
       ```json
       {
         "extends": "../../tsconfig.base.json",
         "compilerOptions": {
           "module": "commonjs",
           "forceConsistentCasingInFileNames": true,
           "strict": true,
           "noImplicitOverride": true,
           "noPropertyAccessFromIndexSignature": true,
           "noImplicitReturns": true,
           "noFallthroughCasesInSwitch": true
         },
         "include": ["src/**/*.ts"],
         "exclude": ["node_modules", "dist"]
       }
       ```

    4. Create `libs/feature-auth/tsconfig.lib.json`:
       ```json
       {
         "extends": "./tsconfig.json",
         "compilerOptions": {
           "outDir": "../../dist/out-tsc",
           "declaration": true,
           "types": ["node"]
         },
         "include": ["src/**/*.ts"],
         "exclude": ["src/**/*.spec.ts", "src/**/*.test.ts"]
       }
       ```

    5. Create `libs/feature-auth/src/index.ts`:
       ```typescript
       // Feature Auth - Authentication domain module
       // DDD structure: domain -> application -> infrastructure -> presentation

       export * from './domain';
       export * from './application';
       export * from './infrastructure';
       ```
  </action>
  <verify>
    <automated>test -f libs/feature-auth/project.json && test -f libs/feature-auth/src/index.ts && grep -q "feature-auth" libs/feature-auth/project.json && echo "feature-auth library structure created"</automated>
  </verify>
  <done>
    - libs/feature-auth/project.json exists with correct tags
    - Directory structure created for DDD layers
    - libs/feature-auth/src/index.ts exports domain, application, infrastructure
    - tsconfig files configured
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Verify auth schemas:**
   ```bash
   test -f libs/shared-types/src/auth/auth.schema.ts
   grep "signupSchema" libs/shared-types/src/auth/auth.schema.ts
   ```

2. **Verify domain entities:**
   ```bash
   test -f libs/feature-auth/src/domain/entities/user.entity.ts
   test -f libs/feature-auth/src/domain/entities/tenant.entity.ts
   test -f libs/feature-auth/src/domain/value-objects/password.vo.ts
   ```

3. **Verify repositories:**
   ```bash
   test -f libs/feature-auth/src/infrastructure/repositories/user.repository.ts
   test -f libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts
   ```

4. **Verify Zxcvbn password validation:**
   ```bash
   grep "zxcvbn" libs/feature-auth/src/domain/value-objects/password.vo.ts
   grep "score >= 3" libs/feature-auth/src/domain/value-objects/password.vo.ts
   ```

5. **Verify library structure:**
   ```bash
   test -f libs/feature-auth/project.json
   grep "domain:auth" libs/feature-auth/project.json
   ```
</verification>

<success_criteria>
- Auth Zod schemas created in shared-types
- feature-auth library with DDD structure (domain, application, infrastructure, presentation)
- UserEntity, TenantEntity created with fromPrisma factory methods
- Password value object created with Zxcvbn score 3+ validation
- UserRepository, TenantRepository created
- bcrypt and zxcvbn packages installed
- Library tags properly configured for module boundary enforcement
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-PLAN-05-SUMMARY.md`
</output>
