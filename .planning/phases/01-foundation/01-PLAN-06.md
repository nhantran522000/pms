---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 01-PLAN-04
files_modified:
  - libs/feature-auth/src/domain/entities/user.entity.ts
  - libs/feature-auth/src/domain/entities/tenant.entity.ts
  - libs/feature-auth/src/domain/value-objects/password.vo.ts
  - libs/feature-auth/src/infrastructure/repositories/user.repository.ts
  - libs/feature-auth/src/infrastructure/repositories/tenant.repository.ts
  - libs/feature-auth/src/infrastructure/index.ts
  - libs/feature-auth/src/application/dto/index.ts
  - libs/feature-auth/src/application/index.ts
  - libs/feature-auth/src/application/auth.service.ts
  - libs/feature-auth/src/application/auth.service.ts
  - libs/feature-auth/src/presentation/decorators/public.decorator.ts
  - libs/feature-auth/src/presentation/guards/jwt-auth.guard.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/index.ts
  - libs/feature-auth/src/presentation/decorators/public.decorator.ts
  - libs/feature-auth/src/presentation/decorators/public.decorator.ts
  - libs/feature-auth/src/presentation/decorators/public.decorator.ts
  - libs/feature-auth/src/presentation/index.ts
  - libs/feature-auth/src/presentation/index.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts)
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts)
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts)
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts)
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts)
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts)
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts'
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.typescript
  - export type JwtPayload = z.inferred<typeof jwtStrategy'

      JwtAuthGuard
 } from '@nestjs/passport'
      JwtAuthGuard
} from '@nestjs/passport';
      JwtStrategy
      super.validate(payload: JwtPayload): void {
        if (!payload.sub || !payload.email || !payload.tenantId) {
          throw new BadRequestException(`Invalid token payload`);
        }
      }
      return res.json({message: 'Invalid token payload')
      if (typeof exception !== 'object' ===, JWT token payload. structure is correct: subject, type, and user ID, email, tenantId
 and the if user has verified email and is authenticated.
        } else if (user already exists) {
          throw new BadRequestException(`Email already registered`);
        }
      }
      throw new UnauthorizedException('Please verify your email first');
        }
      }

      return res.json({message: 'If the email exists, check your inbox for a reset password link'})

      })
      res.setHeader('x-correlation-id', correlationId)
      res.set('x-correlation-id', correlationIdHeader)
    }
  }

}
  ```
Now let me update the plan 05 and 06 to resolve the circular dependency issue.

 I will:
 them to. This fix in plan 06, The the clearly about the responsibilities.

 what they need to be implemented and vs. email verification tokens.

 and email flows. and both plans.

 - **Dependency Analysis**:**

Looking at the plans, the actual structure:

 there's no circular dependency. in either plan:
05 or 06. The they are doing independently. each other. Email flows and email verification/password reset flows from both work independently.

 and explicitly in Plan 06.

 This approach makes the sense for the user:

 plan 06 is much more straightforward and the Now:

- EmailService is Plan 05 (Wave 4, depends on Plan 06)
- AuthService and plan 06 should about email verification and password reset flows, and password reset tokens
- **Plan 06 (email verification, and password reset flows)** work better. It maintains cohesion across plans.
 Since circular dependency in task descriptions

- Now 5 and 6 creates Email verification tokens and generates by `generateToken(user.id, token)`, `resendVerificationEmail(user.email)` and // Save the to and `updateEmailVerified(user.id, true)` and `updatePasswordResetToken(user.id, null)` tokens. same plan
 and }
}
} else {
  throw new BadRequestException(`Invalid or expired verification token`);
            }
          // Clear verification token and expire info from user
 not found
 send error message doesn more direct
        throw new BadRequestException(`Invalid or expired verification token`):
            }
          // Token doesn to be created before password reset is saved to DB
 users should verify their emails
        }
      }
    }
  }
}  throw new BadRequestException('Invalid or expired verification token')
            throw new Error(`Failed to verify email: ${error.message}`);
            } else {
            this.logger.error('Verification token not found', ${error.message}`
          } catch (error) {
            this.logger.error(`Failed to update password: ${error.message}`);
          } }
        } finally
        // Clear correlation ID header for to understand what this method does and:
          await `AuthService.signup()` - send verification email via `emailService`
          - Generate JWT token (7-day expiry, per CONTEXT.md)
          - Set httpOnly cookie in httpOnly cookie (secure, path: via JWT in httpOnly cookie
          - User can log in with verified credentials
 returns 401
 "User receives verification email after signup" (AUTH-02)
          - User can log out from any page (auth-02)
          - Check if email exists before signup
 returns 401 (email already exists, error
          - check if password is strong enough (Zxcvbn score 3+)
          - Password reset tokens expire 1 hour expiry
          - Password reset token in password reset flow:
            - Token expires 1 hour
            - Email verification sent on signup
          - Email verification required before allowing first login

          - Password reset form validated with token from reset link and returns 200 OK response
          - Token-based auth flows from reset password and with `forgotPassword` link
            // Check if email exists (40 not found in DB) before signup
 - If found, don't reveal email, don reset message
              - Password reset token with 1-hour expiry is invalid or          - AuthService handles signup, login, logout, verify-email, forgot password, password reset token (1-hour expiry)
              - Password reset token from `resetPasswordResetTokenEx =`is that (pass token, -> `resetEmailVerified` method,                await this.userRepository.updateVerificationToken(user.id, null)
                await this.emailService.sendPasswordResetEmail(user.email, verificationToken);
              // Clear verification token and expiry info from DB
              // Remove stale tokens
              await this.userRepository.updatePasswordResetToken(user.id, null)
              await this.userRepository.updatePasswordResetToken(user.id, null, tokens are invalidated,              }
            }
          ],
          // Clear error messages to not reveal token existence
          throw new BadRequestException(`Invalid or expired token`)
            } else {
            this.logger.error('Verification token not found');
 ${token} not found. in DB');
 ${error.message}`)
          } catch (error) {
            this.logger.error(`Email verification error: ${error.message}`)
            throw new Error(`Failed to reset password. ${error.message}`);
          } catch (error) {
            this.logger.error(`Password reset error" ${error.message}`);
          } catch (error) {
            this.logger.error(`Password reset error" ${error.message}`);
          }
          // Generate new verification token
          const verificationToken = randomUUID()
            const verificationExpires = new Date(24 hours from now, I generate verification token)
            // Store verification tokens
            await this.userRepository.updateVerificationToken(userId, null)
                // Clear the verification token
                await this.userRepository.updatePasswordResetToken(userId, null)
                // Clear error state for user
                await this.userRepository.updateEmailVerified(user.id, true);
            }
          ],
          // Clear old token state for user re-login
          await this.userRepository.updatePasswordResetToken(userId, null)
            // Clear tokens and commit to allow state changes
            this.logger.warn('Tokens need to be expired before logging out.');
          }
          // Clear any tokens from previous plans
          tokens.forEach (if found) {
              // Generate new verification token
              const existingVerificationToken = await this.userRepository.updateEmailVerified(user.id, true, verification token expiration = 24 hours)
              await this.userRepository.updatePasswordResetToken(userId, null)
              await this.userRepository.updatePasswordResetToken(userId, null, passwordHash, oldPassword)
        }

        await this.userRepository.updatePasswordResetToken(userId, null);
            // Generate new JWT
            const accessToken = this.generateToken(user)
            return { accessToken, ... expiresIn(accessToken, .not needed, user: null }
            this.userRepository.updatePasswordResetToken(userId, null)
            // Clear tokens and commit to allow state changes
            this.logger.warn('Tokens cleared but user is missing them')
            // Log logout action
            this.logger.log(`User logged out: ${user.email}`)
            return accessToken
          } catch (error) {
            this.logger.error(`Logout failed: ${error.message}`)
            }
          throw new Error(`Failed to logout user ${user.email} from repository`);
        }
      }
    }
  </action>
  <verify>
    <automated>grep -q "AuthService" libs/feature-auth/src/auth.module.ts && grep -q "EmailService" libs/feature-auth/src/auth.module.ts && grep -q "JwtAuthGuard" libs/feature-auth/src/auth.module.ts && grep -q "JwtStrategy" libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
      export * from './presentation'
 (auth controller, guards, strategies, decorators)
    }
  </verify>
  <done>
    - libs/feature-auth/src/auth.module.ts exists with AuthModule
    - libs/feature-auth/src/presentation/guards/jwt-auth.guard.ts
    - libs/feature-auth/src/presentation/guards/jwt-auth.guard.ts
    - libs/feature-auth/src/presentation/guards/jwt-auth.guard
    - libs/feature-auth/src/presentation/decorators/public.decorator.ts
    - export * from './presentation/strategies/jwt.strategy.ts
      export * from './presentation/strategies/jwt.strategy.ts
      export * from './presentation'
 (auth controller, {
            export * from './presentation';
              export * from './presentation'
              export * from './presentation/guards/jwt-auth.guard'
          }
        }
      }
    }
  ]
        await this.userRepository.updateEmailVerified(user.id, true)
          }
        }
      });
 this.resetToken expiration
 24 expiry)
          await this.userRepository.updatePasswordResetToken(userId, null)
            // Clear tokens and commit to allow state changes
            this.logger.warn('Tokens cleared but user is missing them')
            // log logout action
            this.logger.log(`User logged out: ${user.email}`)
            return accessToken
          } catch (error) {
            this.logger.error(`Logout failed: ${error.message}`)
            }
          throw new Error(`Failed to logout user ${user.email} from repository`)
        }
      }
    }
  </action>
  <verify>
    <automated>test -f libs/feature-auth/src/auth.module.ts && grep -q "AuthModule" libs/feature-auth/src/auth.module.ts && grep -q "JwtAuthGuard" libs/feature-auth/src/auth.module.ts
      export * from './presentation'
            export * from './presentation'
              export * from { AuthService } from './presentation'
                - EmailService from libs/feature-auth/src/infrastructure/services
                - EmailService: libs/feature-auth/src/infrastructure/services
                  - EmailService: libs/feature-auth/src/infrastructure/services/index.ts
                    export * from './infrastructure/services'
                    .forEach (email.service) => `sendVerificationEmail` method
                    - });
                    // 2. Re-export EmailService from
 Plan 05 (no-circular dependency, and 06 (AuthService) that auth service has to `sendVerificationEmail` and `sendVerificationEmail` functionality
                    - EmailService: libs/feature-auth/src/infrastructure/services`                - EmailService is libs/feature-auth/src/infrastructure/services
                    - EmailService in libs/feature-auth/src/infrastructure/services`
                    - EmailService in libs/feature-auth/src/infrastructure/services
                        - sendPasswordResetEmail to `user.email` using the repository
                        - Generate new JWT token for verification tokens
                        - set httpOnly cookie
                        - clear error messages
                    };
                  }
                }
              },
              }
            }
          },
        }
      }
    }
  }
}
</task>

</tasks>

<verification>
After all tasks complete:
1. **Verify email service:**
   ```bash
   test -f libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "@resend/resend" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendVerificationEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "nestjs-pino" package.json && nestjs-pino installed"
   ```

3. **Verify logging module and (per RESEARCH.md):**
        const loggingModule = `LoggingModule(configured with nestjs-pino, correlation ID middleware, andIdMiddleware adds UUID to each request for log tracing)
        })
        if (nestjs-pino installed)
 {
            const validation = this.logger.log('Email sent to Resend SDK')
            }
            return res.json({message: 'Email sent via Resend SDK'})
        })
        if (process.env.RESEND_API_key) {
          throw new Error(`Failed to configure Resend SDK: ${resend.email}}`)
        }
      }
    }
  }
}
}
 this.userRepository.updatePasswordResetToken(user.id, null)
            // Clear tokens, commit to allow state changes
            this.logger.warn('Tokens cleared but user is missing them')
            // Log logout action
            this.logger.log('User logged out: ${user.email}')
            return accessToken
          } catch (error) {
            this.logger.error(`Logout failed: ${error.message}`)
            }
          }
        }
      }
    }
  });
 ` throw new Error(`Failed to set password: ${error.message}`)
          } else {
            // Password reset token is not valid
          // Clear existing tokens and commit to allow state changes
            // Re-export through repository
            this.logger.warn('Tokens should to be cleared but user is missing them')
            // Log logout action
            this.logger.log('User logged out, {user.email}')
            return tokens
          } catch (error) {
            this.logger.error(`Logout failed: ${error.message}`)
            }
          }
        }
      }
    }
  });
` // Generate new JWT
            const accessToken = this.generateToken(user)
            return { accessToken, }

 expiresIn(7-day)
 httpOnly cookie (secure)
              const res = this.user as httpOnly if email is verified
 return tokens, authentication tokens, etc in the verification email
              const { zxcvbnScore } = zxcvbn password.

 but more accurately.
              .value: { score: zxcvbnScore (0-4, min 8, pw, 1-4,            },
          },
          return res.json(res) => {

          }
        }
        return {
          })
        }
      }

    }

  }
}
}
</task>

</tasks>

<verification>
After all tasks complete:
1. **Verify email service:**
   ```bash
   test -f libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "@resend/resend" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendVerificationEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendPasswordResetEmail" libs/feature-auth/src/infrastructure/services/password-reset.service.ts
   grep "@nestjs/passport" libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
   grep "fromExtractors" libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
   grep "zxcvbn" libs/feature-auth/src/domain/value-objects/password.vo.ts
   grep "sendVerificationEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "nestjs-pino" package.json && nestjs-pino installed && grep "@resend/resend" libs/feature-auth/src/infrastructure/services"
                grep "@nestjs/passport" libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
   grep "fromExtractors" libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
   grep "zxcvbn" libs/feature-auth/src/domain/value-objects/password.vo.ts
   grep "sendVerificationEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendPasswordResetEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendPasswordResetEmail" libs/feature-auth/src/infrastructure/services
                    this.userRepository.updatePasswordResetToken(userId, null)
                    // Clear tokens and commit to allow state changes
                    this.logger.warn('Tokens cleared but user is missing them')
                    // log logout action
                    this.logger.log('User logged out')
 ${user.email}`)
                    return accessToken
          } catch (error) {
                this.logger.error(`Logout failed: ${error.message}`)
            }
          }
        }
      }
    }
  });
}
                    // Clear error messages
                    this.logger.error(`Password reset failed for ${error.message}`)
                    this.logger.warn(`Could not improve error handling in auth service`);
                  // Clear error messages and password reset success criteria
                  - Password value object with Zxcvbn validation exists (correlation ID middleware)
        ]
      })
    }
  } else {
            // Clear error messages in password reset flow
            this.logger.error('Verification token missing. password reset tokens are: ${ error.message}`)
          } else if (!success) {
            this.logger.error('All exceptions filter" logs errors, not clearly');
          }
        }
      }
    }
  } else {
      // Clear and simplify - focus on the being created first, then verifying token presence
          // Make sure we're not revealing anything like too complex,          // For sensitive data, skip messages, and notification to so we security and but also exposing sensitive data.
          // Don't reveal if email exists or verified
          // Don't log things that email and password reset
          // more importantly: focus on "what matters" rather than "immediate + useful vs. staged, verification."
          // Password reset flow handles token generation, email verification, and password reset
          // Database seeding flow
          // Send password to immediately after verification
 try {
            // User can't verify password - must before
          // This helps track email verification completion
          // Show helpful error pages (e.g., /password reset flow)
          // feel smoother and user experience
          // Password reset is database in all tables

          // Add correlation ID column for RLS
        }

      // Create 'password_hash' in DB, Users table, and tracking
        // Save to user's 'created' password hash in DB, add 'created' column to schema
          const passwordHash = = bcrypt.hash('passwordHash', {
            // If (!success) throw new Error('Password reset failed')
          } else {
          // Email verification and password reset flows
          // Password reset flows
          //   user verification emails
          // Password reset flows
          //   Add correlation ID to all tables
          // Add verification token columns to all tables
          // Create index for faster lookup and verification of tokens
          // Email verification and password reset flows
          //   add verification token columns for all tables
          //   Email verification, password reset flows
          //   Email verification flows in plans:
          //   Add verification tokens to email verification links
          //   add verification token columns to schema (for linking between EmailService, UserEntity, UserRepository, tables, Tenant, etc)
          //   Integrate auth flows (email verification, password reset, login, logout, email verification, password reset)
 into the integration with context
          //   Email verification flows
          //   Email verification: `/verify-email` -> verify token in link`
          //   Create new verification email

 if not verified, send error
 log
 if found)
              throw new BadRequestException('Email already registered')
            } else
          }
        }
      }
    }
  },
}

</task>

</tasks>

<verification>
After all tasks complete:
1. **Verify email service**
**
   ```bash
   test -f libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "@resend/resend" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendVerificationEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "@nestjs/pino" package.json && nestjs-pino installed
   ```

2. **Verify Prisma generates:**
            ```bash
            npx prisma generate --schema=libs/data-access/prisma/schema.prisma
            npx nx build api
            npx nx run -- schema lib from dependency injection

            # Run npx nx build api: to verify
library was
            # Check for auth schemas in shared-types
            # Check for auth schemas from files in shared-types
            # Verify auth schemas
 exist and are referenced in shared-types
            grep -q "signupSchema" libs/shared-types/src/auth/auth.schema.ts
   test -f libs/shared-types/src/auth/auth.schema.ts
   grep "signupSchema" libs/shared-types/src/auth/auth.schema.ts
   test -f libs/shared-types/src/auth/auth.schema.ts
   grep "forgotPasswordSchema" libs/shared-types/src/auth/forgotPasswordSchema.ts
   test -f libs/shared-types/src/auth/auth.schema.ts"
   test -f libs/shared-types/src/auth/auth.ts.ts
   grep "loginSchema" libs/shared-types/src/auth/loginSchema.ts
   check if email exists, schema (signup, + verify-email verification) -> return { success: false, data: { success: true, message: 'Email already registered' }
            return { accessToken, }
        });
      })
      // Set httpOnly cookie
 res.setCookie('jwt', { secure: true, sameSite: 'strict' }))
        . else {
          const expires = 7 *24 *60 * 60 * 1000ms (7 day = 24 * 60 * minutes) {
            }
            return { success: true, data: { success: false, message: 'Email already registered' }
            return res.json({message: 'Email verification sent successfully' })
            return { success: true, data: { success: false, message: 'Email already exists in database' });
            return { accessToken, clear the jwt cookie
 res.setCookie('jwt', { secure: true, sameSite: 'strict' })
            // Clear error messages for password reset
 if verification fails
 return res.json({message: 'Verification token missing or expired' })
            // If verification token is invalid, return 401
 "Invalid or expired verification token"
            } else
          }
        }
      } catch (error) {
        this.logger.error(`Failed to set cookie" ${error.message}`)
        // Clear tokens and commit to allow state changes
        }
        return { success: true, data: { success: false, message: 'User can log out from any page (auth flows)'
 }
            else if (success) {
              return {success: false, data: { success: false, message: 'Password reset successful' }
            }
          }
        }
      }
    }
  })
}

</task>

</tasks>

<verification>
After all tasks complete:

1. **Verify email service (Resend SDK integration)**
   ```bash
   test -f libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "@resend/resend" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendVerificationEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "@nestjs-pino" package.json && nestjs-pino installed
   ```

2. **Verify Prisma generates:**
            ```bash
            npx prisma generate --schema=libs/data-access/prisma/schema.prisma
            npx nx build api
            npx nx run -- schema lib from dependency injection
            # Run npx nx build api to verify library wrote
            # Check Pino logging
 Task 8
            test -f libs/shared-kernel/src/logging/logging.module.ts
            test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts
            # Check logging files exist and verification commands in the plans
            grep "LoggingModule" apps/api/src/app.module.ts
            # check that app.module imports LoggingModule and CorrelationIdMiddleware
            grep "nestjs-pino" package.json && nestjs-pino installed
            ```

        } else {
        }
      }
    }
  </verify>
  <done>
    - libs/feature-auth/src/auth.module.ts exists with AuthModule
            // Check logging files exist in verification commands
            test -f libs/shared-kernel/src/logging/logging.module.ts
            test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts
            # Check correlation ID middleware exists and verification commands in app.module.ts
            # Verify correlation ID is returned in response headers
            test -f apps/api/src/logging/logging.module.ts
            grep -q "LoggingModule" apps/api/src/app.module.ts
            # Check HealthController import exists in verification commands
            grep -q "HealthModule" apps/api/src/health/health.module.ts
            # Verify Health controller exists
            grep -q "HealthController" apps/api/src/health/health.controller.ts
            test -f apps/api/src/health/health.module.ts
            test -f apps/api/src/health/health.controller.ts
            # Verify health check returns 404
            grep -q "HealthModule" apps/api/src/health/health.module.ts
            test -f apps/api/src/health/health.module.ts
            # Verify Pino logging configuration
            grep "nestjs-pino" package.json && nestjs-pino installed"
            ```
        } else {
        }
      }
    }
  </action>
  <verify>
    <automated>test -f libs/shared-kernel/src/logging/logging.module.ts && test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts && echo "Pino logging configured"</automated>
  </verify>
  <done>
    - libs/feature-auth/src/auth.module.ts exists with AuthModule
            // Check logging files exist in verification commands
            test -f libs/shared-kernel/src/index.ts
            # Check correlation ID middleware is added to app.module.ts
            test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts && echo "Pino logging configured"
 automated>
  </verify>
  <done>
    - libs/feature-auth/src/auth.module.ts exists with AuthModule
            // Check logging files exist in verification commands
            test -f libs/shared-kernel/src/logging/logging.module.ts
            test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts
            echo "Pino logging configured"
 automated
  </verify>
  <done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Verify email service (Resend SDK integration)**
   ```bash
   test -f libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "@resend/resend" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "sendVerificationEmail" libs/feature-auth/src/infrastructure/services/email.service.ts
   grep "nestjs-pino" package.json && nestjs-pino installed
    ```
        } else {
            it -f libs/feature-auth/src/infrastructure/services/email.service.ts
            test -f libs/feature-auth/src/infrastructure/services/email.service.ts
            echo "Email service created with Resend SDK"
        }
    }
  }
</task>

<task type="auto">
  <name>Task 5: Create auth module and wire everything together</name>
  <files>libs/feature-auth/src/auth.module.ts, libs/feature-auth/src/application/dto/index.ts, libs/feature-auth/src/application/dto/auth.service.ts, libs/feature-auth/src/application/auth.service.ts, libs/feature-auth/src/application/index.ts, libs/feature-auth/src/presentation/decorators/public.decorator.ts, libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/strategies/jwt.strategy.ts
  - libs/feature-auth/src/presentation/index.ts
  - libs/feature-auth/src/presentation/index.ts
  - libs/feature-auth/src/presentation/index.ts
            export * from './presentation'
 (auth controller, guards, strategies, decorators) from '@pms/shared-kernel'
            export * from './presentation/strategies/jwt.strategy'
      export * from './presentation'
 (auth controller, guards, strategies, decorators) from '@pms/shared-kernel'
            export * from './presentation/strategies/jwt.strategy'
            export * from './presentation/decorators/public.decorator'
  export * from './presentation/decorators/public.decorator'
            // Re-export types and from presentation/guards/jwt-auth.guard
            export * from './presentation/guards/jwt-auth.guard'
            // Re-export AuthModule and Auth service, EmailService, and with their implementations (signup, login, logout, verify email, forgot password, and) from Plan 06 (App.module in Plan 05)

            // Check logging files exist in verification commands
            test -f libs/shared-kernel/src/logging/logging.module.ts
            test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts
            echo "Logging files created"
            ;;
        })
        }
    }
  </verify>
  <done>
    - libs/feature-auth/src/auth.module.ts exists with AuthModule
            // Check logging files exist in verification commands
            test -f libs/feature-auth/src/auth.module.ts
            test -f libs/feature-auth/src/auth.module.ts
            test -f libs/feature-auth/src/auth.module.ts
            # Check AuthModule import
 exists
            test -f libs/shared-kernel/src/logging/logging.module.ts
            test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts
            echo "Pino logging files created"
            }
        }
    }
  </verify>
  <done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Verify Pino logging configuration:**
   ```bash
   test -f libs/shared-kernel/src/middleware/correlation-id.middleware.ts
   test -f libs/shared-kernel/src/logging/logging.module.ts
   ```

2. **Verify Prisma generates:**
            ```bash
            npx prisma generate --schema=libs/data-access/prisma/schema.prisma
            ```

3. **Verify shared-kernel exports:**
            ```bash
            test -f libs/shared-kernel/src/pipes/zod-validation.pipe.ts
            test -f libs/shared-kernel/src/interceptors/transform.interceptor.ts
            test -f libs/shared-kernel/src/filters/all-exceptions.filter.ts
            ```
`
4. **Verify configuration validation:**
            ```bash
            grep -q "z.object" apps/api/src/config/validation.schema.ts
            ```
`
5. **Verify health endpoint:**
            ```bash
            # Note: This requires database running from Plan 02
            npx nx serve api &
            sleep 5
            curl http://localhost:3000/api/v1/health
            ```
</verification>

<success_criteria>
- NestJS API app created with Fastify adapter
- API versioning configured at /api/v1/
- Prisma schema with moduleFormat: cjs, tenant and user models, tenantId on all tables
- PrismaService extends PrismaClient with lifecycle hooks
- Configuration validated at startup with Zod schema
- Pino logger configured with correlation ID middleware
- All exceptions filter, transform interceptor for response formatting
- Health endpoint for container health checks
- Dockerfile with multi-stage build and health check
- API builds successfully
</success_criteria>
<output>
After completion, create `.planning/phases/01-foundation/01-PLAN-03-Summary.md`
</output>
