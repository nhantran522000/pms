---
phase: 05-health
plan: 05
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - libs/feature-health/src/infrastructure/email/email.service.ts
  - libs/feature-health/src/infrastructure/email/templates/health-digest.template.tsx
  - libs/feature-health/src/infrastructure/email/index.ts
  - libs/feature-health/src/health.module.ts
  - .env.example
autonomous: true
requirements:
  - HLTH-07
user_setup: []
---

<objective>
Integrate Resend email service with React Email templates for Sunday health digest delivery.

Purpose: Send weekly health digest emails with unsubscribe support.
Output: EmailService, React Email template, and Resend integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-health/05-CONTEXT.md

## Email Integration (from CONTEXT.md)
- Provider: Resend
- Templates: React Email with @react-email/components
- Unsubscribe: One-click link in footer
- Failure Handling: Retry 3x with exponential backoff
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install email dependencies</name>
  <files>
    package.json
  </files>
  <action>
    Install required packages:
    ```bash
    pnpm add resend @react-email/components @react-email/render
    ```
    Add RESEND_API_KEY to .env.example
  </action>
  <verify>
    <automated>grep -q "resend" package.json && grep -q "@react-email/components" package.json</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create React Email template</name>
  <files>
    libs/feature-health/src/infrastructure/email/templates/health-digest.template.tsx
  </files>
  <read_first>
    - .planning/phases/05-health/05-RESEARCH.md (template example)
  </read_first>
  <action>
    Create React Email template:
    - HealthDigestTemplate component
    - Props: userName, weekEnd, insights, recommendations, weekSummary, unsubscribeUrl
    - Sections: Greeting, Stats Summary, Insights, Recommendations, Footer with unsubscribe
  </action>
  <verify>
    <automated>test -f libs/feature-health/src/infrastructure/email/templates/health-digest.template.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Create EmailService</name>
  <files>
    libs/feature-health/src/infrastructure/email/email.service.ts
  </files>
  <action>
    Create EmailService with Resend integration:
    - @Injectable() decorator
    - Constructor with ConfigService
    - sendHealthDigest(email: string, digest: HealthDigest, unsubscribeUrl: string): Promise<void>
      - Render React template with @react-email/render
      - Send via Resend API
      - Include List-Unsubscribe header
    - Retry logic: 3x with exponential backoff
  </action>
  <verify>
    <automated>grep -q "export class EmailService" libs/feature-health/src/infrastructure/email/email.service.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Update HealthDigestJob to send emails</name>
  <files>
    libs/feature-health/src/infrastructure/jobs/health-digest.job.ts
  </files>
  <read_first>
    - libs/feature-health/src/infrastructure/jobs/health-digest.job.ts
  </read_first>
  <action>
    Update job to send emails after generating digest:
    - Inject EmailService
    - In handleJob(): after generateDigest(), call emailService.sendHealthDigest()
    - Get user email from User table via tenantId
    - Generate unsubscribeUrl with token
  </action>
  <verify>
    <automated>grep -q "emailService" libs/feature-health/src/infrastructure/jobs/health-digest.job.ts</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] Health digest sent via email every Sunday (HLTH-07)
- [ ] Uses Resend for delivery
- [ ] React Email template renders correctly
- [ ] Unsubscribe link in footer
- [ ] Retry logic handles failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-health/05-05-SUMMARY.md`
</output>
