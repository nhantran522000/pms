---
phase: 05-health
plan: 03
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - libs/feature-health/src/application/services/sleep.service.ts
  - libs/feature-health/src/application/services/workout.service.ts
  - libs/feature-health/src/presentation/controllers/sleep.controller.ts
  - libs/feature-health/src/presentation/controllers/workout.controller.ts
  - libs/feature-health/src/health.module.ts
autonomous: true
requirements:
  - HLTH-04
  - HLTH-05
user_setup: []

must_haves:
  truths:
    - "User can log sleep duration and quality (1-5 scale)"
    - "User can log workouts with type, duration, intensity as JSONB"
    - "Data stored in single HealthLog table with type discriminator"
    - "Sleep quality scale: 1=poor, 5=excellent"
  artifacts:
    - path: "libs/feature-health/src/application/services/sleep.service.ts"
      provides: "Sleep logging and history"
      exports: ["SleepService"]
    - path: "libs/feature-health/src/application/services/workout.service.ts"
      provides: "Workout logging and history"
      exports: ["WorkoutService"]
  key_links:
    - from: "libs/feature-health/src/presentation/controllers/sleep.controller.ts"
      to: "libs/feature-health/src/application/services/sleep.service.ts"
      via: "dependency injection"
---

<objective>
Add sleep tracking (duration, quality) and workout logging (type, duration, intensity) using the established HealthLog infrastructure.

Purpose: Enable comprehensive health tracking with sleep and exercise data stored as JSONB.
Output: SleepService, WorkoutService, and corresponding controllers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-health/05-CONTEXT.md
@.planning/phases/05-health/05-RESEARCH.md

## Data Structures (from CONTEXT.md)

```typescript
// Sleep data
interface SleepData {
  durationMinutes: number;
  quality: 1 | 2 | 3 | 4 | 5;  // 1=poor, 5=excellent
}

// Workout data
interface WorkoutData {
  type: string;        // 'running', 'cycling', 'weightlifting', etc.
  durationMinutes: number;
  intensity: 'low' | 'moderate' | 'high';
  caloriesBurned?: number;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SleepService</name>
  <files>
    libs/feature-health/src/application/services/sleep.service.ts
    libs/feature-health/src/application/services/index.ts
  </files>
  <read_first>
    - libs/feature-health/src/application/services/vitals.service.ts (service pattern)
    - libs/feature-health/src/infrastructure/repositories/health-log.repository.ts
  </read_first>
  <action>
    Create SleepService for sleep tracking:

    1. Create `libs/feature-health/src/application/services/sleep.service.ts`:
       - @Injectable() decorator
       - Constructor with HealthLogRepository
       - logSleep(dto: LogSleepDto): Promise<HealthLogEntity>
         - Validate durationMinutes (0-1440, max 24 hours)
         - Validate quality (1-5)
         - Create HealthLog with type=SLEEP
         - Data: { durationMinutes, quality }
       - getSleepHistory(options?: { startDate?: Date; endDate?: Date }): Promise<SleepHistoryResponse>
       - getSleepStats(startDate: Date, endDate: Date): Promise<{ avgDuration: number; avgQuality: number; totalNights: number }>
       - getSleepTrend(range: '30' | '90' | '365'): Promise<SleepTrendData>

    2. Update index.ts to export SleepService.
  </action>
  <verify>
    <automated>grep -q "export class SleepService" libs/feature-health/src/application/services/sleep.service.ts && grep -q "logSleep" libs/feature-health/src/application/services/sleep.service.ts</automated>
  </verify>
  <done>
    - SleepService with sleep logging
    - Duration and quality validation
    - History and stats methods
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SleepController</name>
  <files>
    libs/feature-health/src/presentation/controllers/sleep.controller.ts
    libs/feature-health/src/presentation/controllers/index.ts
  </files>
  <read_first>
    - libs/feature-health/src/presentation/controllers/vitals.controller.ts (controller pattern)
    - libs/feature-health/src/application/services/sleep.service.ts
  </read_first>
  <action>
    Create SleepController with sleep endpoints:

    1. Create `libs/feature-health/src/presentation/controllers/sleep.controller.ts`:
       - @Controller('health/sleep') decorator
       - Constructor with SleepService

       Endpoints:
       - POST /health/sleep - log sleep entry
       - GET /health/sleep - get sleep history
       - GET /health/sleep/stats - get sleep statistics
       - GET /health/sleep/trend - get sleep trend for charts

    2. Update index.ts to export SleepController.
  </action>
  <verify>
    <automated>grep -q "@Controller('health/sleep')" libs/feature-health/src/presentation/controllers/sleep.controller.ts && grep -q "POST /health/sleep" libs/feature-health/src/presentation/controllers/sleep.controller.ts</automated>
  </verify>
  <done>
    - SleepController with sleep logging endpoints
    - All endpoints validate input with Zod schemas
  </done>
</task>

<task type="auto">
  <name>Task 3: Create WorkoutService</name>
  <files>
    libs/feature-health/src/application/services/workout.service.ts
    libs/feature-health/src/application/services/index.ts
  </files>
  <read_first>
    - libs/feature-health/src/application/services/sleep.service.ts
    - libs/feature-health/src/infrastructure/repositories/health-log.repository.ts
  </read_first>
  <action>
    Create WorkoutService for workout tracking:

    1. Create `libs/feature-health/src/application/services/workout.service.ts`:
       - @Injectable() decorator
       - Constructor with HealthLogRepository
       - logWorkout(dto: LogWorkoutDto): Promise<HealthLogEntity>
         - Validate type (string, max 100 chars)
         - Validate durationMinutes (0-max)
         - Validate intensity ('low' | 'moderate' | 'high')
         - Optional caloriesBurned validation
         - Create HealthLog with type=WORKOUT
         - Data: { type, durationMinutes, intensity, caloriesBurned? }
       - getWorkoutHistory(options?: { startDate?: Date; endDate?: Date; type?: string }): Promise<WorkoutHistoryResponse>
       - getWorkoutStats(startDate: Date, endDate: Date): Promise<{ count: number; totalMinutes: number; avgIntensity: string; byType: Record<string, number> }>
       - getWorkoutTrend(range: '30' | '90' | '365'): Promise<WorkoutTrendData>

    2. Update index.ts to export WorkoutService.
  </action>
  <verify>
    <automated>grep -q "export class WorkoutService" libs/feature-health/src/application/services/workout.service.ts && grep -q "logWorkout" libs/feature-health/src/application/services/workout.service.ts</automated>
  </verify>
  <done>
    - WorkoutService with workout logging
    - Type, duration, intensity validation
    - History and stats methods
  </done>
</task>

<task type="auto">
  <name>Task 4: Create WorkoutController</name>
  <files>
    libs/feature-health/src/presentation/controllers/workout.controller.ts
    libs/feature-health/src/presentation/controllers/index.ts
  </files>
  <read_first>
    - libs/feature-health/src/presentation/controllers/sleep.controller.ts
    - libs/feature-health/src/application/services/workout.service.ts
  </read_first>
  <action>
    Create WorkoutController with workout endpoints:

    1. Create `libs/feature-health/src/presentation/controllers/workout.controller.ts`:
       - @Controller('health/workouts') decorator
       - Constructor with WorkoutService

       Endpoints:
       - POST /health/workouts - log workout entry
       - GET /health/workouts - get workout history (optional ?type filter)
       - GET /health/workouts/stats - get workout statistics
       - GET /health/workouts/trend - get workout trend for charts

    2. Update index.ts to export WorkoutController.
  </action>
  <verify>
    <automated>grep -q "@Controller('health/workouts')" libs/feature-health/src/presentation/controllers/workout.controller.ts && grep -q "POST /health/workouts" libs/feature-health/src/presentation/controllers/workout.controller.ts</automated>
  </verify>
  <done>
    - WorkoutController with workout logging endpoints
    - All endpoints validate input with Zod schemas
  </done>
</task>

<task type="auto">
  <name>Task 5: Update HealthModule with Sleep and Workout services</name>
  <files>
    libs/feature-health/src/health.module.ts
  </files>
  <read_first>
    - libs/feature-health/src/health.module.ts
  </read_first>
  <action>
    Update HealthModule to include all new services and controllers:

    - Add SleepService and WorkoutService to providers
    - Add SleepController and WorkoutController to controllers
    - Export SleepService and WorkoutService
  </action>
  <verify>
    <automated>grep -q "SleepService" libs/feature-health/src/health.module.ts && grep -q "WorkoutService" libs/feature-health/src/health.module.ts</automated>
  </verify>
  <done>
    - HealthModule updated with sleep and workout support
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `pnpm nx build feature-health` to verify compilation
2. Verify sleep logging stores JSONB: { durationMinutes: 480, quality: 4 }
3. Verify workout logging stores JSONB: { type: "running", durationMinutes: 30, intensity: "moderate" }
</verification>

<success_criteria>
- [ ] User can log sleep with duration and quality (HLTH-04)
- [ ] User can log workouts with type, duration, intensity (HLTH-05)
- [ ] Data stored as JSONB in HealthLog table
- [ ] Stats endpoints calculate averages and totals
- [ ] Trend endpoints return chart-compatible data
</success_criteria>

<output>
After completion, create `.planning/phases/05-health/05-03-SUMMARY.md`
</output>
