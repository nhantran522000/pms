---
phase: 05-health
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - libs/feature-health/src/application/services/health-digest.service.ts
  - libs/feature-health/src/infrastructure/jobs/health-digest.job.ts
  - libs/feature-health/src/health.module.ts
autonomous: true
requirements:
  - HLTH-06
user_setup: []

must_haves:
  truths:
    - "System generates weekly health digest via AI (ANALYZE task type)"
    - "Digest includes trend analysis, cross-metric correlations, recommendations"
    - "Tone: supportive/friendly, 5-7 paragraphs"
    - "If AI fails, send data-only digest without insights"
---

<objective>
Create AI-powered weekly health digest generation using the existing AiGatewayService with ANALYZE task type.

Purpose: Generate personalized health insights from weekly data.
Output: HealthDigestService with AI integration and pg-boss scheduling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-health/05-CONTEXT.md
@.planning/phases/05-health/05-RESEARCH.md

## AI Digest Format (from CONTEXT.md)
- Content: Trend analysis, cross-metric correlations, notable changes, recommendations
- Tone: Supportive/friendly
- Length: Medium (5-7 paragraphs)
- Personalization: Greeting, historical comparison, goal progress, achievements
- Failure Handling: Send data-only digest if AI fails
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HealthDigestService</name>
  <files>
    libs/feature-health/src/application/services/health-digest.service.ts
  </files>
  <read_first>
    - libs/shared-kernel/src/ai/ai-gateway.service.ts
    - libs/feature-financial/src/application/services/ai-categorization.service.ts
  </read_first>
  <action>
    Create HealthDigestService with AI integration:

    1. Create `libs/feature-health/src/application/services/health-digest.service.ts`:
       - @Injectable() decorator
       - Constructor with AiGatewayService, HealthLogRepository
       - generateDigest(tenantId: string, userId: string): Promise<HealthDigest>
         - Gather week's health data (weight, vitals, sleep, workouts)
         - Build DigestInput structure
         - Call AiGatewayService.execute({ taskType: 'ANALYZE', prompt, context })
         - Parse AI response into insights, recommendations, trends
         - On failure: return createDataOnlyDigest()
       - createDataOnlyDigest(input: DigestInput): HealthDigest
         - Fallback with raw stats, no AI insights
         - Include encouragement message
  </action>
  <verify>
    <automated>grep -q "export class HealthDigestService" libs/feature-health/src/application/services/health-digest.service.ts && grep -q "taskType: 'ANALYZE'" libs/feature-health/src/application/services/health-digest.service.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create HealthDigestJob with pg-boss</name>
  <files>
    libs/feature-health/src/infrastructure/jobs/health-digest.job.ts
  </files>
  <read_first>
    - libs/feature-financial/src/infrastructure/jobs/recurring-transaction.job.ts
  </read_first>
  <action>
    Create pg-boss job for Sunday digest delivery:

    1. Create `libs/feature-health/src/infrastructure/jobs/health-digest.job.ts`:
       - @Injectable() decorator
       - Implements OnModuleInit, OnModuleDestroy
       - SUNDAY_CRON = '0 9 * * 0' (every Sunday at 9 AM)
       - onModuleInit(): Start pg-boss, register worker, schedule job
       - handleJob(job): Get tenants with health data, generate and queue digests
       - triggerDigestManually(tenantId: string): For testing
  </action>
  <verify>
    <automated>grep -q "export class HealthDigestJob" libs/feature-health/src/infrastructure/jobs/health-digest.job.ts && grep -q "'0 9 \* \* 0'" libs/feature-health/src/infrastructure/jobs/health-digest.job.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update HealthModule with digest services</name>
  <files>
    libs/feature-health/src/health.module.ts
  </files>
  <read_first>
    - libs/feature-health/src/health.module.ts
  </read_first>
  <action>
    Add HealthDigestService and HealthDigestJob to module:
    - Add HealthDigestService to providers
    - Add HealthDigestJob to providers
    - Export HealthDigestService
  </action>
  <verify>
    <automated>grep -q "HealthDigestService" libs/feature-health/src/health.module.ts</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] System generates weekly health digest via AI (HLTH-06)
- [ ] Uses ANALYZE task type from AiGatewayService
- [ ] Digest includes correlations and recommendations
- [ ] pg-boss schedules Sunday 9 AM delivery
</success_criteria>

<output>
After completion, create `.planning/phases/05-health/05-04-SUMMARY.md`
</output>
