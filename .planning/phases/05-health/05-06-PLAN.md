---
phase: 05-health
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - libs/feature-health/src/application/services/health-dashboard.service.ts
  - libs/feature-health/src/presentation/controllers/health-dashboard.controller.ts
  - libs/feature-health/src/health.module.ts
autonomous: true
requirements:
  - HLTH-08
user_setup: []
---

<objective>
Create health dashboard with metrics summary and trend visualization endpoints.

Purpose: Provide aggregated health data for dashboard display.
Output: HealthDashboardService and controller with summary endpoints.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-health/05-CONTEXT.md

## Dashboard Requirements (from CONTEXT.md)
- Shows health metrics summary with trends (weight, vitals, sleep, workouts)
- Trend indicators: up/down/stable
- Logging streak display
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HealthDashboardService</name>
  <files>
    libs/feature-health/src/application/services/health-dashboard.service.ts
  </files>
  <read_first>
    - libs/feature-health/src/application/services/health-log.service.ts
    - libs/feature-health/src/application/services/sleep.service.ts
    - libs/feature-health/src/application/services/workout.service.ts
  </read_first>
  <action>
    Create HealthDashboardService:
    - @Injectable() decorator
    - Constructor with all health services
    - getDashboard(): Promise<HealthDashboardResponse>
      - Weight summary: latest, trend, entries this week
      - Vitals summary: latest BP, HR, categories
      - Sleep summary: avg duration, avg quality, trend
      - Workout summary: count this week, total minutes, by type
      - Logging streak: consecutive days with any entry
      - Achievements: streak milestones, consistency badges
  </action>
  <verify>
    <automated>grep -q "export class HealthDashboardService" libs/feature-health/src/application/services/health-dashboard.service.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create HealthDashboardController</name>
  <files>
    libs/feature-health/src/presentation/controllers/health-dashboard.controller.ts
  </files>
  <action>
    Create HealthDashboardController:
    - @Controller('health/dashboard') decorator
    - GET /health/dashboard - get full dashboard
    - GET /health/dashboard/weight - weight summary only
    - GET /health/dashboard/vitals - vitals summary only
    - GET /health/dashboard/sleep - sleep summary only
    - GET /health/dashboard/workouts - workout summary only
  </action>
  <verify>
    <automated>grep -q "@Controller('health/dashboard')" libs/feature-health/src/presentation/controllers/health-dashboard.controller.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update HealthModule</name>
  <files>
    libs/feature-health/src/health.module.ts
  </files>
  <action>
    Add HealthDashboardService and HealthDashboardController to module.
  </action>
  <verify>
    <automated>grep -q "HealthDashboardService" libs/feature-health/src/health.module.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Verify build</name>
  <files>
  </files>
  <action>
    Run build verification:
    ```bash
    pnpm nx build feature-health
    ```
  </action>
  <verify>
    <automated>pnpm nx build feature-health 2>&1 | grep -q "Successfully ran target build"</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- [ ] Dashboard shows health metrics summary with trends (HLTH-08)
- [ ] Weight, vitals, sleep, workout summaries available
- [ ] Trend indicators (up/down/stable) calculated
- [ ] Logging streak displayed
- [ ] All services compile successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-health/05-06-SUMMARY.md`
</output>
