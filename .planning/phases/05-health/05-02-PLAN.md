---
phase: 05-health
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - libs/shared-types/src/health/health.schema.ts
  - libs/feature-health/src/application/services/vitals.service.ts
  - libs/feature-health/src/presentation/controllers/vitals.controller.ts
  - libs/feature-health/src/health.module.ts
autonomous: true
requirements:
  - HLTH-03
user_setup: []

must_haves:
  truths:
    - "User can log blood pressure (systolic/diastolic) as JSONB"
    - "User can log heart rate (bpm) as JSONB"
    - "Data stored in single HealthLog table with type discriminator"
    - "Reference ranges for vitals stored in code constants"
  artifacts:
    - path: "libs/feature-health/src/application/services/vitals.service.ts"
      provides: "Vitals-specific logging and retrieval"
      exports: ["VitalsService"]
    - path: "libs/feature-health/src/presentation/controllers/vitals.controller.ts"
      provides: "REST API endpoints for vitals"
      exports: ["VitalsController"]
  key_links:
    - from: "libs/feature-health/src/presentation/controllers/vitals.controller.ts"
      to: "libs/feature-health/src/application/services/vitals.service.ts"
      via: "dependency injection"
---

<objective>
Add vitals logging (blood pressure, heart rate) with JSONB storage using the established HealthLog infrastructure.

Purpose: Enable tracking of vital signs with type-specific validation and reference range support.
Output: VitalsService and VitalsController for blood pressure and heart rate logging.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-health/05-CONTEXT.md
@.planning/phases/05-health/05-RESEARCH.md

## Reference Ranges (from RESEARCH.md)

```typescript
export const HEALTH_REFERENCE_RANGES = {
  bloodPressure: {
    systolic: {
      low: 90,
      normal: { min: 90, max: 120 },
      elevated: { min: 120, max: 130 },
      high: 130,
    },
    diastolic: {
      low: 60,
      normal: { min: 60, max: 80 },
      high: 80,
    },
  },
  heartRate: {
    resting: {
      low: 60,
      normal: { min: 60, max: 100 },
      high: 100,
    },
  },
};
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reference ranges constants</name>
  <files>
    libs/feature-health/src/domain/constants/reference-ranges.ts
    libs/feature-health/src/domain/constants/index.ts
  </files>
  <read_first>
    - libs/feature-health/src/domain/entities/health-log.entity.ts
  </read_first>
  <action>
    Create reference ranges constants file:

    1. Create `libs/feature-health/src/domain/constants/reference-ranges.ts`:
       - Export HEALTH_REFERENCE_RANGES object with blood pressure and heart rate ranges
       - Export getBloodPressureCategory(systolic: number, diastolic: number): 'normal' | 'elevated' | 'high' | 'low'
       - Export getHeartRateCategory(bpm: number): 'low' | 'normal' | 'high'

    2. Create index.ts to export constants.
  </action>
  <verify>
    <automated>grep -q "HEALTH_REFERENCE_RANGES" libs/feature-health/src/domain/constants/reference-ranges.ts && grep -q "getBloodPressureCategory" libs/feature-health/src/domain/constants/reference-ranges.ts</automated>
  </verify>
  <done>
    - Reference ranges defined in code constants
    - Category helper functions for vitals assessment
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VitalsService</name>
  <files>
    libs/feature-health/src/application/services/vitals.service.ts
    libs/feature-health/src/application/services/index.ts
  </files>
  <read_first>
    - libs/feature-health/src/application/services/health-log.service.ts
    - libs/feature-health/src/domain/constants/reference-ranges.ts
  </read_first>
  <action>
    Create VitalsService extending health log functionality:

    1. Create `libs/feature-health/src/application/services/vitals.service.ts`:
       - @Injectable() decorator
       - Constructor with HealthLogRepository
       - logBloodPressure(dto: LogBloodPressureDto): Promise<HealthLogEntity>
         - Validate systolic (60-250) and diastolic (40-150) ranges
         - Create HealthLog with type=BLOOD_PRESSURE
         - Data: { systolic, diastolic }
       - logHeartRate(dto: LogHeartRateDto): Promise<HealthLogEntity>
         - Validate bpm (30-220)
         - Create HealthLog with type=HEART_RATE
         - Data: { bpm }
       - getBloodPressureHistory(options?: { startDate?: Date; endDate?: Date }): Promise<VitalsHistoryResponse>
       - getHeartRateHistory(options?: { startDate?: Date; endDate?: Date }): Promise<VitalsHistoryResponse>
       - getLatestVitals(): Promise<{ bloodPressure?: BloodPressureLog; heartRate?: HeartRateLog }>

    2. Update index.ts to export VitalsService.
  </action>
  <verify>
    <automated>grep -q "export class VitalsService" libs/feature-health/src/application/services/vitals.service.ts && grep -q "logBloodPressure" libs/feature-health/src/application/services/vitals.service.ts</automated>
  </verify>
  <done>
    - VitalsService with blood pressure and heart rate logging
    - Input validation with physiological ranges
    - History retrieval methods
  </done>
</task>

<task type="auto">
  <name>Task 3: Create VitalsController</name>
  <files>
    libs/feature-health/src/presentation/controllers/vitals.controller.ts
    libs/feature-health/src/presentation/controllers/index.ts
  </files>
  <read_first>
    - libs/feature-health/src/presentation/controllers/health-log.controller.ts
    - libs/feature-health/src/application/services/vitals.service.ts
  </read_first>
  <action>
    Create VitalsController with vitals-specific endpoints:

    1. Create `libs/feature-health/src/presentation/controllers/vitals.controller.ts`:
       - @Controller('health/vitals') decorator
       - Constructor with VitalsService

       Endpoints:
       - POST /health/vitals/blood-pressure - log blood pressure
       - POST /health/vitals/heart-rate - log heart rate
       - GET /health/vitals/blood-pressure - get blood pressure history
       - GET /health/vitals/heart-rate - get heart rate history
       - GET /health/vitals/latest - get latest vitals summary

    2. Update index.ts to export VitalsController.
  </action>
  <verify>
    <automated>grep -q "@Controller('health/vitals')" libs/feature-health/src/presentation/controllers/vitals.controller.ts && grep -q "POST.*blood-pressure" libs/feature-health/src/presentation/controllers/vitals.controller.ts</automated>
  </verify>
  <done>
    - VitalsController with blood pressure and heart rate endpoints
    - All endpoints validate input with Zod schemas
    - Consistent response format
  </done>
</task>

<task type="auto">
  <name>Task 4: Update HealthModule with VitalsService</name>
  <files>
    libs/feature-health/src/health.module.ts
  </files>
  <read_first>
    - libs/feature-health/src/health.module.ts
  </read_first>
  <action>
    Update HealthModule to include VitalsService and VitalsController:

    - Add VitalsService to providers
    - Add VitalsController to controllers
    - Export VitalsService
  </action>
  <verify>
    <automated>grep -q "VitalsService" libs/feature-health/src/health.module.ts && grep -q "VitalsController" libs/feature-health/src/health.module.ts</automated>
  </verify>
  <done>
    - HealthModule updated with vitals support
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `pnpm nx build feature-health` to verify compilation
2. Verify blood pressure logging stores JSONB correctly: { systolic: 120, diastolic: 80 }
3. Verify heart rate logging stores JSONB correctly: { bpm: 72 }
</verification>

<success_criteria>
- [ ] User can log blood pressure with systolic/diastolic values (HLTH-03)
- [ ] User can log heart rate with bpm value (HLTH-03)
- [ ] Data stored as JSONB in HealthLog table
- [ ] Reference ranges available for categorization
- [ ] History endpoints return paginated results
</success_criteria>

<output>
After completion, create `.planning/phases/05-health/05-02-SUMMARY.md`
</output>
