---
phase: 03-financial-module
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - libs/feature-financial/src/infrastructure/repositories/account.repository.ts
  - libs/feature-financial/src/infrastructure/repositories/index.ts
  - libs/feature-financial/src/application/services/account.service.ts
  - libs/feature-financial/src/application/services/index.ts
  - libs/feature-financial/src/presentation/controllers/account.controller.ts
  - libs/feature-financial/src/presentation/controllers/index.ts
  - libs/feature-financial/src/financial.module.ts
  - libs/shared-types/src/financial/financial.schema.ts
autonomous: true
requirements: [FIN-04, FIN-05]
user_setup: []

must_haves:
  truths:
    - User can create accounts with name, type (checking/savings/cash/credit), initial balance
    - User can view all accounts with current balance totals
    - User can archive accounts (soft delete)
    - Account balance updates when transactions are added/modified/deleted
  artifacts:
    - path: libs/feature-financial/src/infrastructure/repositories/account.repository.ts
      provides: Account database operations with balance tracking
      exports: ["AccountRepository"]
    - path: libs/feature-financial/src/application/services/account.service.ts
      provides: Account business logic
      exports: ["AccountService"]
    - path: libs/feature-financial/src/presentation/controllers/account.controller.ts
      provides: REST API endpoints
      contains: "@Controller('accounts')"
  key_links:
    - from: AccountService
      to: AccountRepository
      via: constructor injection
      pattern: "constructor(private readonly accountRepository"
    - from: AccountRepository.updateBalance
      to: Prisma.account.update
      via: atomic balance update
      pattern: "currentBalance"
---

<objective>
Implement Account CRUD with running balance totals. Users can create multiple accounts (checking, savings, cash, credit) and track balances.

Purpose: Enable multi-account financial tracking with real-time balance calculations.
Output: Account repository with balance tracking, service, and controller.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-financial-module/03-01-PLAN.md

<interfaces>
<!-- Key types from Plan 01 -->

From libs/feature-financial/src/domain/entities/account.entity.ts:
```typescript
export type AccountType = 'checking' | 'savings' | 'cash' | 'credit';

export class AccountEntity {
  public readonly id: string;
  public readonly tenantId: string;
  public readonly name: string;
  public readonly type: AccountType;
  public readonly initialBalance: Money;
  public readonly currentBalance: Money;
  public readonly icon: string | null;
  public readonly color: string | null;
  public readonly isArchived: boolean;

  static fromPrisma(account: Account): AccountEntity;
  isActive(): boolean;
  getBalanceChange(): Money;
  toJSON(): object;
}
```

From libs/feature-financial/src/domain/value-objects/money.vo.ts:
```typescript
export class Money {
  static fromDecimal(value: Decimal | string | number): Money;
  static fromPrisma(value: Decimal): Money;
  add(other: Money): Money;
  subtract(other: Money): Money;
  toPrisma(): Decimal;
  toString(): string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Account schemas to shared-types</name>
  <files>libs/shared-types/src/financial/financial.schema.ts</files>
  <read_first>
    - libs/shared-types/src/financial/financial.schema.ts (existing schemas)
  </read_first>
  <action>
Add Account schemas to the existing financial.schema.ts file:

```typescript
// Account types
export const AccountTypeSchema = z.enum(['checking', 'savings', 'cash', 'credit']);
export type AccountType = z.infer<typeof AccountTypeSchema>;

// Create Account
export const CreateAccountSchema = z.object({
  name: z.string().min(1).max(100),
  type: AccountTypeSchema,
  initialBalance: z.string().regex(/^-?\d+(\.\d{1,2})?$/).default('0'),
  icon: z.string().max(50).optional(),
  color: z.string().regex(/^#[0-9A-Fa-f]{6}$/).optional(),
});
export type CreateAccountDto = z.infer<typeof CreateAccountSchema>;

// Update Account
export const UpdateAccountSchema = z.object({
  name: z.string().min(1).max(100).optional(),
  icon: z.string().max(50).optional(),
  color: z.string().regex(/^#[0-9A-Fa-f]{6}$/).optional(),
});
export type UpdateAccountDto = z.infer<typeof UpdateAccountSchema>;

// Account response
export const AccountResponseSchema = z.object({
  id: z.string(),
  tenantId: z.string(),
  name: z.string(),
  type: AccountTypeSchema,
  initialBalance: z.string(),
  currentBalance: z.string(),
  icon: z.string().nullable(),
  color: z.string().nullable(),
  isArchived: z.boolean(),
  balanceChange: z.string().optional(),
  createdAt: z.date(),
  updatedAt: z.date(),
});
export type AccountResponse = z.infer<typeof AccountResponseSchema>;

// Account summary with transaction count
export const AccountSummarySchema = AccountResponseSchema.extend({
  transactionCount: z.number(),
});
export type AccountSummary = z.infer<typeof AccountSummarySchema>;
```
  </action>
  <verify>
    <automated>grep -q "CreateAccountSchema" libs/shared-types/src/financial/financial.schema.ts && grep -q "AccountTypeSchema" libs/shared-types/src/financial/financial.schema.ts</automated>
  </verify>
  <done>Account Zod schemas exist with Create, Update, Response, and Summary types</done>
</task>

<task type="auto">
  <name>Task 2: Create AccountRepository with balance tracking</name>
  <files>
    - libs/feature-financial/src/infrastructure/repositories/account.repository.ts
    - libs/feature-financial/src/infrastructure/repositories/index.ts
  </files>
  <read_first>
    - libs/feature-financial/src/infrastructure/repositories/category.repository.ts (repository pattern)
    - libs/feature-financial/src/domain/entities/account.entity.ts (entity)
    - libs/feature-financial/src/domain/value-objects/money.vo.ts (Money VO)
  </read_first>
  <action>
Create AccountRepository with balance update methods.

Create libs/feature-financial/src/infrastructure/repositories/account.repository.ts:
```typescript
import { Injectable, ConflictException } from '@nestjs/common';
import { PrismaService } from '@pms/data-access';
import { AccountEntity, AccountType } from '../../domain/entities/account.entity';
import { Money } from '../../domain/value-objects/money.vo';
import { CreateAccountDto, UpdateAccountDto } from '@pms/shared-types';
import { Decimal } from '@prisma/client/runtime/library';

export interface CreateAccountInput extends Omit<CreateAccountDto, 'initialBalance'> {
  tenantId: string;
  initialBalance: Money;
}

@Injectable()
export class AccountRepository {
  constructor(private readonly prisma: PrismaService) {}

  async create(input: CreateAccountInput): Promise<AccountEntity> {
    const account = await this.prisma.account.create({
      data: {
        tenantId: input.tenantId,
        name: input.name,
        type: input.type,
        initialBalance: input.initialBalance.toPrisma(),
        currentBalance: input.initialBalance.toPrisma(),
        icon: input.icon,
        color: input.color,
        isArchived: false,
      },
    });

    return AccountEntity.fromPrisma(account);
  }

  async findById(id: string, tenantId: string): Promise<AccountEntity | null> {
    const account = await this.prisma.account.findFirst({
      where: { id, tenantId },
    });
    return account ? AccountEntity.fromPrisma(account) : null;
  }

  async findByName(name: string, tenantId: string): Promise<AccountEntity | null> {
    const account = await this.prisma.account.findFirst({
      where: { name, tenantId },
    });
    return account ? AccountEntity.fromPrisma(account) : null;
  }

  async findAll(tenantId: string, includeArchived = false): Promise<AccountEntity[]> {
    const accounts = await this.prisma.account.findMany({
      where: {
        tenantId,
        ...(includeArchived ? {} : { isArchived: false }),
      },
      orderBy: { name: 'asc' },
    });
    return accounts.map((a) => AccountEntity.fromPrisma(a));
  }

  async findByType(tenantId: string, type: AccountType): Promise<AccountEntity[]> {
    const accounts = await this.prisma.account.findMany({
      where: { tenantId, type, isArchived: false },
      orderBy: { name: 'asc' },
    });
    return accounts.map((a) => AccountEntity.fromPrisma(a));
  }

  async update(id: string, tenantId: string, input: UpdateAccountDto): Promise<AccountEntity> {
    const account = await this.prisma.account.update({
      where: { id },
      data: {
        name: input.name,
        icon: input.icon,
        color: input.color,
      },
    });

    return AccountEntity.fromPrisma(account);
  }

  async archive(id: string, tenantId: string): Promise<AccountEntity> {
    const account = await this.prisma.account.update({
      where: { id },
      data: { isArchived: true },
    });

    return AccountEntity.fromPrisma(account);
  }

  async unarchive(id: string, tenantId: string): Promise<AccountEntity> {
    const account = await this.prisma.account.update({
      where: { id },
      data: { isArchived: false },
    });

    return AccountEntity.fromPrisma(account);
  }

  /**
   * Update account balance by applying a transaction amount
   * Used when creating/modifying/deleting transactions
   */
  async updateBalance(
    accountId: string,
    tenantId: string,
    amount: Money,
  ): Promise<AccountEntity> {
    const account = await this.prisma.account.update({
      where: { id: accountId },
      data: {
        currentBalance: {
          increment: amount.toPrisma(),
        },
      },
    });

    return AccountEntity.fromPrisma(account);
  }

  /**
   * Recalculate account balance from all transactions
   * Used for data integrity checks or after bulk operations
   */
  async recalculateBalance(accountId: string, tenantId: string): Promise<AccountEntity> {
    // Get account's initial balance
    const account = await this.prisma.account.findFirst({
      where: { id: accountId, tenantId },
    });

    if (!account) {
      throw new Error('Account not found');
    }

    // Sum all transactions
    const result = await this.prisma.transaction.aggregate({
      where: {
        accountId,
        isDeleted: false,
      },
      _sum: {
        amount: true,
      },
    });

    const initialBalance = Money.fromPrisma(account.initialBalance);
    const transactionSum = result._sum.amount
      ? Money.fromPrisma(result._sum.amount)
      : Money.zero();

    // Calculate: income adds, expense subtracts
    // The sum already accounts for type via signed amounts
    const newBalance = initialBalance.add(transactionSum);

    // Update balance
    const updated = await this.prisma.account.update({
      where: { id: accountId },
      data: { currentBalance: newBalance.toPrisma() },
    });

    return AccountEntity.fromPrisma(updated);
  }

  async countTransactions(id: string, tenantId: string): Promise<number> {
    return this.prisma.transaction.count({
      where: { accountId: id, isDeleted: false },
    });
  }

  async getTotalBalance(tenantId: string): Promise<Money> {
    const result = await this.prisma.account.aggregate({
      where: { tenantId, isArchived: false },
      _sum: { currentBalance: true },
    });

    return result._sum.currentBalance
      ? Money.fromPrisma(result._sum.currentBalance)
      : Money.zero();
  }
}
```

Update libs/feature-financial/src/infrastructure/repositories/index.ts:
```typescript
export * from './category.repository';
export * from './account.repository';
```
  </action>
  <verify>
    <automated>grep -q "class AccountRepository" libs/feature-financial/src/infrastructure/repositories/account.repository.ts && grep -q "updateBalance" libs/feature-financial/src/infrastructure/repositories/account.repository.ts && grep -q "getTotalBalance" libs/feature-financial/src/infrastructure/repositories/account.repository.ts</automated>
  </verify>
  <done>AccountRepository exists with CRUD, balance tracking, and total balance calculation</done>
</task>

<task type="auto">
  <name>Task 3: Create AccountService</name>
  <files>
    - libs/feature-financial/src/application/services/account.service.ts
    - libs/feature-financial/src/application/services/index.ts
  </files>
  <read_first>
    - libs/feature-financial/src/application/services/category.service.ts (service pattern)
    - libs/feature-financial/src/infrastructure/repositories/account.repository.ts (repository)
  </read_first>
  <action>
Create AccountService with balance management.

Create libs/feature-financial/src/application/services/account.service.ts:
```typescript
import { Injectable, NotFoundException, Logger } from '@nestjs/common';
import { AccountRepository } from '../../infrastructure/repositories/account.repository';
import { AccountEntity } from '../../domain/entities/account.entity';
import { Money } from '../../domain/value-objects/money.vo';
import { CreateAccountDto, UpdateAccountDto, AccountSummary } from '@pms/shared-types';
import { getTenantId } from '@pms/data-access';

@Injectable()
export class AccountService {
  private readonly logger = new Logger(AccountService.name);

  constructor(private readonly accountRepository: AccountRepository) {}

  async create(dto: CreateAccountDto): Promise<AccountEntity> {
    const tenantId = getTenantId();
    this.logger.log(`Creating account: ${dto.name}`);

    // Check for duplicate name
    const existing = await this.accountRepository.findByName(dto.name, tenantId);
    if (existing) {
      throw new Error(`Account with name "${dto.name}" already exists`);
    }

    const initialBalance = Money.fromDecimal(dto.initialBalance);

    return this.accountRepository.create({
      ...dto,
      tenantId,
      initialBalance,
    });
  }

  async findById(id: string): Promise<AccountEntity> {
    const tenantId = getTenantId();
    const account = await this.accountRepository.findById(id, tenantId);

    if (!account) {
      throw new NotFoundException('Account not found');
    }

    return account;
  }

  async findAll(includeArchived = false): Promise<AccountEntity[]> {
    const tenantId = getTenantId();
    return this.accountRepository.findAll(tenantId, includeArchived);
  }

  async findByType(type: 'checking' | 'savings' | 'cash' | 'credit'): Promise<AccountEntity[]> {
    const tenantId = getTenantId();
    return this.accountRepository.findByType(tenantId, type);
  }

  async getSummaries(includeArchived = false): Promise<AccountSummary[]> {
    const tenantId = getTenantId();
    const accounts = await this.accountRepository.findAll(tenantId, includeArchived);

    const summaries: AccountSummary[] = await Promise.all(
      accounts.map(async (account) => {
        const transactionCount = await this.accountRepository.countTransactions(
          account.id,
          tenantId,
        );

        return {
          ...account.toJSON(),
          balanceChange: account.getBalanceChange().toString(),
          transactionCount,
        };
      }),
    );

    return summaries;
  }

  async update(id: string, dto: UpdateAccountDto): Promise<AccountEntity> {
    const tenantId = getTenantId();

    // Verify account exists
    const existing = await this.accountRepository.findById(id, tenantId);
    if (!existing) {
      throw new NotFoundException('Account not found');
    }

    // Check for duplicate name if changing
    if (dto.name && dto.name !== existing.name) {
      const duplicate = await this.accountRepository.findByName(dto.name, tenantId);
      if (duplicate) {
        throw new Error(`Account with name "${dto.name}" already exists`);
      }
    }

    this.logger.log(`Updating account: ${id}`);
    return this.accountRepository.update(id, tenantId, dto);
  }

  async archive(id: string): Promise<AccountEntity> {
    const tenantId = getTenantId();

    const existing = await this.accountRepository.findById(id, tenantId);
    if (!existing) {
      throw new NotFoundException('Account not found');
    }

    this.logger.log(`Archiving account: ${id}`);
    return this.accountRepository.archive(id, tenantId);
  }

  async unarchive(id: string): Promise<AccountEntity> {
    const tenantId = getTenantId();

    const existing = await this.accountRepository.findById(id, tenantId);
    if (!existing) {
      throw new NotFoundException('Account not found');
    }

    this.logger.log(`Unarchiving account: ${id}`);
    return this.accountRepository.unarchive(id, tenantId);
  }

  async getTotalBalance(): Promise<{ balance: string }> {
    const tenantId = getTenantId();
    const total = await this.accountRepository.getTotalBalance(tenantId);
    return { balance: total.toString() };
  }

  async recalculateBalance(id: string): Promise<AccountEntity> {
    const tenantId = getTenantId();

    const existing = await this.accountRepository.findById(id, tenantId);
    if (!existing) {
      throw new NotFoundException('Account not found');
    }

    this.logger.log(`Recalculating balance for account: ${id}`);
    return this.accountRepository.recalculateBalance(id, tenantId);
  }
}
```

Update libs/feature-financial/src/application/services/index.ts:
```typescript
export * from './category.service';
export * from './account.service';
```
  </action>
  <verify>
    <automated>grep -q "class AccountService" libs/feature-financial/src/application/services/account.service.ts && grep -q "getTotalBalance" libs/feature-financial/src/application/services/account.service.ts</automated>
  </verify>
  <done>AccountService exists with CRUD, balance management, and total balance calculation</done>
</task>

<task type="auto">
  <name>Task 4: Create AccountController</name>
  <files>
    - libs/feature-financial/src/presentation/controllers/account.controller.ts
    - libs/feature-financial/src/presentation/controllers/index.ts
    - libs/feature-financial/src/financial.module.ts
  </files>
  <read_first>
    - libs/feature-financial/src/presentation/controllers/category.controller.ts (controller pattern)
    - libs/feature-financial/src/application/services/account.service.ts (service)
    - libs/feature-financial/src/financial.module.ts (module)
  </read_first>
  <action>
Create AccountController with REST endpoints.

Create libs/feature-financial/src/presentation/controllers/account.controller.ts:
```typescript
import {
  Controller,
  Get,
  Post,
  Put,
  Delete,
  Body,
  Param,
  Query,
} from '@nestjs/common';
import { AccountService } from '../../application/services/account.service';
import { ZodValidationPipe } from '@pms/shared-kernel';
import {
  CreateAccountSchema,
  CreateAccountDto,
  UpdateAccountSchema,
  UpdateAccountDto,
} from '@pms/shared-types';
import { z } from 'zod';

const BooleanQuerySchema = z.object({ includeArchived: z.enum(['true', 'false']).optional() });

@Controller('accounts')
export class AccountController {
  constructor(private readonly accountService: AccountService) {}

  @Post()
  async create(
    @Body(new ZodValidationPipe(CreateAccountSchema)) dto: CreateAccountDto,
  ) {
    const account = await this.accountService.create(dto);
    return {
      success: true,
      data: account.toJSON(),
    };
  }

  @Get()
  async findAll(@Query('includeArchived') includeArchived?: string) {
    const accounts = await this.accountService.findAll(includeArchived === 'true');
    return {
      success: true,
      data: accounts.map((a) => a.toJSON()),
    };
  }

  @Get('summaries')
  async getSummaries(@Query('includeArchived') includeArchived?: string) {
    const summaries = await this.accountService.getSummaries(includeArchived === 'true');
    return {
      success: true,
      data: summaries,
    };
  }

  @Get('total-balance')
  async getTotalBalance() {
    const result = await this.accountService.getTotalBalance();
    return {
      success: true,
      data: result,
    };
  }

  @Get(':id')
  async findById(@Param('id') id: string) {
    const account = await this.accountService.findById(id);
    return {
      success: true,
      data: account.toJSON(),
    };
  }

  @Put(':id')
  async update(
    @Param('id') id: string,
    @Body(new ZodValidationPipe(UpdateAccountSchema)) dto: UpdateAccountDto,
  ) {
    const account = await this.accountService.update(id, dto);
    return {
      success: true,
      data: account.toJSON(),
    };
  }

  @Put(':id/archive')
  async archive(@Param('id') id: string) {
    const account = await this.accountService.archive(id);
    return {
      success: true,
      data: account.toJSON(),
    };
  }

  @Put(':id/unarchive')
  async unarchive(@Param('id') id: string) {
    const account = await this.accountService.unarchive(id);
    return {
      success: true,
      data: account.toJSON(),
    };
  }

  @Post(':id/recalculate-balance')
  async recalculateBalance(@Param('id') id: string) {
    const account = await this.accountService.recalculateBalance(id);
    return {
      success: true,
      data: account.toJSON(),
    };
  }
}
```

Update libs/feature-financial/src/presentation/controllers/index.ts:
```typescript
export * from './category.controller';
export * from './account.controller';
```

Update libs/feature-financial/src/financial.module.ts:
```typescript
import { Module } from '@nestjs/common';
import { PrismaModule } from '@pms/data-access';
import { CategoryRepository } from './infrastructure/repositories/category.repository';
import { AccountRepository } from './infrastructure/repositories/account.repository';
import { CategoryService } from './application/services/category.service';
import { AccountService } from './application/services/account.service';
import { CategoryController } from './presentation/controllers/category.controller';
import { AccountController } from './presentation/controllers/account.controller';

@Module({
  imports: [PrismaModule],
  controllers: [CategoryController, AccountController],
  providers: [CategoryService, AccountService, CategoryRepository, AccountRepository],
  exports: [CategoryService, AccountService],
})
export class FinancialModule {}
```
  </action>
  <verify>
    <automated>grep -q "@Controller('accounts')" libs/feature-financial/src/presentation/controllers/account.controller.ts && grep -q "AccountController" libs/feature-financial/src/financial.module.ts</automated>
  </verify>
  <done>AccountController exists with CRUD and balance endpoints, registered in FinancialModule</done>
</task>

</tasks>

<verification>
- POST /api/v1/accounts creates an account
- GET /api/v1/accounts lists all accounts
- GET /api/v1/accounts/summaries returns accounts with transaction counts
- GET /api/v1/accounts/total-balance returns sum of all account balances
- PUT /api/v1/accounts/:id/archive archives an account
- POST /api/v1/accounts/:id/recalculate-balance recalculates from transactions
</verification>

<success_criteria>
- Account balances track correctly as Decimal values
- Total balance sums across all active accounts
- Archived accounts excluded from default listing
- Balance recalculation works from transaction history
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-module/03-03-SUMMARY.md`
</output>
