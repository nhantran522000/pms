---
phase: 07-hobbies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [libs/data-access/prisma/schema.prisma, libs/shared-types/src/hobbies/hobbies.schema.ts, libs/shared-types/src/hobbies/index.ts, libs/shared-types/src/index.ts]
autonomous: true
requirements: [HOBB-01, HOBB-05]

must_haves:
  truths:
    - "User can create a hobby with name and tracking type (COUNTER, PERCENTAGE, LIST)"
    - "User can set a goal target on a hobby (optional, for COUNTER and PERCENTAGE types)"
    - "User can set a goal deadline on a hobby (optional)"
    - "Prisma schema includes Hobby and HobbyLog models with tenantId for RLS"
  artifacts:
    - path: "libs/data-access/prisma/schema.prisma"
      provides: "Hobby and HobbyLog Prisma models with HobbyTrackingType enum"
      contains: "model Hobby"
      min_lines: 20
    - path: "libs/shared-types/src/hobbies/hobbies.schema.ts"
      provides: "Zod validation schemas for Hobby DTOs"
      exports: ["HobbyTrackingTypeSchema", "CreateHobbySchema", "CreateHobbyLogSchema"]
      min_lines: 50
    - path: "libs/shared-types/src/hobbies/index.ts"
      provides: "Barrel export for hobbies schemas"
  key_links:
    - from: "libs/data-access/prisma/schema.prisma"
      to: "Tenant"
      via: "tenantId foreign key"
      pattern: "tenantId.*String.*@map"
    - from: "libs/shared-types/src/hobbies/hobbies.schema.ts"
      to: "Prisma HobbyTrackingType enum"
      via: "Zod enum matching Prisma enum values"
      pattern: "z.enum\\(['\"]COUNTER['\"], ['\"]PERCENTAGE['\"], ['\"]LIST['\"]\\)"
---

<objective>
Create Prisma schema models and Zod validation DTOs for the Hobbies module.

Purpose: Establish the database foundation and type-safe API contracts for hobby tracking with flexible tracking types (COUNTER, PERCENTAGE, LIST) and goal support.
Output: Hobby/HobbyLog Prisma models, HobbyTrackingType enum, Zod schemas for all Hobby DTOs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hobbies/07-CONTEXT.md
@.planning/phases/07-hobbies/07-RESEARCH.md

<interfaces>
<!-- Reference patterns from existing codebase -->

From libs/shared-types/src/health/health.schema.ts:
```typescript
export const HealthLogTypeSchema = z.enum(['WEIGHT', 'BLOOD_PRESSURE', ...]);
export type HealthLogType = z.infer<typeof HealthLogTypeSchema>;

export const TrendQuerySchema = z.object({
  type: HealthLogTypeSchema,
  startDate: z.string().date().optional(),
  endDate: z.string().date().optional(),
  range: z.enum(['30', '90', '365']).optional(),
});

export const TrendDataPointSchema = z.object({
  x: z.number(), // timestamp
  y: z.number(), // value
  date: z.date(),
  notes: z.string().nullable(),
});
```

From libs/data-access/prisma/schema.prisma (HealthLog pattern):
```prisma
model HealthLog {
  id        String        @id @default(cuid())
  tenantId  String        @map("tenantId")
  type      HealthLogType
  loggedAt  DateTime      @map("loggedAt") @db.Timestamp
  data      Json
  notes     String?       @db.VarChar(1000)
  source    String        @default("manual") @db.VarChar(50)
  deletedAt DateTime?
  createdAt DateTime      @default(now())
  updatedAt DateTime      @updatedAt
  tenant    Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, type, loggedAt])
  @@map("health_logs")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Hobby models and HobbyTrackingType enum to Prisma schema</name>
  <files>libs/data-access/prisma/schema.prisma</files>
  <read_first>
    - libs/data-access/prisma/schema.prisma (current state, especially HealthLog model pattern at line 375-392)
    - libs/shared-types/src/health/health.schema.ts (Zod enum pattern for reference)
  </read_first>
  <action>
    Add the following to libs/data-access/prisma/schema.prisma:

    1. Add HobbyTrackingType enum BEFORE the Tenant model (following HealthLogType pattern):
    ```prisma
    /// HobbyTrackingType - Types of hobby progress tracking
    enum HobbyTrackingType {
      COUNTER
      PERCENTAGE
      LIST
    }
    ```

    2. Add Hobby model AFTER HealthLog model:
    ```prisma
    /// Hobby - User hobbies for flexible tracking
    model Hobby {
      id            String            @id @default(cuid())
      tenantId      String            @map("tenantId")
      name          String            @db.VarChar(100)
      description   String?           @db.VarChar(500)
      trackingType  HobbyTrackingType @map("trackingType")
      goalTarget    Decimal?          @map("goalTarget") @db.Decimal(10, 2) // For COUNTER: total count, for PERCENTAGE: 100
      goalDeadline  DateTime?         @map("goalDeadline") // Optional deadline for goal
      isActive      Boolean           @default(true) @map("isActive")
      createdAt     DateTime          @default(now())
      updatedAt     DateTime          @updatedAt
      tenant        Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
      logs          HobbyLog[]

      @@index([tenantId])
      @@index([isActive])
      @@map("hobbies")
    }
    ```

    3. Add HobbyLog model AFTER Hobby model:
    ```prisma
    /// HobbyLog - Progress entries for hobbies with flexible JSONB data
    model HobbyLog {
      id           String            @id @default(cuid())
      tenantId     String            @map("tenantId")
      hobbyId      String            @map("hobbyId")
      trackingType HobbyTrackingType // Copied from hobby for query efficiency
      logValue     Json              @map("logValue") // { increment: number } | { percentage: number } | { label: string }
      loggedAt     DateTime          @map("loggedAt") @db.Timestamp
      notes        String?           @db.VarChar(500)
      createdAt    DateTime          @default(now())
      updatedAt    DateTime          @updatedAt
      hobby        Hobby             @relation(fields: [hobbyId], references: [id], onDelete: Cascade)
      tenant       Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)

      @@index([tenantId, hobbyId, loggedAt])
      @@index([trackingType])
      @@map("hobby_logs")
    }
    ```

    4. Add relations to Tenant model (add after journalEntries line):
    ```prisma
    hobbies       Hobby[]
    hobbyLogs     HobbyLog[]
    ```

    IMPORTANT: Per CONTEXT.md decisions:
    - goalTarget uses Decimal for precision (supports both counter totals and percentages)
    - goalDeadline is optional (users can set "by [date]" or leave open-ended)
    - trackingType copied to HobbyLog for efficient querying without joining
    - Multiple log entries per day allowed (no unique constraint on date)
  </action>
  <acceptance_criteria>
    - `grep -c "^enum HobbyTrackingType" libs/data-access/prisma/schema.prisma` returns 1
    - `grep -c "^model Hobby" libs/data-access/prisma/schema.prisma` returns 1
    - `grep -c "^model HobbyLog" libs/data-access/prisma/schema.prisma` returns 1
    - `grep "hobbies.*Hobby\[\]" libs/data-access/prisma/schema.prisma` exits 0
    - `grep "hobbyLogs.*HobbyLog\[\]" libs/data-access/prisma/schema.prisma` exits 0
    - Schema contains COUNTER, PERCENTAGE, LIST enum values
    - Schema contains goalTarget Decimal field with @map("goalTarget")
    - Schema contains goalDeadline DateTime? field with @map("goalDeadline")
  </acceptance_criteria>
  <verify>
    <automated>pnpm prisma validate --schema=libs/data-access/prisma/schema.prisma</automated>
  </verify>
  <done>Prisma schema contains Hobby, HobbyLog models and HobbyTrackingType enum with proper tenant relations and indexes</done>
</task>

<task type="auto">
  <name>Task 2: Create Zod schemas for Hobbies DTOs</name>
  <files>libs/shared-types/src/hobbies/hobbies.schema.ts, libs/shared-types/src/hobbies/index.ts, libs/shared-types/src/index.ts</files>
  <read_first>
    - libs/shared-types/src/health/health.schema.ts (reference pattern for type-specific data schemas)
    - libs/shared-types/src/index.ts (barrel export pattern)
  </read_first>
  <action>
    1. Create libs/shared-types/src/hobbies/hobbies.schema.ts with:

    ```typescript
    import { z } from 'zod';

    // Hobby tracking types
    export const HobbyTrackingTypeSchema = z.enum(['COUNTER', 'PERCENTAGE', 'LIST']);
    export type HobbyTrackingType = z.infer<typeof HobbyTrackingTypeSchema>;

    // Type-specific log value schemas
    export const CounterLogValueSchema = z.object({
      increment: z.number().positive().default(1), // Default +1 per CONTEXT.md
    });
    export type CounterLogValue = z.infer<typeof CounterLogValueSchema>;

    export const PercentageLogValueSchema = z.object({
      percentage: z.number().min(0).max(100),
    });
    export type PercentageLogValue = z.infer<typeof PercentageLogValueSchema>;

    export const ListLogValueSchema = z.object({
      label: z.string().max(200), // String label per CONTEXT.md
    });
    export type ListLogValue = z.infer<typeof ListLogValueSchema>;

    // Create hobby schema
    export const CreateHobbySchema = z.object({
      name: z.string().min(1).max(100),
      description: z.string().max(500).optional(),
      trackingType: HobbyTrackingTypeSchema,
      goalTarget: z.number().positive().optional(), // Optional goal target
      goalDeadline: z.string().datetime().or(z.date()).optional(), // Optional deadline
    });
    export type CreateHobbyDto = z.infer<typeof CreateHobbySchema>;

    // Update hobby schema
    export const UpdateHobbySchema = z.object({
      name: z.string().min(1).max(100).optional(),
      description: z.string().max(500).optional(),
      goalTarget: z.number().positive().nullable().optional(), // Allow clearing goal
      goalDeadline: z.string().datetime().or(z.date()).nullable().optional(),
      isActive: z.boolean().optional(),
    });
    export type UpdateHobbyDto = z.infer<typeof UpdateHobbySchema>;

    // Hobby response schema
    export const HobbyResponseSchema = z.object({
      id: z.string(),
      tenantId: z.string(),
      name: z.string(),
      description: z.string().nullable(),
      trackingType: HobbyTrackingTypeSchema,
      goalTarget: z.number().nullable(),
      goalDeadline: z.date().nullable(),
      isActive: z.boolean(),
      createdAt: z.date(),
      updatedAt: z.date(),
    });
    export type HobbyResponse = z.infer<typeof HobbyResponseSchema>;

    // Create hobby log schema
    export const CreateHobbyLogSchema = z.object({
      hobbyId: z.string(),
      logValue: z.record(z.unknown()), // Validated in service based on trackingType
      loggedAt: z.string().datetime().or(z.date()).optional(),
      notes: z.string().max(500).optional(),
    });
    export type CreateHobbyLogDto = z.infer<typeof CreateHobbyLogSchema>;

    // Hobby log response schema
    export const HobbyLogResponseSchema = z.object({
      id: z.string(),
      tenantId: z.string(),
      hobbyId: z.string(),
      trackingType: HobbyTrackingTypeSchema,
      logValue: z.record(z.unknown()),
      loggedAt: z.date(),
      notes: z.string().nullable(),
      createdAt: z.date(),
      updatedAt: z.date(),
    });
    export type HobbyLogResponse = z.infer<typeof HobbyLogResponseSchema>;

    // Hobby with completion percentage (for dashboard)
    export const HobbyWithCompletionSchema = HobbyResponseSchema.extend({
      currentTotal: z.number(), // Sum of all increments (COUNTER) or last percentage (PERCENTAGE)
      completionPercentage: z.number().min(0).max(100), // Capped at 100% per CONTEXT.md
    });
    export type HobbyWithCompletion = z.infer<typeof HobbyWithCompletionSchema>;

    // Hobby trend query schema (7/30/90/365 day views per CONTEXT.md)
    export const HobbyTrendQuerySchema = z.object({
      hobbyId: z.string(),
      startDate: z.string().date().optional(),
      endDate: z.string().date().optional(),
      range: z.enum(['7', '30', '90', '365']).optional(),
    });
    export type HobbyTrendQuery = z.infer<typeof HobbyTrendQuerySchema>;

    // Trend data point schema (consistent with health module)
    export const HobbyTrendDataPointSchema = z.object({
      x: z.number(), // timestamp
      y: z.number(), // value
      date: z.date(),
      notes: z.string().nullable(),
    });
    export type HobbyTrendDataPoint = z.infer<typeof HobbyTrendDataPointSchema>;

    // Counter chart data (bars + running total line per CONTEXT.md)
    export const CounterChartDataSchema = z.object({
      bars: z.array(HobbyTrendDataPointSchema),
      line: z.array(HobbyTrendDataPointSchema), // Running total overlay
    });
    export type CounterChartData = z.infer<typeof CounterChartDataSchema>;

    // Hobby trend data response
    export const HobbyTrendDataSchema = z.object({
      hobbyId: z.string(),
      trackingType: HobbyTrackingTypeSchema,
      data: z.union([CounterChartDataSchema, z.array(HobbyTrendDataPointSchema)]),
      startDate: z.date(),
      endDate: z.date(),
    });
    export type HobbyTrendData = z.infer<typeof HobbyTrendDataSchema>;

    // Hobby insights schema (AI-generated per CONTEXT.md)
    export const HobbyInsightsSchema = z.object({
      hobbyId: z.string(),
      hobbyName: z.string(),
      summary: z.string(),
      trends: z.array(z.string()),
      consistencyStreaks: z.array(z.string()),
      goalTrajectory: z.array(z.string()),
      milestones: z.array(z.string()),
      generatedAt: z.date(),
      isDataOnly: z.boolean(), // True when AI fails, falls back to data-only
    });
    export type HobbyInsights = z.infer<typeof HobbyInsightsSchema>;
    ```

    2. Create libs/shared-types/src/hobbies/index.ts:
    ```typescript
    export * from './hobbies.schema';
    ```

    3. Update libs/shared-types/src/index.ts - add after the notes export:
    ```typescript
    export * from './hobbies';
    ```

    Per CONTEXT.md decisions:
    - Counter increment defaults to 1, allows any positive integer
    - Percentage range 0-100
    - List uses string label only
    - Trend ranges: 7/30/90/365 days
    - Counter charts include both bars and running total line
    - Completion percentage capped at 100%
  </action>
  <acceptance_criteria>
    - `test -f libs/shared-types/src/hobbies/hobbies.schema.ts` exits 0
    - `test -f libs/shared-types/src/hobbies/index.ts` exits 0
    - `grep "export \* from './hobbies'" libs/shared-types/src/index.ts` exits 0
    - File contains HobbyTrackingTypeSchema with COUNTER, PERCENTAGE, LIST
    - File contains CreateHobbySchema with goalTarget and goalDeadline optional fields
    - File contains CounterLogValueSchema, PercentageLogValueSchema, ListLogValueSchema
    - File contains HobbyTrendQuerySchema with range enum ['7', '30', '90', '365']
    - File contains CounterChartDataSchema with bars and line arrays
    - File contains HobbyInsightsSchema with isDataOnly field
  </acceptance_criteria>
  <verify>
    <automated>pnpm tsc --noEmit --project libs/shared-types/tsconfig.lib.json 2>&1 | head -20</automated>
  </verify>
  <done>Zod schemas exist for all Hobby DTOs with proper validation and type exports</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate Prisma client</name>
  <files>libs/data-access/src/generated/</files>
  <read_first>
    - libs/data-access/prisma/schema.prisma (verify models added correctly)
  </read_first>
  <action>
    Regenerate Prisma client to include Hobby and HobbyLog models:

    ```bash
    pnpm prisma generate --schema=libs/data-access/prisma/schema.prisma
    ```

    This generates TypeScript types for Hobby, HobbyLog, and HobbyTrackingType enum in libs/data-access/src/generated/.
  </action>
  <acceptance_criteria>
    - `grep -c "export type Hobby " libs/data-access/src/generated/index.d.ts` returns 1 or `grep -c "model Hobby" libs/data-access/src/generated/schema.prisma` returns 1
    - `grep -c "export type HobbyLog " libs/data-access/src/generated/index.d.ts` returns 1 or `grep -c "model HobbyLog" libs/data-access/src/generated/schema.prisma` returns 1
    - `grep "HobbyTrackingType" libs/data-access/src/generated/index.d.ts` exits 0 or `grep "enum HobbyTrackingType" libs/data-access/src/generated/schema.prisma` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm prisma generate --schema=libs/data-access/prisma/schema.prisma 2>&1 | grep -E "(Generated|error)"</automated>
  </verify>
  <done>Prisma client regenerated with Hobby, HobbyLog models and HobbyTrackingType enum</done>
</task>

</tasks>

<verification>
- Prisma schema validates without errors
- Zod schemas compile without TypeScript errors
- Prisma client includes Hobby and HobbyLog types
- All tenant relations properly defined for RLS
</verification>

<success_criteria>
- User can define Hobby model with tracking type and optional goal
- HobbyLog model supports polymorphic JSONB log values
- All DTOs have Zod validation schemas
- Types available for use in feature module
</success_criteria>

<output>
After completion, create `.planning/phases/07-hobbies/07-01-SUMMARY.md`
</output>
