---
phase: 10-mobile-client
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/mobile/package.json
  - apps/mobile/tailwind.config.js
  - apps/mobile/nativewind-env.d.ts
  - apps/mobile/babel.config.js
  - apps/mobile/global.css
  - apps/mobile/app/_layout.tsx
  - apps/mobile/src/components/ui/Button.tsx
  - apps/mobile/src/components/ui/Text.tsx
  - apps/mobile/src/components/ui/Card.tsx
  - apps/mobile/src/components/ui/index.ts
autonomous: true
requirements:
  - MOB-02
user_setup: []
must_haves:
  truths:
    - "NativeWind v4 is configured and working"
    - "Tailwind classes apply correctly to React Native components"
    - "className prop works on all styled components"
    - "Theme colors match web client (indigo-500 primary)"
  artifacts:
    - path: "apps/mobile/tailwind.config.js"
      provides: "Tailwind/NativeWind configuration"
      contains: "presets: [require('nativewind/preset')]"
    - path: "apps/mobile/nativewind-env.d.ts"
      provides: "TypeScript types for className"
      contains: "nativewind/types"
    - path: "apps/mobile/src/components/ui/Button.tsx"
      provides: "Styled button component"
      contains: "className="
    - path: "apps/mobile/babel.config.js"
      provides: "Babel with NativeWind"
      contains: "nativewind/babel"
  key_links:
    - from: "apps/mobile/src/components/ui/Button.tsx"
      to: "tailwind.config.js"
      via: "className prop"
      pattern: "className.*bg-indigo-500"
---

<objective>
Configure NativeWind v4 for Tailwind CSS styling in the Expo mobile app. NativeWind provides build-time compilation of Tailwind classes to React Native styles, maintaining consistency with the web client's Tailwind approach.

Purpose: Enable Tailwind CSS styling in mobile app for consistent design language with web client.
Output: NativeWind configured with reusable UI components (Button, Text, Card).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mobile-client/10-CONTEXT.md
@.planning/phases/10-mobile-client/10-RESEARCH.md
</context>

<interfaces>
<!-- Web client Tailwind config pattern -->
From apps/web/tailwind.config.ts (if exists) or standard Tailwind:
- Primary color: indigo-500 (#6366f1)
- Dark mode support: class-based

From apps/mobile/app/_layout.tsx (created in 10-01):
- Root layout with Stack navigator
- StatusBar component
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install NativeWind and Tailwind dependencies</name>
  <files>
    apps/mobile/package.json
  </files>
  <read_first>
    - apps/mobile/package.json (created in 10-01)
    - apps/web/package.json (for Tailwind version reference)
  </read_first>
  <action>
    Add NativeWind v4 and Tailwind dependencies to mobile app.

    Update apps/mobile/package.json to add:
    ```json
    {
      "dependencies": {
        "nativewind": "^4.2.3",
        "tailwindcss": "^3.4.3"
      },
      "devDependencies": {
        "@types/react-native": "^0.73.0"
      }
    }
    ```

    Run pnpm install after updating:
    ```bash
    pnpm install
    ```

    EXACT versions to use:
    - nativewind: ^4.2.3
    - tailwindcss: ^3.4.3
  </action>
  <verify>
    <automated>cat apps/mobile/package.json | grep '"nativewind": "^4.2.3"'</automated>
  </verify>
  <done>
    nativewind@4.2.3 installed
    tailwindcss@3.4.3 installed
    pnpm install succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure NativeWind with Tailwind</name>
  <files>
    apps/mobile/tailwind.config.js
    apps/mobile/nativewind-env.d.ts
    apps/mobile/global.css
    apps/mobile/babel.config.js
  </files>
  <read_first>
    - apps/mobile/babel.config.js (created in 10-01)
    - .planning/phases/10-mobile-client/10-RESEARCH.md (NativeWind patterns section)
  </read_first>
  <action>
    Create NativeWind configuration files.

    1. Create apps/mobile/tailwind.config.js:
       ```javascript
       /** @type {import('tailwindcss').Config} */
       module.exports = {
         content: [
           './app/**/*.{js,jsx,ts,tsx}',
           './src/**/*.{js,jsx,ts,tsx}',
         ],
         presets: [require('nativewind/preset')],
         theme: {
           extend: {
             colors: {
               // Match web client colors
               primary: {
                 50: '#eef2ff',
                 100: '#e0e7ff',
                 200: '#c7d2fe',
                 300: '#a5b4fc',
                 400: '#818cf8',
                 500: '#6366f1', // indigo-500 - primary brand color
                 600: '#4f46e5',
                 700: '#4338ca',
                 800: '#3730a3',
                 900: '#312e81',
               },
             },
           },
         },
         plugins: [],
       };
       ```

    2. Update apps/mobile/nativewind-env.d.ts:
       ```typescript
       /// <reference types="nativewind/types" />
       ```

    3. Create apps/mobile/global.css (required by NativeWind):
       ```css
       @tailwind base;
       @tailwind components;
       @tailwind utilities;
       ```

    4. Update apps/mobile/babel.config.js to include NativeWind:
       ```javascript
       module.exports = function (api) {
         api.cache(true);
         return {
           presets: [
             ['babel-preset-expo', { jsxImportSource: 'nativewind' }],
             'nativewind/babel',
           ],
         };
       };
       ```
  </action>
  <verify>
    <automated>cat apps/mobile/tailwind.config.js | grep "nativewind/preset"</automated>
  </verify>
  <done>
    tailwind.config.js exists with nativewind preset
    nativewind-env.d.ts exists with type reference
    global.css exists with tailwind directives
    babel.config.js updated with nativewind/babel preset
  </done>
</task>

<task type="auto">
  <name>Task 3: Update root layout to import global CSS</name>
  <files>
    apps/mobile/app/_layout.tsx
  </files>
  <read_first>
    - apps/mobile/app/_layout.tsx (created in 10-01)
    - apps/mobile/global.css (created in Task 2)
  </read_first>
  <action>
    Update root layout to import global CSS and set up NativeWind context.

    Update apps/mobile/app/_layout.tsx:
    ```typescript
    import '../global.css';
    import { Stack } from 'expo-router';
    import { StatusBar } from 'expo-status-bar';
    import { View } from 'react-native';

    export default function RootLayout() {
      return (
        <View className="flex-1 bg-white dark:bg-gray-900">
          <StatusBar style="auto" />
          <Stack
            screenOptions={{
              headerStyle: {
                backgroundColor: '#6366f1',
              },
              headerTintColor: '#fff',
              headerTitleStyle: {
                fontWeight: 'bold',
              },
            }}
          >
            <Stack.Screen name="index" options={{ title: 'PMS Mobile' }} />
          </Stack>
        </View>
      );
    }
    ```

    Note: The className prop on View demonstrates NativeWind is working.
  </action>
  <verify>
    <automated>cat apps/mobile/app/_layout.tsx | grep "import '../global.css'"</automated>
  </verify>
  <done>
    Root layout imports global.css
    className prop used on View component
    Stack navigator styled with primary color
  </done>
</task>

<task type="auto">
  <name>Task 4: Create reusable UI components</name>
  <files>
    apps/mobile/src/components/ui/Button.tsx
    apps/mobile/src/components/ui/Text.tsx
    apps/mobile/src/components/ui/Card.tsx
    apps/mobile/src/components/ui/Input.tsx
    apps/mobile/src/components/ui/index.ts
  </files>
  <read_first>
    - apps/mobile/tailwind.config.js (for color values)
    - .planning/phases/10-mobile-client/10-RESEARCH.md (NativeWind patterns)
  </read_first>
  <action>
    Create reusable UI components with NativeWind styling.

    1. Create apps/mobile/src/components/ui/Button.tsx:
       ```typescript
       import { TouchableOpacity, Text, ActivityIndicator } from 'react-native';
       import type { ReactNode } from 'react';

       interface ButtonProps {
         onPress: () => void;
         variant?: 'primary' | 'secondary' | 'outline' | 'danger';
         size?: 'sm' | 'md' | 'lg';
         disabled?: boolean;
         loading?: boolean;
         children: ReactNode;
       }

       export function Button({
         onPress,
         variant = 'primary',
         size = 'md',
         disabled = false,
         loading = false,
         children,
       }: ButtonProps) {
         const baseClasses = 'rounded-lg items-center justify-center flex-row';

         const variantClasses = {
           primary: 'bg-primary-500 active:bg-primary-600',
           secondary: 'bg-gray-200 active:bg-gray-300',
           outline: 'bg-transparent border-2 border-primary-500',
           danger: 'bg-red-500 active:bg-red-600',
         };

         const sizeClasses = {
           sm: 'px-3 py-2',
           md: 'px-4 py-3',
           lg: 'px-6 py-4',
         };

         const textVariantClasses = {
           primary: 'text-white',
           secondary: 'text-gray-900',
           outline: 'text-primary-500',
           danger: 'text-white',
         };

         const textSizeClasses = {
           sm: 'text-sm',
           md: 'text-base',
           lg: 'text-lg',
         };

         return (
           <TouchableOpacity
             onPress={onPress}
             disabled={disabled || loading}
             className={`${baseClasses} ${variantClasses[variant]} ${sizeClasses[size]} ${disabled ? 'opacity-50' : ''}`}
           >
             {loading ? (
               <ActivityIndicator color={variant === 'outline' ? '#6366f1' : '#fff'} />
             ) : (
               <Text className={`font-semibold ${textVariantClasses[variant]} ${textSizeClasses[size]}`}>
                 {children}
               </Text>
             )}
           </TouchableOpacity>
         );
       }
       ```

    2. Create apps/mobile/src/components/ui/Text.tsx:
       ```typescript
       import { Text as RNText } from 'react-native';
       import type { ReactNode } from 'react';

       interface TextProps {
         variant?: 'h1' | 'h2' | 'h3' | 'body' | 'caption' | 'label';
         color?: 'default' | 'muted' | 'primary' | 'danger';
         children: ReactNode;
       }

       export function Text({ variant = 'body', color = 'default', children }: TextProps) {
         const variantClasses = {
           h1: 'text-3xl font-bold',
           h2: 'text-2xl font-semibold',
           h3: 'text-xl font-medium',
           body: 'text-base',
           caption: 'text-sm text-gray-500',
           label: 'text-sm font-medium',
         };

         const colorClasses = {
           default: 'text-gray-900 dark:text-white',
           muted: 'text-gray-500 dark:text-gray-400',
           primary: 'text-primary-500',
           danger: 'text-red-500',
         };

         return (
           <RNText className={`${variantClasses[variant]} ${colorClasses[color]}`}>
             {children}
           </RNText>
         );
       }
       ```

    3. Create apps/mobile/src/components/ui/Card.tsx:
       ```typescript
       import { View } from 'react-native';
       import type { ReactNode } from 'react';

       interface CardProps {
         children: ReactNode;
         padding?: 'none' | 'sm' | 'md' | 'lg';
         className?: string;
       }

       export function Card({ children, padding = 'md', className = '' }: CardProps) {
         const paddingClasses = {
           none: '',
           sm: 'p-2',
           md: 'p-4',
           lg: 'p-6',
         };

         return (
           <View className={`bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 ${paddingClasses[padding]} ${className}`}>
             {children}
           </View>
         );
       }
       ```

    4. Create apps/mobile/src/components/ui/Input.tsx:
       ```typescript
       import { TextInput, View, Text } from 'react-native';

       interface InputProps {
         label?: string;
         placeholder?: string;
         value: string;
         onChangeText: (text: string) => void;
         secureTextEntry?: boolean;
         keyboardType?: 'default' | 'email-address' | 'numeric' | 'phone-pad';
         error?: string;
       }

       export function Input({
         label,
         placeholder,
         value,
         onChangeText,
         secureTextEntry = false,
         keyboardType = 'default',
         error,
       }: InputProps) {
         return (
           <View className="mb-4">
             {label && (
               <Text className="text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                 {label}
               </Text>
             )}
             <TextInput
               value={value}
               onChangeText={onChangeText}
               placeholder={placeholder}
               secureTextEntry={secureTextEntry}
               keyboardType={keyboardType}
               className={`bg-gray-50 dark:bg-gray-700 border ${error ? 'border-red-500' : 'border-gray-300 dark:border-gray-600'} rounded-lg px-4 py-3 text-base text-gray-900 dark:text-white`}
               placeholderTextColor="#9ca3af"
             />
             {error && (
               <Text className="text-red-500 text-sm mt-1">{error}</Text>
             )}
           </View>
         );
       }
       ```

    5. Create apps/mobile/src/components/ui/index.ts:
       ```typescript
       export { Button } from './Button';
       export { Text } from './Text';
       export { Card } from './Card';
       export { Input } from './Input';
       ```
  </action>
  <verify>
    <automated>cat apps/mobile/src/components/ui/Button.tsx | grep "className="</automated>
  </verify>
  <done>
    Button component with variants (primary/secondary/outline/danger)
    Text component with variants (h1/h2/h3/body/caption/label)
    Card component with padding options
    Input component with error state
    All components exported from index.ts
  </done>
</task>

<task type="auto">
  <name>Task 5: Update index screen to demonstrate NativeWind</name>
  <files>
    apps/mobile/app/index.tsx
  </files>
  <read_first>
    - apps/mobile/app/index.tsx (created in 10-01)
    - apps/mobile/src/components/ui/Button.tsx
    - apps/mobile/src/components/ui/Text.tsx
    - apps/mobile/src/components/ui/Card.tsx
  </read_first>
  <action>
    Update the home screen to demonstrate NativeWind components.

    Update apps/mobile/app/index.tsx:
    ```typescript
    import { View, ScrollView } from 'react-native';
    import { Button, Text, Card } from '../src/components/ui';

    export default function HomeScreen() {
      return (
        <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900">
          <View className="p-4">
            <Card className="mb-4">
              <Text variant="h1" color="primary">PMS Mobile</Text>
              <Text variant="body" color="muted" className="mt-2">
                Expo SDK 55 + NativeWind
              </Text>
            </Card>

            <Card className="mb-4">
              <Text variant="h3" className="mb-3">Button Variants</Text>
              <View className="gap-3">
                <Button onPress={() => console.log('Primary')} variant="primary">
                  Primary Button
                </Button>
                <Button onPress={() => console.log('Secondary')} variant="secondary">
                  Secondary Button
                </Button>
                <Button onPress={() => console.log('Outline')} variant="outline">
                  Outline Button
                </Button>
              </View>
            </Card>

            <Card>
              <Text variant="h3" className="mb-3">Text Variants</Text>
              <Text variant="h1">Heading 1</Text>
              <Text variant="h2">Heading 2</Text>
              <Text variant="h3">Heading 3</Text>
              <Text variant="body">Body text example</Text>
              <Text variant="caption">Caption text</Text>
            </Card>
          </View>
        </ScrollView>
      );
    }
    ```
  </action>
  <verify>
    <automated>cat apps/mobile/app/index.tsx | grep "from '../src/components/ui'"</automated>
  </verify>
  <done>
    Index screen uses UI components
    NativeWind classes applied (className props)
    Multiple component variants demonstrated
  </done>
</task>

</tasks>

<verification>
1. Run `cd apps/mobile && npx tsc --noEmit` to verify TypeScript compiles
2. Check babel.config.js contains 'nativewind/babel'
3. Check tailwind.config.js contains 'nativewind/preset'
4. Check at least 3 UI components exist with className props
</verification>

<success_criteria>
- NativeWind v4.2.3 configured and working
- Tailwind config matches web client colors (primary: indigo-500)
- Reusable UI components created (Button, Text, Card, Input)
- className prop works on all styled components
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-client/10-03-SUMMARY.md`
</output>
