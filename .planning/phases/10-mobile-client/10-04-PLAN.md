---
phase: 10-mobile-client
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/dashboard.tsx
  - apps/mobile/app/(tabs)/financial.tsx
  - apps/mobile/app/(tabs)/habits.tsx
  - apps/mobile/app/(tabs)/health.tsx
  - apps/mobile/app/(tabs)/profile.tsx
  - apps/mobile/app/(tabs)/tasks.tsx
  - apps/mobile/app/financial/transactions/index.tsx
  - apps/mobile/app/financial/transactions/[id].tsx
  - apps/mobile/app/financial/accounts/index.tsx
  - apps/mobile/app/financial/budgets/index.tsx
  - apps/mobile/app/habits/[id].tsx
  - apps/mobile/app/health/logs/index.tsx
  - apps/mobile/app/tasks/[id].tsx
  - apps/mobile/app/profile/notes/index.tsx
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/index.tsx
  - apps/mobile/src/components/navigation/TabBarIcon.tsx
autonomous: true
requirements:
  - MOB-03
user_setup: []
must_haves:
  truths:
    - "User can navigate between 5 bottom tabs (Dashboard, Financial, Habits, Health, Profile)"
    - "Each tab has stack navigation for drill-down screens"
    - "Tasks accessible from bottom tab navigation"
    - "Notes accessible from Profile tab menu"
    - "Navigation follows file-based Expo Router convention"
  artifacts:
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "Tab navigator configuration"
      contains: "Tabs.Screen"
    - path: "apps/mobile/app/(tabs)/dashboard.tsx"
      provides: "Dashboard tab screen"
      min_lines: 20
    - path: "apps/mobile/app/financial/transactions/index.tsx"
      provides: "Financial drill-down stack"
      min_lines: 20
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "6 tab screens configured"
      contains: "Tabs.Screen name=\"tasks\""
  key_links:
    - from: "apps/mobile/app/(tabs)/_layout.tsx"
      to: "expo-router"
      via: "import { Tabs } from 'expo-router'"
      pattern: "from 'expo-router'"
---

<objective>
Implement Expo Router file-based navigation with 6 bottom tabs (Dashboard, Financial, Habits, Health, Tasks, Profile) and stack navigation per module for drill-down screens. This implements the locked navigation decision from CONTEXT.md.

Purpose: Establish the complete navigation structure for the mobile app following the user's locked decision for 5 tabs (with Tasks as 6th per updated requirements).
Output: Working tab and stack navigation matching the navigation structure decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mobile-client/10-CONTEXT.md
@.planning/phases/10-mobile-client/10-RESEARCH.md
</context>

<interfaces>
<!-- Locked decision from CONTEXT.md -->
Navigation Structure (LOCKED):
- 5 bottom tabs: Dashboard, Financial, Habits, Health, Profile
- Stack navigation per module for drill-down
- Notes lives in Profile tab as a menu item

<!-- Expo Router patterns from RESEARCH.md -->
From 10-RESEARCH.md:
- File-based routing: files in app/ directory become routes
- Layout groups: (tabs) for tab navigation
- Stack screens: folders with _layout.tsx for stack navigators
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tab navigator layout</name>
  <files>
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/src/components/navigation/TabBarIcon.tsx
  </files>
  <read_first>
    - apps/mobile/app/_layout.tsx (root layout from 10-01/10-03)
    - .planning/phases/10-mobile-client/10-CONTEXT.md (locked navigation decision)
    - .planning/phases/10-mobile-client/10-RESEARCH.md (Expo Router patterns)
  </read_first>
  <action>
    Create the tab navigator with 6 tabs per the updated requirements (Dashboard, Financial, Habits, Health, Tasks, Profile).

    1. Create apps/mobile/src/components/navigation/TabBarIcon.tsx:
       ```typescript
       import { Ionicons } from '@expo/vector-icons';
       import type { ComponentProps } from 'react';

       type IconName = ComponentProps<typeof Ionicons>['name'];

       interface TabBarIconProps {
         name: IconName;
         color: string;
       }

       export function TabBarIcon({ name, color }: TabBarIconProps) {
         return <Ionicons name={name} size={24} color={color} />;
       }
       ```

    2. Create apps/mobile/app/(tabs)/_layout.tsx:
       ```typescript
       import { Tabs } from 'expo-router';
       import { TabBarIcon } from '../../src/components/navigation/TabBarIcon';

       export default function TabLayout() {
         return (
           <Tabs
             screenOptions={{
               tabBarActiveTintColor: '#6366f1', // primary-500
               tabBarInactiveTintColor: '#9ca3af', // gray-400
               headerStyle: {
                 backgroundColor: '#6366f1',
               },
               headerTintColor: '#fff',
               headerTitleStyle: {
                 fontWeight: 'bold',
               },
               tabBarStyle: {
                 backgroundColor: '#fff',
                 borderTopWidth: 1,
                 borderTopColor: '#e5e7eb',
               },
             }}
           >
             <Tabs.Screen
               name="dashboard"
               options={{
                 title: 'Dashboard',
                 tabBarIcon: ({ color }) => <TabBarIcon name="grid" color={color} />,
               }}
             />
             <Tabs.Screen
               name="financial"
               options={{
                 title: 'Financial',
                 tabBarIcon: ({ color }) => <TabBarIcon name="wallet" color={color} />,
               }}
             />
             <Tabs.Screen
               name="habits"
               options={{
                 title: 'Habits',
                 tabBarIcon: ({ color }) => <TabBarIcon name="checkmark-circle" color={color} />,
               }}
             />
             <Tabs.Screen
               name="health"
               options={{
                 title: 'Health',
                 tabBarIcon: ({ color }) => <TabBarIcon name="heart" color={color} />,
               }}
             />
             <Tabs.Screen
               name="tasks"
               options={{
                 title: 'Tasks',
                 tabBarIcon: ({ color }) => <TabBarIcon name="checkbox" color={color} />,
               }}
             />
             <Tabs.Screen
               name="profile"
               options={{
                 title: 'Profile',
                 tabBarIcon: ({ color }) => <TabBarIcon name="person" color={color} />,
               }}
             />
           </Tabs>
         );
       }
       ```

    Note: This implements 6 tabs (Dashboard, Financial, Habits, Health, Tasks, Profile). Per CONTEXT.md, Notes is accessed via Profile tab menu, not as a separate tab.
  </action>
  <verify>
    <automated>cat apps/mobile/app/\(tabs\)/_layout.tsx | grep "Tabs.Screen" | wc -l | xargs -I {} test {} -ge 6 && echo "6 tabs configured" || echo "Wrong tab count"</automated>
  </verify>
  <done>
    apps/mobile/app/(tabs)/_layout.tsx exists with 6 Tabs.Screen entries
    TabBarIcon component created
    All tabs configured with icons and titles
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tab screen placeholders</name>
  <files>
    apps/mobile/app/(tabs)/dashboard.tsx
    apps/mobile/app/(tabs)/financial.tsx
    apps/mobile/app/(tabs)/habits.tsx
    apps/mobile/app/(tabs)/health.tsx
    apps/mobile/app/(tabs)/tasks.tsx
    apps/mobile/app/(tabs)/profile.tsx
  </files>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (tab names must match)
    - apps/mobile/src/components/ui/Card.tsx
    - apps/mobile/src/components/ui/Text.tsx
  </read_first>
  <action>
    Create placeholder screens for each tab with navigation links to drill-down screens.

    1. Create apps/mobile/app/(tabs)/dashboard.tsx:
       ```typescript
       import { ScrollView, View, Pressable } from 'react-native';
       import { Link } from 'expo-router';
       import { Text, Card } from '../../src/components/ui';

       export default function DashboardScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h1" className="mb-4">Dashboard</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Quick Actions</Text>
               <View className="flex-row flex-wrap gap-2">
                 <Link href="/financial/transactions" asChild>
                   <Pressable className="bg-primary-500 px-4 py-2 rounded-lg">
                     <Text className="text-white">Add Transaction</Text>
                   </Pressable>
                 </Link>
                 <Link href="/habits" asChild>
                   <Pressable className="bg-primary-500 px-4 py-2 rounded-lg">
                     <Text className="text-white">Check Habits</Text>
                   </Pressable>
                 </Link>
               </View>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Today's Summary</Text>
               <Text variant="body" color="muted">Dashboard metrics will appear here</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    2. Create apps/mobile/app/(tabs)/financial.tsx:
       ```typescript
       import { ScrollView, View, Pressable } from 'react-native';
       import { Link } from 'expo-router';
       import { Text, Card } from '../../src/components/ui';

       export default function FinancialScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h1" className="mb-4">Financial</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">Accounts</Text>
               <Link href="/financial/accounts" asChild>
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>Manage Accounts</Text>
                 </Pressable>
               </Link>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">Transactions</Text>
               <Link href="/financial/transactions" asChild>
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>View Transactions</Text>
                 </Pressable>
               </Link>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">Budgets</Text>
               <Link href="/financial/budgets" asChild>
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>View Budgets</Text>
                 </Pressable>
               </Link>
             </Card>
           </ScrollView>
         );
       }
       ```

    3. Create apps/mobile/app/(tabs)/habits.tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { Text, Card } from '../../src/components/ui';

       export default function HabitsScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h1" className="mb-4">Habits</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Today's Habits</Text>
               <Text variant="body" color="muted">Habits scheduled for today will appear here</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Streaks</Text>
               <Text variant="body" color="muted">Current and longest streaks</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    4. Create apps/mobile/app/(tabs)/health.tsx:
       ```typescript
       import { ScrollView, View, Pressable } from 'react-native';
       import { Link } from 'expo-router';
       import { Text, Card } from '../../src/components/ui';

       export default function HealthScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h1" className="mb-4">Health</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">Log Health Data</Text>
               <Link href="/health/logs" asChild>
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>Add Health Entry</Text>
                 </Pressable>
               </Link>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Weight Trend</Text>
               <Text variant="body" color="muted">Weight chart will appear here</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Sleep Stats</Text>
               <Text variant="body" color="muted">Sleep statistics will appear here</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    5. Create apps/mobile/app/(tabs)/tasks.tsx:
       ```typescript
       import { ScrollView, View, Pressable } from 'react-native';
       import { Link } from 'expo-router';
       import { Text, Card } from '../../src/components/ui';

       export default function TasksScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h1" className="mb-4">Tasks</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Pending Tasks</Text>
               <Text variant="body" color="muted">Pending tasks will appear here</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Overdue</Text>
               <Text variant="body" color="muted">Overdue tasks will be highlighted</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Completed</Text>
               <Text variant="body" color="muted">Completed tasks history</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    6. Create apps/mobile/app/(tabs)/profile.tsx:
       ```typescript
       import { ScrollView, View, Pressable } from 'react-native';
       import { Link } from 'expo-router';
       import { Text, Card } from '../../src/components/ui';

       export default function ProfileScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h1" className="mb-4">Profile</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">Account</Text>
               <View className="gap-2">
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>User Settings</Text>
                 </Pressable>
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>Subscription</Text>
                 </Pressable>
               </View>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">Notes & Journal</Text>
               <Link href="/profile/notes" asChild>
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>View Notes</Text>
                 </Pressable>
               </Link>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">App Settings</Text>
               <View className="gap-2">
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>Notifications</Text>
                 </Pressable>
                 <Pressable className="bg-gray-100 dark:bg-gray-700 p-3 rounded-lg">
                   <Text>Theme</Text>
                 </Pressable>
               </View>
             </Card>
           </ScrollView>
         );
       }
       ```
  </action>
  <verify>
    <automated>ls apps/mobile/app/\(tabs\)/ | grep -c ".tsx" | xargs -I {} test {} -ge 7 && echo "All tab files exist" || echo "Missing tab files"</automated>
  </verify>
  <done>
    All 6 tab screen files exist (dashboard, financial, habits, health, tasks, profile)
    Each screen has placeholder content with navigation links
    Profile screen includes Notes link per CONTEXT.md decision
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Financial stack navigation</name>
  <files>
    apps/mobile/app/financial/_layout.tsx
    apps/mobile/app/financial/transactions/index.tsx
    apps/mobile/app/financial/transactions/[id].tsx
    apps/mobile/app/financial/accounts/index.tsx
    apps/mobile/app/financial/budgets/index.tsx
  </files>
  <read_first>
    - apps/mobile/app/(tabs)/financial.tsx (links to these routes)
    - .planning/phases/10-mobile-client/10-RESEARCH.md (stack navigation pattern)
  </read_first>
  <action>
    Create Financial module stack navigation for drill-down screens.

    1. Create apps/mobile/app/financial/_layout.tsx:
       ```typescript
       import { Stack } from 'expo-router';

       export default function FinancialLayout() {
         return (
           <Stack
             screenOptions={{
               headerStyle: { backgroundColor: '#6366f1' },
               headerTintColor: '#fff',
               headerTitleStyle: { fontWeight: 'bold' },
             }}
           >
             <Stack.Screen name="transactions/index" options={{ title: 'Transactions' }} />
             <Stack.Screen name="transactions/[id]" options={{ title: 'Transaction Details' }} />
             <Stack.Screen name="accounts/index" options={{ title: 'Accounts' }} />
             <Stack.Screen name="budgets/index" options={{ title: 'Budgets' }} />
           </Stack>
         );
       }
       ```

    2. Create apps/mobile/app/financial/transactions/index.tsx:
       ```typescript
       import { ScrollView, FlatList, Pressable } from 'react-native';
       import { Link } from 'expo-router';
       import { Text, Card } from '../../../src/components/ui';

       export default function TransactionsScreen() {
         // Placeholder data - will be replaced with TanStack Query
         const transactions = [
           { id: '1', payee: 'Grocery Store', amount: '-$45.00', date: '2026-03-24' },
           { id: '2', payee: 'Salary', amount: '+$3,500.00', date: '2026-03-23' },
         ];

         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Transactions</Text>

             <FlatList
               data={transactions}
               keyExtractor={(item) => item.id}
               scrollEnabled={false}
               renderItem={({ item }) => (
                 <Link href={`/financial/transactions/${item.id}`} asChild>
                   <Pressable>
                     <Card className="mb-3 flex-row justify-between items-center">
                       <View>
                         <Text variant="label">{item.payee}</Text>
                         <Text variant="caption">{item.date}</Text>
                       </View>
                       <Text variant="label" color={item.amount.startsWith('+') ? 'primary' : 'danger'}>
                         {item.amount}
                       </Text>
                     </Card>
                   </Pressable>
                 </Link>
               )}
             />
           </ScrollView>
         );
       }
       ```

    3. Create apps/mobile/app/financial/transactions/[id].tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { useLocalSearchParams } from 'expo-router';
       import { Text, Card } from '../../../src/components/ui';

       export default function TransactionDetailScreen() {
         const { id } = useLocalSearchParams<{ id: string }>();

         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Transaction Details</Text>

             <Card className="mb-4">
               <Text variant="caption" color="muted">Transaction ID</Text>
               <Text variant="body">{id}</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-3">Details</Text>
               <Text variant="body" color="muted">Transaction details will be loaded from API</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    4. Create apps/mobile/app/financial/accounts/index.tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { Text, Card } from '../../../src/components/ui';

       export default function AccountsScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Accounts</Text>

             <Card className="mb-4">
               <Text variant="h3">Checking</Text>
               <Text variant="body" color="primary">$2,450.00</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3">Savings</Text>
               <Text variant="body" color="primary">$15,200.00</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    5. Create apps/mobile/app/financial/budgets/index.tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { Text, Card } from '../../../src/components/ui';

       export default function BudgetsScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Budgets</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Groceries</Text>
               <Text variant="body" color="muted">$280 / $400</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Entertainment</Text>
               <Text variant="body" color="muted">$50 / $200</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    Note: Import View from react-native where needed.
  </action>
  <verify>
    <automated>cat apps/mobile/app/financial/_layout.tsx | grep "Stack.Screen" | wc -l | xargs -I {} test {} -ge 4 && echo "Financial stack configured" || echo "Missing stack screens"</automated>
  </verify>
  <done>
    Financial stack layout created with 4 screens
    Transactions list and detail screens created
    Accounts and Budgets placeholder screens created
    Dynamic route [id].tsx for transaction detail
  </done>
</task>

<task type="auto">
  <name>Task 4: Create additional stack navigations</name>
  <files>
    apps/mobile/app/habits/_layout.tsx
    apps/mobile/app/habits/[id].tsx
    apps/mobile/app/health/_layout.tsx
    apps/mobile/app/health/logs/index.tsx
    apps/mobile/app/tasks/_layout.tsx
    apps/mobile/app/tasks/[id].tsx
    apps/mobile/app/profile/_layout.tsx
    apps/mobile/app/profile/notes/index.tsx
  </files>
  <read_first>
    - apps/mobile/app/financial/_layout.tsx (pattern to follow)
    - apps/mobile/app/(tabs)/profile.tsx (links to notes)
  </read_first>
  <action>
    Create stack navigations for remaining modules.

    1. Create apps/mobile/app/habits/_layout.tsx:
       ```typescript
       import { Stack } from 'expo-router';

       export default function HabitsLayout() {
         return (
           <Stack
             screenOptions={{
               headerStyle: { backgroundColor: '#6366f1' },
               headerTintColor: '#fff',
               headerTitleStyle: { fontWeight: 'bold' },
             }}
           >
             <Stack.Screen name="[id]" options={{ title: 'Habit Details' }} />
           </Stack>
         );
       }
       ```

    2. Create apps/mobile/app/habits/[id].tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { useLocalSearchParams } from 'expo-router';
       import { Text, Card } from '../../src/components/ui';

       export default function HabitDetailScreen() {
         const { id } = useLocalSearchParams<{ id: string }>();

         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Habit Details</Text>
             <Card>
               <Text variant="body">Habit ID: {id}</Text>
               <Text variant="caption" color="muted">Habit details loaded from API</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    3. Create apps/mobile/app/health/_layout.tsx:
       ```typescript
       import { Stack } from 'expo-router';

       export default function HealthLayout() {
         return (
           <Stack
             screenOptions={{
               headerStyle: { backgroundColor: '#6366f1' },
               headerTintColor: '#fff',
               headerTitleStyle: { fontWeight: 'bold' },
             }}
           >
             <Stack.Screen name="logs/index" options={{ title: 'Health Logs' }} />
           </Stack>
         );
       }
       ```

    4. Create apps/mobile/app/health/logs/index.tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { Text, Card } from '../../../src/components/ui';

       export default function HealthLogsScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Health Logs</Text>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Log Weight</Text>
               <Text variant="body" color="muted">Weight logging form</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Log Sleep</Text>
               <Text variant="body" color="muted">Sleep logging form</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3" className="mb-2">Log Workout</Text>
               <Text variant="body" color="muted">Workout logging form</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    5. Create apps/mobile/app/tasks/_layout.tsx:
       ```typescript
       import { Stack } from 'expo-router';

       export default function TasksLayout() {
         return (
           <Stack
             screenOptions={{
               headerStyle: { backgroundColor: '#6366f1' },
               headerTintColor: '#fff',
               headerTitleStyle: { fontWeight: 'bold' },
             }}
           >
             <Stack.Screen name="[id]" options={{ title: 'Task Details' }} />
           </Stack>
         );
       }
       ```

    6. Create apps/mobile/app/tasks/[id].tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { useLocalSearchParams } from 'expo-router';
       import { Text, Card } from '../../src/components/ui';

       export default function TaskDetailScreen() {
         const { id } = useLocalSearchParams<{ id: string }>();

         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Task Details</Text>
             <Card>
               <Text variant="body">Task ID: {id}</Text>
               <Text variant="caption" color="muted">Task details loaded from API</Text>
             </Card>
           </ScrollView>
         );
       }
       ```

    7. Create apps/mobile/app/profile/_layout.tsx:
       ```typescript
       import { Stack } from 'expo-router';

       export default function ProfileLayout() {
         return (
           <Stack
             screenOptions={{
               headerStyle: { backgroundColor: '#6366f1' },
               headerTintColor: '#fff',
               headerTitleStyle: { fontWeight: 'bold' },
             }}
           >
             <Stack.Screen name="notes/index" options={{ title: 'Notes' }} />
           </Stack>
         );
       }
       ```

    8. Create apps/mobile/app/profile/notes/index.tsx:
       ```typescript
       import { ScrollView } from 'react-native';
       import { Text, Card } from '../../../src/components/ui';

       export default function NotesScreen() {
         return (
           <ScrollView className="flex-1 bg-gray-50 dark:bg-gray-900 p-4">
             <Text variant="h2" className="mb-4">Notes</Text>

             <Card className="mb-4">
               <Text variant="h3">Note 1</Text>
               <Text variant="body" color="muted">Note preview text...</Text>
             </Card>

             <Card className="mb-4">
               <Text variant="h3">Note 2</Text>
               <Text variant="body" color="muted">Another note preview...</Text>
             </Card>
           </ScrollView>
         );
       }
       ```
  </action>
  <verify>
    <automated>ls apps/mobile/app/habits/ apps/mobile/app/health/ apps/mobile/app/tasks/ apps/mobile/app/profile/ 2>/dev/null | grep -c "_layout.tsx"</automated>
  </verify>
  <done>
    Habits stack with detail screen created
    Health stack with logs screen created
    Tasks stack with detail screen created
    Profile stack with notes screen created (per CONTEXT.md decision)
  </done>
</task>

<task type="auto">
  <name>Task 5: Update root layout and index for tab navigation</name>
  <files>
    apps/mobile/app/_layout.tsx
    apps/mobile/app/index.tsx
  </files>
  <read_first>
    - apps/mobile/app/_layout.tsx (current root layout)
    - apps/mobile/app/(tabs)/_layout.tsx (tab navigator)
  </read_first>
  <action>
    Update root layout to redirect to tabs and remove old index screen content.

    1. Update apps/mobile/app/_layout.tsx:
       ```typescript
       import '../global.css';
       import { Stack } from 'expo-router';
       import { StatusBar } from 'expo-status-bar';
       import { View } from 'react-native';

       export default function RootLayout() {
         return (
           <View className="flex-1">
             <StatusBar style="auto" />
             <Stack
               screenOptions={{
                 headerShown: false,
               }}
             >
               <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
               <Stack.Screen name="financial/transactions/[id]" options={{ headerShown: false }} />
               <Stack.Screen name="habits/[id]" options={{ headerShown: false }} />
               <Stack.Screen name="health/logs/index" options={{ headerShown: false }} />
               <Stack.Screen name="tasks/[id]" options={{ headerShown: false }} />
               <Stack.Screen name="profile/notes/index" options={{ headerShown: false }} />
             </Stack>
           </View>
         );
       }
       ```

    2. Update apps/mobile/app/index.tsx to redirect to tabs:
       ```typescript
       import { Redirect } from 'expo-router';

       export default function Index() {
         return <Redirect href="/(tabs)/dashboard" />;
       }
       ```
  </action>
  <verify>
    <automated>cat apps/mobile/app/_layout.tsx | grep 'name="(tabs)"'</automated>
  </verify>
  <done>
    Root layout references (tabs) group
    Index redirects to dashboard tab
    All stack screens registered in root layout
  </done>
</task>

</tasks>

<verification>
1. Run `cd apps/mobile && npx tsc --noEmit` to verify TypeScript compiles
2. Verify (tabs)/_layout.tsx has 6 Tabs.Screen entries
3. Verify at least 4 stack navigations exist (financial, habits, health, tasks, profile)
4. Verify profile/notes/index.tsx exists (Notes in Profile per CONTEXT.md)
</verification>

<success_criteria>
- 6 bottom tabs configured (Dashboard, Financial, Habits, Health, Tasks, Profile)
- Each tab has working placeholder screen
- Financial module has stack navigation with drill-down screens
- Other modules have stack navigation placeholders
- Notes accessible from Profile tab (per locked decision)
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-client/10-04-SUMMARY.md`
</output>
