---
phase: 09-web-client
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/chart-utils.ts
  - apps/web/src/components/charts/FinancialChart.tsx
  - apps/web/src/components/charts/WeightChart.tsx
  - apps/web/src/components/charts/HabitStreakChart.tsx
  - apps/web/src/components/charts/HobbyProgressChart.tsx
autonomous: true
gap_closure: true
requirements:
  - WEB-01
  - WEB-05

must_haves:
  truths:
    - "Web client builds as static export without TypeScript errors"
    - "All chart components compile successfully"
    - "Static files generated in dist/apps/web or out directory"
  artifacts:
    - path: "apps/web/src/lib/chart-utils.ts"
      provides: "Generic BaseChartProps interface"
      contains: "BaseChartProps<T"
    - path: "apps/web/src/components/charts/FinancialChart.tsx"
      provides: "Financial chart with type-safe data"
      exports: ["FinancialChart", "FinancialChartProps"]
    - path: "apps/web/src/components/charts/WeightChart.tsx"
      provides: "Weight chart with type-safe data"
      exports: ["WeightChart", "WeightChartProps"]
    - path: "apps/web/src/components/charts/HabitStreakChart.tsx"
      provides: "Habit streak chart with type-safe data"
      exports: ["HabitStreakChart", "HabitStreakChartProps"]
    - path: "apps/web/src/components/charts/HobbyProgressChart.tsx"
      provides: "Hobby progress chart with type-safe data"
      exports: ["HobbyProgressChart", "HobbyProgressChartProps"]
  key_links:
    - from: "apps/web/src/components/charts/*.tsx"
      to: "apps/web/src/lib/chart-utils.ts"
      via: "import BaseChartProps"
      pattern: "extends BaseChartProps"
---

<objective>
Fix TypeScript build error in chart components by converting BaseChartProps to use a generic type parameter.

Purpose: Enable successful static export build for the web client (WEB-01, WEB-05)
Output: Type-safe chart components that compile without errors
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-client/09-VERIFICATION.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/src/lib/chart-utils.ts (current - NEEDS FIX):
```typescript
// CURRENT (broken):
export interface BaseChartProps {
  data: Array<Record<string, unknown>>;
  days?: number;
  className?: string;
}

// TARGET (fix):
export interface BaseChartProps<T extends Record<string, unknown> = Record<string, unknown>> {
  data: T[];
  days?: number;
  className?: string;
}
```

From apps/web/src/components/charts/FinancialChart.tsx:
```typescript
export interface FinancialDataPoint {
  date: string;
  income?: number;
  expense?: number;
}

// Current extends clause (broken):
export interface FinancialChartProps extends BaseChartProps {
  data: FinancialDataPoint[];
  // ...
}

// Target (after fix - no change needed, generic handles it):
export interface FinancialChartProps extends BaseChartProps<FinancialDataPoint> {
  days?: number;
  currency?: string;
}
```

Similar pattern applies to WeightChart, HabitStreakChart, HobbyProgressChart.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix BaseChartProps to use generic type parameter</name>
  <files>apps/web/src/lib/chart-utils.ts</files>
  <read_first>
    - apps/web/src/lib/chart-utils.ts
  </read_first>
  <behavior>
    - BaseChartProps accepts generic type parameter T extending Record<string, unknown>
    - Default type parameter maintains backward compatibility
    - data property uses T[] instead of Record<string, unknown>[]
  </behavior>
  <action>
    Update the BaseChartProps interface in apps/web/src/lib/chart-utils.ts (line 83-87).

    Change FROM:
    ```typescript
    export interface BaseChartProps {
      data: Array<Record<string, unknown>>;
      days?: number;
      className?: string;
    }
    ```

    Change TO:
    ```typescript
    export interface BaseChartProps<T extends Record<string, unknown> = Record<string, unknown>> {
      data: T[];
      days?: number;
      className?: string;
    }
    ```

    This allows chart components to extend BaseChartProps with their specific data point types while maintaining type safety.
  </action>
  <verify>
    <automated>grep -n "BaseChartProps<T" apps/web/src/lib/chart-utils.ts</automated>
  </verify>
  <done>BaseChartProps uses generic type parameter T with default Record<string, unknown></done>
</task>

<task type="auto">
  <name>Task 2: Update chart components to use generic BaseChartProps</name>
  <files>
    apps/web/src/components/charts/FinancialChart.tsx
    apps/web/src/components/charts/WeightChart.tsx
    apps/web/src/components/charts/HabitStreakChart.tsx
    apps/web/src/components/charts/HobbyProgressChart.tsx
  </files>
  <read_first>
    - apps/web/src/components/charts/FinancialChart.tsx
    - apps/web/src/components/charts/WeightChart.tsx
    - apps/web/src/components/charts/HabitStreakChart.tsx
    - apps/web/src/components/charts/HobbyProgressChart.tsx
  </read_first>
  <behavior>
    - Each chart component extends BaseChartProps with its specific data point type
    - No data property re-declaration needed (inherited from generic base)
    - Type safety preserved for each chart's specific data structure
  </behavior>
  <action>
    Update each chart component's Props interface to extend BaseChartProps with the specific data type:

    1. FinancialChart.tsx (line 26-30):
       Change FROM:
       ```typescript
       export interface FinancialChartProps extends BaseChartProps {
         data: FinancialDataPoint[];
         days?: number;
         currency?: string;
       }
       ```
       Change TO:
       ```typescript
       export interface FinancialChartProps extends BaseChartProps<FinancialDataPoint> {
         currency?: string;
       }
       ```

    2. WeightChart.tsx (line 24-28):
       Change FROM:
       ```typescript
       export interface WeightChartProps extends BaseChartProps {
         data: WeightDataPoint[];
         days?: number;
         unit?: 'kg' | 'lbs';
       }
       ```
       Change TO:
       ```typescript
       export interface WeightChartProps extends BaseChartProps<WeightDataPoint> {
         unit?: 'kg' | 'lbs';
       }
       ```

    3. HabitStreakChart.tsx (line 26-30):
       Change FROM:
       ```typescript
       export interface HabitStreakChartProps extends BaseChartProps {
         data: HabitStreakDataPoint[];
         days?: number;
         showArea?: boolean;
       }
       ```
       Change TO:
       ```typescript
       export interface HabitStreakChartProps extends BaseChartProps<HabitStreakDataPoint> {
         showArea?: boolean;
       }
       ```

    4. HobbyProgressChart.tsx (line 30-35):
       Change FROM:
       ```typescript
       export interface HobbyProgressChartProps extends BaseChartProps {
         data: HobbyProgressDataPoint[];
         trackingType: HobbyTrackingType;
         days?: number;
         goalTarget?: number;
       }
       ```
       Change TO:
       ```typescript
       export interface HobbyProgressChartProps extends BaseChartProps<HobbyProgressDataPoint> {
         trackingType: HobbyTrackingType;
         goalTarget?: number;
       }
       ```

    Note: Remove the redundant `days?: number` from each interface since it's already in BaseChartProps.
  </action>
  <verify>
    <automated>grep -n "extends BaseChartProps<" apps/web/src/components/charts/*.tsx | wc -l | xargs -I {} test {} -eq 4</automated>
  </verify>
  <done>All four chart components extend BaseChartProps with their specific data types</done>
</task>

<task type="auto">
  <name>Task 3: Verify build succeeds</name>
  <files>None (verification only)</files>
  <read_first>
    - apps/web/src/lib/chart-utils.ts
    - apps/web/src/components/charts/FinancialChart.tsx
  </read_first>
  <action>
    Run the Next.js build to verify the TypeScript errors are resolved:

    ```bash
    cd /Users/nhan/Developer/my-projects/pms && pnpm nx build web
    ```

    The build should complete without TypeScript errors. The expected output should show:
    - No TypeScript compilation errors
    - Static export generated in apps/web/out/ directory
    - All pages compiled successfully
  </action>
  <verify>
    <automated>cd /Users/nhan/Developer/my-projects/pms && pnpm nx build web 2>&1 | grep -E "(error|Error|failed|Successfully)" | head -20</automated>
  </verify>
  <done>Build completes successfully with static files in apps/web/out/</done>
</task>

</tasks>

<verification>
1. Run `grep -n "BaseChartProps<T" apps/web/src/lib/chart-utils.ts` - should find the generic interface
2. Run `grep -n "extends BaseChartProps<" apps/web/src/components/charts/*.tsx` - should find 4 matches
3. Run `pnpm nx build web` - should complete without errors
4. Verify `apps/web/out/` directory contains static HTML/CSS/JS files
</verification>

<success_criteria>
- [ ] BaseChartProps uses generic type parameter T with default type
- [ ] All four chart components extend BaseChartProps with specific data types
- [ ] No redundant data property declarations in chart interfaces
- [ ] TypeScript build completes without errors
- [ ] Static export files generated in apps/web/out/
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-client/09-08-SUMMARY.md`
</output>
