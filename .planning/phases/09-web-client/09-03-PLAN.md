---
phase: 09-web-client
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/api.ts
  - apps/web/src/lib/api/financial.ts
  - apps/web/src/lib/api/habits.ts
  - apps/web/src/lib/api/tasks.ts
  - apps/web/src/lib/api/health.ts
  - apps/web/src/lib/api/notes.ts
  - apps/web/src/lib/api/hobbies.ts
  - apps/web/src/lib/queryClient.ts
  - apps/web/src/hooks/useFinancialData.ts
  - apps/web/src/hooks/useHabitsData.ts
  - apps/web/src/hooks/useTasksData.ts
  - apps/web/src/hooks/useHealthData.ts
  - apps/web/src/hooks/useNotesData.ts
  - apps/web/src/hooks/useHobbiesData.ts
autonomous: true
requirements:
  - WEB-03
must_haves:
  truths:
    - "TanStack Query v5 installed and configured"
    - "QueryClientProvider wraps app in root layout"
    - "API client functions fetch from /api/v1 endpoints"
    - "Custom hooks use useQuery for data fetching"
    - "Stale time configured to 30 seconds per CONTEXT.md"
    - "Refetch on window focus enabled per CONTEXT.md"
  artifacts:
    - path: "apps/web/package.json"
      provides: "TanStack Query v5 dependency"
      contains: "\"@tanstack/react-query\": \"^5\""
    - path: "apps/web/src/lib/queryClient.ts"
      provides: "Configured QueryClient with 30s staleTime"
      contains: "staleTime.*30000"
    - path: "apps/web/src/lib/api.ts"
      provides: "Base API client with fetch wrapper"
      min_lines: 20
    - path: "apps/web/src/lib/api/*.ts"
      provides: "Module-specific API functions"
      contains: "export.*function"
    - path: "apps/web/src/hooks/use*.ts"
      provides: "TanStack Query hooks for each module"
      contains: "useQuery"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/lib/queryClient.ts"
      via: "QueryClientProvider"
      pattern: "QueryClientProvider.*client"
    - from: "apps/web/src/hooks/useFinancialData.ts"
      to: "apps/web/src/lib/api/financial.ts"
      via: "import API functions"
      pattern: "import.*from.*api/financial"
    - from: "apps/web/src/app/dashboard/page.tsx"
      to: "apps/web/src/hooks/use*.ts"
      via: "use custom hooks in components"
      pattern: "useFinancialData\\|useHabitsData"
---

<objective>
Integrate TanStack Query for server state management with shared data-access hooks

Purpose: Provide efficient data fetching, caching, and synchronization with backend APIs for all modules
Output: Working TanStack Query setup with custom hooks for each module's data needs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-client/09-CONTEXT.md

@/Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (root layout for provider)
@/Users/nhan/Developer/my-projects/pms/libs/feature-financial/src/presentation/controllers/ (backend API endpoints to consume)
@/Users/nhan/Developer/my-projects/pms/libs/shared-types/src/index.ts (Zod schemas for validation)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install TanStack Query v5 and configure QueryClient</name>
  <files>apps/web/package.json, apps/web/src/lib/queryClient.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/package.json (current dependencies)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (TanStack Query v5 decision)
  </read_first>
  <action>
    Install TanStack Query v5 and create QueryClient configuration:

    1. Add dependencies via pnpm:
       - pnpm add @tanstack/react-query@^5
       - pnpm add @tanstack/react-query-devtools@^5 -D (dev tools)

    2. Create apps/web/src/lib/queryClient.ts:
       - Import QueryClient from @tanstack/react-query
       - Create and export queryClient with default options:
         * staleTime: 30000 (30 seconds)
         * refetchOnWindowFocus: true
         * retry: 1 (one retry on failure)
         * retryDelay: 1000 (1 second)
       - Add React Query DevTools in development only

    Per CONTEXT.md: "TanStack Query v5 with React Query DevTools in development"
    Per CONTEXT.md: "Stale time: 30 seconds"
    Per CONTEXT.md: "Refetch strategy: On window focus"
  </action>
  <verify>
    <automated>grep -q "\"@tanstack/react-query\"" apps/web/package.json && grep -q "staleTime: 30000" apps/web/src/lib/queryClient.ts && grep -q "refetchOnWindowFocus: true" apps/web/src/lib/queryClient.ts</automated>
  </verify>
  <done>
    - @tanstack/react-query@^5 in package.json dependencies
    - queryClient.ts exports configured QueryClient instance
    - QueryClient has staleTime: 30000, refetchOnWindowFocus: true
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wrap app with QueryClientProvider in root layout</name>
  <files>apps/web/src/app/layout.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (current layout)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/queryClient.ts (QueryClient instance)
  </read_first>
  <action>
    Update root layout to wrap with TanStack Query providers:

    1. Import QueryClientProvider from @tanstack/react-query
    2. Import queryClient from lib/queryClient
    3. Import ReactQueryDevtools from @tanstack/react-query-devtools
    4. Wrap children with QueryClientProvider client={queryClient}
    5. Add ReactQueryDevtools initialIsOpen={false} after provider (dev only)

    Ensure providers are in correct order:
    - Outer: QueryClientProvider
    - Inner: ReactQueryDevtools (conditionally rendered in dev)

    Pattern: Check for process.env.NODE_ENV === 'development' for DevTools
  </action>
  <verify>
    <automated>grep -q "QueryClientProvider" apps/web/src/app/layout.tsx && grep -q "ReactQueryDevtools" apps/web/src/app/layout.tsx && grep -q "queryClient" apps/web/src/app/layout.tsx</automated>
  </verify>
  <done>
    - layout.tsx imports QueryClientProvider and queryClient
    - layout.tsx wraps children with QueryClientProvider
    - layout.tsx includes ReactQueryDevtools (conditional on dev)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create base API client with fetch wrapper</name>
  <files>apps/web/src/lib/api.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (API base URL: /api/v1/)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (app context)
  </read_first>
  <action>
    Create base API client in apps/web/src/lib/api.ts:

    1. Create fetch wrapper function with:
       - Base URL: /api/v1/ (relative path for same-origin)
       - Headers: Content-Type: application/json
       - Cookie handling: credentials: 'include' (for httpOnly auth cookies)
       - Error handling: Check response.ok, throw error with message
       - Response parsing: await res.json()

    2. Export typed API client functions:
       - get<T>(url: string): Promise<T>
       - post<T>(url: string, data: unknown): Promise<T>
       - put<T>(url: string, data: unknown): Promise<T>
       - delete<T>(url: string): Promise<T>

    3. Handle error response format from backend:
       - Backend returns { success: false, error: { code, message, details } }
       - Extract and throw error message

    Per CONTEXT.md: "Base URL: API at /api/v1/ relative path"
    Per CONTEXT.md: "Error format: { success: false, error: { code, message, details } }"
  </action>
  <verify>
    <automated>grep -q "export.*function get" apps/web/src/lib/api.ts && grep -q "/api/v1/" apps/web/src/lib/api.ts && grep -q "credentials.*include" apps/web/src/lib/api.ts</automated>
  </verify>
  <done>
    - api.ts exports get, post, put, delete functions
    - Functions use /api/v1/ base URL
    - Functions include credentials: 'include' for cookies
    - Error handling extracts backend error messages
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create Financial API client functions</name>
  <files>apps/web/src/lib/api/financial.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/libs/feature-financial/src/presentation/controllers/financial.controller.ts (backend endpoints)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api.ts (base client)
  </read_first>
  <action>
    Create Financial API client in apps/web/src/lib/api/financial.ts:

    1. Import { get, post, put, delete } from lib/api
    2. Create typed functions for all Financial endpoints:
       - getAccounts(): Promise<Account[]>
       - getTransactions(filters?: TransactionFilters): Promise<Transaction[]>
       - getCategories(): Promise<CategoryTree>
       - getBudgets(): Promise<Budget[]>
       - createTransaction(data: CreateTransactionDto): Promise<Transaction>

    3. Use Zod schemas from shared-types for runtime validation if available
    4. Type definitions should match backend DTOs

    Backend endpoints (from feature-financial):
    - GET /api/v1/financial/accounts
    - GET /api/v1/financial/transactions
    - GET /api/v1/financial/categories
    - GET /api/v1/financial/budgets

    Note: For now, use 'any' types or create basic interfaces. Full typing can come from shared-types.
  </action>
  <verify>
    <automated>grep -q "export.*function getAccounts" apps/web/src/lib/api/financial.ts && grep -q "from '../api'" apps/web/src/lib/api/financial.ts</automated>
  </verify>
  <done>
    - financial.ts exports getAccounts, getTransactions, getCategories, getBudgets
    - Functions use base API client (get, post, etc.)
    - Functions typed with appropriate interfaces
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create remaining module API clients (Habits, Tasks, Health, Notes, Hobbies)</name>
  <files>apps/web/src/lib/api/habits.ts, apps/web/src/lib/api/tasks.ts, apps/web/src/lib/api/health.ts, apps/web/src/lib/api/notes.ts, apps/web/src/lib/api/hobbies.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/libs/feature-habits/src/presentation/controllers/ (backend endpoints)
    - /Users/nhan/Developer/my-projects/pms/libs/feature-tasks/src/presentation/controllers/ (backend endpoints)
    - /Users/nhan/Developer/my-projects/pms/libs/feature-health/src/presentation/controllers/ (backend endpoints)
    - /Users/nhan/Developer/my-projects/pms/libs/feature-notes/src/presentation/controllers/ (backend endpoints)
    - /Users/nhan/Developer/my-projects/pms/libs/feature-hobbies/src/presentation/controllers/ (backend endpoints)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api.ts (base client)
  </read_first>
  <action>
    Create API client files for remaining modules:

    **apps/web/src/lib/api/habits.ts:**
    - getHabits(): Promise<Habit[]>
    - getHabitCheckIns(habitId: string): Promise<CheckIn[]>
    - createCheckIn(habitId: string): Promise<CheckIn>

    **apps/web/src/lib/api/tasks.ts:**
    - getTasks(filters?: TaskFilters): Promise<Task[]>
    - createTask(data: CreateTaskDto): Promise<Task>
    - updateTask(id: string, data: UpdateTaskDto): Promise<Task>

    **apps/web/src/lib/api/health.ts:**
    - getHealthLogs(type: string, days: number): Promise<HealthLog[]>
    - getWeightTrends(days: number): Promise<WeightPoint[]>
    - getHealthDashboard(): Promise<HealthDashboard>

    **apps/web/src/lib/api/notes.ts:**
    - getNotes(): Promise<Note[]>
    - createNote(data: CreateNoteDto): Promise<Note>
    - searchNotes(query: string): Promise<Note[]>

    **apps/web/src/lib/api/hobbies.ts:**
    - getHobbies(): Promise<Hobby[]>
    - getHobbyTrends(hobbyId: string, days: number): Promise<HobbyTrend[]>
    - logHobbyProgress(hobbyId: string, data: LogProgressDto): Promise<HobbyLog>

    Each file follows the pattern from Task 4 (financial.ts).
  </action>
  <verify>
    <automated>grep -q "export.*function getHabits" apps/web/src/lib/api/habits.ts && grep -q "export.*function getTasks" apps/web/src/lib/api/tasks.ts && grep -q "export.*function getHealthLogs" apps/web/src/lib/api/health.ts && grep -q "export.*function getNotes" apps/web/src/lib/api/notes.ts && grep -q "export.*function getHobbies" apps/web/src/lib/api/hobbies.ts</automated>
  </verify>
  <done>
    - habits.ts exports getHabits, getHabitCheckIns, createCheckIn
    - tasks.ts exports getTasks, createTask, updateTask
    - health.ts exports getHealthLogs, getWeightTrends, getHealthDashboard
    - notes.ts exports getNotes, createNote, searchNotes
    - hobbies.ts exports getHobbies, getHobbyTrends, logHobbyProgress
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Create TanStack Query hooks for each module</name>
  <files>apps/web/src/hooks/useFinancialData.ts, apps/web/src/hooks/useHabitsData.ts, apps/web/src/hooks/useTasksData.ts, apps/web/src/hooks/useHealthData.ts, apps/web/src/hooks/useNotesData.ts, apps/web/src/hooks/useHobbiesData.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api/financial.ts (API functions)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api/habits.ts (API functions)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api/tasks.ts (API functions)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api/health.ts (API functions)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api/notes.ts (API functions)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/api/hobbies.ts (API functions)
  </read_first>
  <action>
    Create TanStack Query hooks for each module in apps/web/src/hooks/:

    **useFinancialData.ts:**
    - useAccounts(): Hook for fetching accounts
    - useTransactions(filters?): Hook for fetching transactions
    - useCategories(): Hook for fetching categories

    **useHabitsData.ts:**
    - useHabits(): Hook for fetching habits
    - useHabitCheckIns(habitId): Hook for fetching check-ins

    **useTasksData.ts:**
    - useTasks(filters?): Hook for fetching tasks
    - useTaskStats(): Hook for task statistics

    **useHealthData.ts:**
    - useHealthLogs(type, days): Hook for fetching health logs
    - useWeightTrends(days): Hook for weight trend data
    - useHealthDashboard(): Hook for dashboard summary

    **useNotesData.ts:**
    - useNotes(): Hook for fetching notes
    - useNoteSearch(query): Hook for searching notes

    **useHobbiesData.ts:**
    - useHobbies(): Hook for fetching hobbies
    - useHobbyTrends(hobbyId, days): Hook for hobby trends

    Each hook:
    - Imports useQuery from @tanstack/react-query
    - Imports corresponding API function from lib/api/*
    - Returns { data, isLoading, error, refetch }
    - Uses queryKey pattern: ['module', 'entity', params]
  </action>
  <verify>
    <automated>grep -q "useQuery" apps/web/src/hooks/useFinancialData.ts && grep -q "useQuery" apps/web/src/hooks/useHabitsData.ts && grep -q "useQuery" apps/web/src/hooks/useTasksData.ts && grep -q "useQuery" apps/web/src/hooks/useHealthData.ts && grep -q "useQuery" apps/web/src/hooks/useNotesData.ts && grep -q "useQuery" apps/web/src/hooks/useHobbiesData.ts</automated>
  </verify>
  <done>
    - All 6 hook files created in apps/web/src/hooks/
    - Each hook uses useQuery from @tanstack/react-query
    - Each hook imports API functions from lib/api/*
    - Each hook exports named hook functions
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Update dashboard to use real data from custom hooks</name>
  <files>apps/web/src/app/dashboard/page.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/dashboard/page.tsx (current dashboard with placeholders)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useFinancialData.ts (financial hooks)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useHabitsData.ts (habits hooks)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useTasksData.ts (tasks hooks)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useHealthData.ts (health hooks)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useNotesData.ts (notes hooks)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useHobbiesData.ts (hobbies hooks)
  </read_first>
  <action>
    Update dashboard page to use real data from custom hooks:

    1. Import all data hooks from hooks/
    2. Call hooks in component:
       - const { data: accounts } = useAccounts()
       - const { data: habits } = useHabits()
       - const { data: tasks } = useTasks()
       - const { data: healthDashboard } = useHealthDashboard()
       - const { data: notes } = useNotes()
       - const { data: hobbies } = useHobbies()

    3. Update ModuleCard metrics to use real data:
       - Financial: Sum of account balances, calculate from accounts
       - Habits: Calculate completion rate from habits data
       - Tasks: Count overdue and today's tasks
       - Health: Use healthDashboard for latest metrics
       - Notes: Count notes from data
       - Hobbies: Calculate active and completion from data

    4. Add loading states: Show skeleton or loading text while data fetches
    5. Add error states: Show error message if fetch fails

    Per CONTEXT.md: "Loading states: Skeleton screens"
    Per CONTEXT.md: "Error handling: Toast notification + inline error message"

    For now, use simple loading text. Toast implementation comes in plan 09-04 (shadcn/ui).
  </action>
  <verify>
    <automated>grep -q "useAccounts\\|useHabits\\|useTasks\\|useHealthDashboard\\|useNotes\\|useHobbies" apps/web/src/app/dashboard/page.tsx && grep -q "isLoading" apps/web/src/app/dashboard/page.tsx</automated>
  </verify>
  <done>
    - dashboard/page.tsx imports and uses all 6 module hooks
    - ModuleCard metrics use real data from hooks
    - Loading states displayed (isLoading checks)
    - Error states displayed (error checks)
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. TanStack Query v5 installed in package.json
2. QueryClientProvider wraps app in root layout
3. QueryClient configured with 30s staleTime and refetchOnWindowFocus
4. Base API client with /api/v1/ base URL
5. Module-specific API clients for all 6 modules
6. Custom hooks use useQuery for data fetching
7. Dashboard uses real data from hooks (not placeholders)
</verification>

<success_criteria>
1. TanStack Query v5 installed and configured
2. QueryClientProvider wraps app (root layout)
3. QueryClient has 30s staleTime, refetchOnWindowFocus enabled
4. Base API client at lib/api.ts with /api/v1/ base URL
5. API clients for all 6 modules (Financial, Habits, Tasks, Health, Notes, Hobbies)
6. Custom hooks in hooks/ directory use useQuery
7. Dashboard displays real data from backend APIs
8. WEB-03 requirement verified: TanStack Query for server state management
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-client/09-03-SUMMARY.md`
</output>
