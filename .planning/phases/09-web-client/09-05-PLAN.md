---
phase: 09-web-client
plan: 05
type: execute
wave: 5
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - apps/web/package.json
  - apps/web/src/components/charts/WeightChart.tsx
  - apps/web/src/components/charts/FinancialChart.tsx
  - apps/web/src/components/charts/HabitStreakChart.tsx
  - apps/web/src/components/charts/HobbyProgressChart.tsx
  - apps/web/src/app/financial/page.tsx
  - apps/web/src/app/health/page.tsx
  - apps/web/src/app/habits/page.tsx
  - apps/web/src/app/hobbies/page.tsx
autonomous: true
requirements:
  - WEB-05
must_haves:
  truths:
    - "Recharts installed and integrated"
    - "Weight chart displays line chart with trends"
    - "Financial chart displays spending bars with trends"
    - "Habit streak chart displays completion history"
    - "Hobby progress chart displays tracking-type-specific visualization"
    - "All charts use semantic colors (green=positive, red=negative, blue=neutral)"
    - "Charts have tooltips on hover"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Recharts dependency"
      contains: "\"recharts\":"
    - path: "apps/web/src/components/charts/WeightChart.tsx"
      provides: "Weight trend line chart component"
      min_lines: 30
    - path: "apps/web/src/components/charts/FinancialChart.tsx"
      provides: "Financial spending bar chart component"
      min_lines: 40
    - path: "apps/web/src/components/charts/HabitStreakChart.tsx"
      provides: "Habit streak visualization component"
      min_lines: 30
    - path: "apps/web/src/components/charts/HobbyProgressChart.tsx"
      provides: "Hobby progress chart (counter/percentage/list)"
      min_lines: 50
    - path: "apps/web/src/app/health/page.tsx"
      provides: "Health module page with weight chart"
      min_lines: 20
    - path: "apps/web/src/app/financial/page.tsx"
      provides: "Financial module page with spending chart"
      min_lines: 20
  key_links:
    - from: "apps/web/src/app/health/page.tsx"
      to: "apps/web/src/components/charts/WeightChart.tsx"
      via: "import WeightChart"
      pattern: "import.*WeightChart"
    - from: "apps/web/src/app/financial/page.tsx"
      to: "apps/web/src/components/charts/FinancialChart.tsx"
      via: "import FinancialChart"
      pattern: "import.*FinancialChart"
    - from: "apps/web/src/components/charts/WeightChart.tsx"
      to: "recharts"
      via: "import LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer"
      pattern: "from 'recharts'"
---

<objective>
Integrate Recharts for data visualization across all modules

Purpose: Provide clean, semantic charts for financial, health, habits, and hobbies data with tooltips and interactive hover states
Output: Working chart components using Recharts with semantic colors and time range presets
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-client/09-CONTEXT.md

@/Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useHealthData.ts (health data hooks)
@/Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useFinancialData.ts (financial data hooks)
@/Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useHabitsData.ts (habits data hooks)
@/Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useHobbiesData.ts (hobbies data hooks)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Recharts and create shared chart utilities</name>
  <files>apps/web/package.json, apps/web/src/lib/chart-utils.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/package.json (current dependencies)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (Recharts decision)
  </read_first>
  <action>
    Install Recharts and create shared chart utilities:

    1. Add Recharts dependency:
       ```bash
       pnpm add recharts
       ```

    2. Create apps/web/src/lib/chart-utils.ts with:
       - Semantic color constants:
         * positive: '#22c55e' (green-500)
         * negative: '#ef4444' (red-500)
         * neutral: '#3b82f6' (blue-500)
       - Chart date formatting helper (for X-axis labels)
       - Chart data formatting helper (currency, percentages)
       - Common chart props interface

    Per CONTEXT.md: "Chart style: Minimal line/bar charts — Clean, data-focused, no 3D or gradients"
    Per CONTEXT.md: "Color palette: Semantic colors (green=positive, red=negative, blue=neutral)"
  </action>
  <verify>
    <automated>grep -q "\"recharts\"" apps/web/package.json && grep -q "positive.*green" apps/web/src/lib/chart-utils.ts && grep -q "negative.*red" apps/web/src/lib/chart-utils.ts</automated>
  </verify>
  <done>
    - recharts dependency added to package.json
    - chart-utils.ts created with semantic color constants
    - Helper functions for date and value formatting
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create WeightChart component for health trends</name>
  <files>apps/web/src/components/charts/WeightChart.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/chart-utils.ts (chart utilities)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (chart spec: minimal line, semantic colors, tooltips)
  </read_first>
  <action>
    Create WeightChart component in apps/web/src/components/charts/WeightChart.tsx:

    1. Import from recharts:
       - LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer

    2. Component props:
       - data: Array of { date, weight }
       - days: number (30, 90, 365)

    3. Chart configuration:
       - LineChart with responsive container (100% width, 300px height)
       - XAxis: Date labels (formatted MM/DD)
       - YAxis: Weight values (no decimal for lbs, 1 decimal for kg)
       - Line: indigo-600 color, dot radius 4, active dot radius 6
       - Tooltip: Custom content showing date and weight
       - Grid: Horizontal lines only (clean look)

    4. Styling:
       - Minimal grid (horizontal only)
       - No gradients or fancy effects
       - Smooth curves (type="monotone")

    Per CONTEXT.md: "Chart style: Minimal line/bar charts"
    Per CONTEXT.md: "Interactivity: Tooltips on hover"
  </action>
  <verify>
    <automated>grep -q "LineChart" apps/web/src/components/charts/WeightChart.tsx && grep -q "from 'recharts'" apps/web/src/components/charts/WeightChart.tsx && grep -q "Tooltip" apps/web/src/components/charts/WeightChart.tsx</automated>
  </verify>
  <done>
    - WeightChart.tsx created with LineChart from recharts
    - Chart displays weight trend line
    - Tooltip shows date and weight on hover
    - Uses indigo-600 color for line
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create FinancialChart component for spending visualization</name>
  <files>apps/web/src/components/charts/FinancialChart.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/chart-utils.ts (chart utilities)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (semantic colors: green income, red expense)
  </read_first>
  <action>
    Create FinancialChart component in apps/web/src/components/charts/FinancialChart.tsx:

    1. Import from recharts:
       - BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend

    2. Component props:
       - data: Array of { date, income, expense }
       - days: number (30, 90, 365)

    3. Chart configuration:
       - BarChart with two bars (income, expense)
       - Income bar: green-500 (positive)
       - Expense bar: red-500 (negative)
       - XAxis: Date labels
       - YAxis: Currency values
       - Tooltip: Show income and expense for each date
       - Legend: Show income/expense color key

    4. Styling:
       - Bar radius for rounded corners
       - No gradients
       - Grid lines for readability

    Per CONTEXT.md: "Color palette: Semantic colors (green=positive, red=negative)"
  </action>
  <verify>
    <automated>grep -q "BarChart" apps/web/src/components/charts/FinancialChart.tsx && grep -q "green" apps/web/src/components/charts/FinancialChart.tsx && grep -q "red" apps/web/src/components/charts/FinancialChart.tsx</automated>
  </verify>
  <done>
    - FinancialChart.tsx created with BarChart from recharts
    - Chart displays income (green) and expense (red) bars
    - Tooltip shows both values on hover
    - Legend shows color key
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create HabitStreakChart component for habit visualization</name>
  <files>apps/web/src/components/charts/HabitStreakChart.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/chart-utils.ts (chart utilities)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (habit streaks, calendar view)
  </read_first>
  <action>
    Create HabitStreakChart component in apps/web/src/components/charts/HabitStreakChart.tsx:

    1. Import from recharts:
       - LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer

    2. Component props:
       - data: Array of { date, streakLength }
       - days: number (30, 90, 365)

    3. Chart configuration:
       - LineChart showing streak over time
       - XAxis: Date labels
       - YAxis: Streak length (integer values)
       - Line: blue-500 color (neutral/progress)
       - Tooltip: Show date and streak length
       - Area fill below line (optional, use light blue opacity)

    4. For calendar view alternative (simpler for now):
       - Use line chart to show streak progression
       - Full calendar view can be v2 feature

    Per CONTEXT.md: "Habit calendar with completion history visualization" (simplified to line chart for v1)
  </action>
  <verify>
    <automated>grep -q "LineChart" apps/web/src/components/charts/HabitStreakChart.tsx && grep -q "streak" apps/web/src/components/charts/HabitStreakChart.tsx && grep -q "from 'recharts'" apps/web/src/components/charts/HabitStreakChart.tsx</automated>
  </verify>
  <done>
    - HabitStreakChart.tsx created with LineChart
    - Chart displays streak length over time
    - Tooltip shows date and streak
    - Uses blue-500 color
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create HobbyProgressChart component with tracking-type support</name>
  <files>apps/web/src/components/charts/HobbyProgressChart.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/chart-utils.ts (chart utilities)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (hobby tracking types: counter, percentage, list)
  </read_first>
  <action>
    Create HobbyProgressChart component in apps/web/src/components/charts/HobbyProgressChart.tsx:

    1. Import from recharts:
       - LineChart, Line, BarChart, Bar, ComposedChart, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer

    2. Component props:
       - data: Array of { date, value, cumulative } (from backend trends API)
       - trackingType: 'COUNTER' | 'PERCENTAGE' | 'LIST'
       - days: number

    3. Chart configuration by tracking type:
       - COUNTER: ComposedChart with bars (daily increments) + line (cumulative total)
         * Bars: blue-500 (daily increments)
         * Line: indigo-600 (running total)
       - PERCENTAGE: LineChart showing progress over time
         * Line: green-500 (progress toward goal)
       - LIST: BarChart showing activity count per date
         * Bars: blue-500 (items logged per day)

    4. Styling:
       - Minimal grid
       - Tooltips with appropriate labels
       - Y-axis labels based on type (count, %, items)

    Per CONTEXT.md: "Counter charts include both bars (daily increments) and line (running total)"
  </action>
  <verify>
    <automated>grep -q "trackingType" apps/web/src/components/charts/HobbyProgressChart.tsx && grep -q "COUNTER\\|PERCENTAGE\\|LIST" apps/web/src/components/charts/HobbyProgressChart.tsx && grep -q "from 'recharts'" apps/web/src/components/charts/HobbyProgressChart.tsx</automated>
  </verify>
  <done>
    - HobbyProgressChart.tsx created with tracking type support
    - COUNTER type shows bars + line (dual visualization)
    - PERCENTAGE type shows progress line chart
    - LIST type shows activity bar chart
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Create module pages with chart integration</name>
  <files>apps/web/src/app/financial/page.tsx, apps/web/src/app/health/page.tsx, apps/web/src/app/habits/page.tsx, apps/web/src/app/hobbies/page.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/DashboardLayout.tsx (layout wrapper)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/charts/WeightChart.tsx (weight chart)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/charts/FinancialChart.tsx (financial chart)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/hooks/useFinancialData.ts (data hooks)
  </read_first>
  <action>
    Create module pages with chart integration:

    **apps/web/src/app/financial/page.tsx:**
    1. Wrap in DashboardLayout
    2. Import useTransactions, useAccounts hooks
    3. Render FinancialChart with transaction data
    4. Add time range selector (30d, 90d, 365d buttons)
    5. Show summary cards (total balance, monthly spending)

    **apps/web/src/app/health/page.tsx:**
    1. Wrap in DashboardLayout
    2. Import useWeightTrends hook
    3. Render WeightChart with trend data
    4. Add time range selector
    5. Show latest metrics card

    **apps/web/src/app/habits/page.tsx:**
    1. Wrap in DashboardLayout
    2. Import useHabits hook
    3. Render habit list with HabitStreakChart for each
    4. Show streak badges

    **apps/web/src/app/hobbies/page.tsx:**
    1. Wrap in DashboardLayout
    2. Import useHobbies, useHobbyTrends hooks
    3. Render hobby list with HobbyProgressChart for each
    4. Show goal progress

    Per CONTEXT.md: "Time ranges: Preset buttons (30d/90d/365d) + custom date picker"
  </action>
  <verify>
    <automated>grep -q "FinancialChart" apps/web/src/app/financial/page.tsx && grep -q "WeightChart" apps/web/src/app/health/page.tsx && grep -q "HabitStreakChart\\|habits" apps/web/src/app/habits/page.tsx && grep -q "HobbyProgressChart" apps/web/src/app/hobbies/page.tsx</automated>
  </verify>
  <done>
    - Financial page with FinancialChart component
    - Health page with WeightChart component
    - Habits page with HabitStreakChart component
    - Hobbies page with HobbyProgressChart component
    - All pages have time range selectors (30d/90d/365d)
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Recharts installed in package.json
2. Chart utilities created with semantic colors
3. WeightChart displays weight trend line
4. FinancialChart displays income/expense bars with semantic colors
5. HabitStreakChart displays streak progression
6. HobbyProgressChart supports all 3 tracking types
7. Module pages render appropriate charts
8. Charts have tooltips on hover
9. Time range selectors (30d/90d/365d) functional
</verification>

<success_criteria>
1. Recharts installed and integrated
2. Weight chart in health page shows line trend
3. Financial chart shows income (green) and expense (red) bars
4. Habit streak chart shows completion history
5. Hobby progress chart adapts to tracking type (counter/percentage/list)
6. All charts use semantic colors per CONTEXT.md
7. All charts have tooltips on hover
8. Time range preset buttons (30d/90d/365d) work
9. WEB-05 requirement verified: Recharts for data visualization
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-client/09-05-SUMMARY.md`
</output>
