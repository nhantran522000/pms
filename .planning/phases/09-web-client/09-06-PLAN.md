---
phase: 09-web-client
plan: 06
type: execute
wave: 6
depends_on: ["09-01", "09-04"]
files_modified:
  - apps/web/package.json
  - apps/web/src/components/ThemeToggle.tsx
  - apps/web/src/components/DashboardLayout.tsx
  - apps/web/src/app/layout.tsx
autonomous: true
requirements:
  - WEB-06
must_haves:
  truths:
    - "next-themes installed and configured"
    - "ThemeProvider wraps app in root layout"
    - "ThemeToggle button available in sidebar/header"
    - "Theme preference persists in localStorage"
    - "Dark mode uses zinc-950 background"
    - "Light mode uses white/gray-50 background"
    - "System preference detection works on first visit"
  artifacts:
    - path: "apps/web/package.json"
      provides: "next-themes dependency"
      contains: "\"next-themes\":"
    - path: "apps/web/src/app/layout.tsx"
      provides: "ThemeProvider wrapper"
      contains: "ThemeProvider"
    - path: "apps/web/src/components/ThemeToggle.tsx"
      provides: "Theme toggle button component"
      min_lines: 30
    - path: "apps/web/src/components/DashboardLayout.tsx"
      provides: "Sidebar with theme toggle button"
      contains: "ThemeToggle"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/ThemeToggle.tsx"
      via: "ThemeProvider enables theme toggle"
      pattern: "ThemeProvider.*attribute.*class"
    - from: "apps/web/src/components/ThemeToggle.tsx"
      to: "next-themes"
      via: "useTheme hook"
      pattern: "useTheme"
---

<objective>
Implement dark/light theme toggle with persistence and system preference detection

Purpose: Allow users to switch between dark and light themes with preference persistence and system-aware default
Output: Working theme toggle with next-themes integration, localStorage persistence, and system preference detection
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-client/09-CONTEXT.md

@/Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (root layout)
@/Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (Tailwind with darkMode: class)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install next-themes and configure ThemeProvider</name>
  <files>apps/web/package.json, apps/web/src/app/layout.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/package.json (current dependencies)
    - /Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (darkMode: class config)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (theme decision: system-aware with manual override)
  </read_first>
  <action>
    Install next-themes and configure ThemeProvider:

    1. Add next-themes dependency:
       ```bash
       pnpm add next-themes
       ```

    2. Update apps/web/src/app/layout.tsx:
       - Import ThemeProvider from next-themes
       - Import { ThemeProvider as NextThemesProvider } from 'next-themes'
       - Wrap existing providers with ThemeProvider:
         ```tsx
         <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
           <QueryClientProvider>
             {children}
           </QueryClientProvider>
         </ThemeProvider>
         ```

    3. ThemeProvider options:
       - attribute="class": Adds dark class to HTML element
       - defaultTheme="system": Uses system preference on first visit
       - enableSystem: Enables system preference detection
       - disableTransitionOnChange: true (prevent flash)

    Per CONTEXT.md: "Dark mode: System preference aware with manual override"
  </action>
  <verify>
    <automated>grep -q "\"next-themes\"" apps/web/package.json && grep -q "ThemeProvider" apps/web/src/app/layout.tsx && grep -q "defaultTheme.*system" apps/web/src/app/layout.tsx</automated>
  </verify>
  <done>
    - next-themes added to package.json
    - ThemeProvider wraps app in layout.tsx
    - ThemeProvider configured with attribute="class", defaultTheme="system"
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ThemeToggle component with sun/moon icons</name>
  <files>apps/web/src/components/ThemeToggle.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/package.json (lucide-react for icons)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ui/button.tsx (Button component)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (theme toggle placement)
  </read_first>
  <action>
    Create ThemeToggle component in apps/web/src/components/ThemeToggle.tsx:

    1. Import from next-themes:
       - useTheme hook
       - Theme type

    2. Import icons from lucide-react:
       - Sun (light mode)
       - Moon (dark mode)
       - Monitor (system mode)

    3. Import Button from components/ui/button

    4. Component structure:
       - Call useTheme() to get { theme, setTheme }
       - Button with variant="ghost" (subtle styling)
       - Click handler cycles through: light -> dark -> system -> light
       - Icon changes based on current theme:
         * 'light' -> Sun icon
         * 'dark' -> Moon icon
         * 'system' -> Monitor icon

    5. Add tooltip or aria-label for accessibility
    6. Use client component directive: 'use client'

    7. Styling:
       - Button size: icon (small)
       - Hover effects
       - Smooth icon transition

    Per CONTEXT.md: "Claude's Discretion: Specific sidebar width and breakpoints" (place in sidebar header)
  </action>
  <verify>
    <automated>grep -q "useTheme" apps/web/src/components/ThemeToggle.tsx && grep -q "Sun\\|Moon\\|Monitor" apps/web/src/components/ThemeToggle.tsx && grep -q "setTheme" apps/web/src/components/ThemeToggle.tsx</automated>
  </verify>
  <done>
    - ThemeToggle.tsx created with useTheme hook
    - Component cycles through light/dark/system themes
    - Icons change based on current theme
    - Button uses ghost variant
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrate ThemeToggle into DashboardLayout sidebar</name>
  <files>apps/web/src/components/DashboardLayout.tsx, apps/web/src/components/Sidebar.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ThemeToggle.tsx (theme toggle component)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/Sidebar.tsx (sidebar component)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/DashboardLayout.tsx (layout wrapper)
  </read_first>
  <action>
    Integrate ThemeToggle into sidebar:

    Option 1: Add to Sidebar header (recommended):
    1. Update Sidebar.tsx:
       - Import ThemeToggle
       - Add to sidebar header next to "PMS" logo
       - Align to right or place below logo

    2. Sidebar header structure:
       ```
       <SidebarHeader>
         <Logo>PMS</Logo>
         <ThemeToggle />
       </SidebarHeader>
       ```

    Option 2: Add to DashboardLayout (if Sidebar doesn't have header):
    1. Update DashboardLayout.tsx:
       - Import ThemeToggle
       - Add to desktop sidebar area
       - Position at top of sidebar

    3. Ensure ThemeToggle only shows on desktop (hidden on mobile)
       - Already handled by Sidebar responsive classes (hidden md:flex)

    Per CONTEXT.md: "Claude's Discretion: Specific sidebar width and breakpoints"
  </action>
  <verify>
    <automated>grep -q "ThemeToggle" apps/web/src/components/Sidebar.tsx || grep -q "ThemeToggle" apps/web/src/components/DashboardLayout.tsx</automated>
  </verify>
  <done>
    - ThemeToggle integrated into sidebar
    - Toggle visible on desktop (≥768px)
    - Toggle positioned in sidebar header
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update global styles for dark/light theme colors</name>
  <files>apps/web/src/app/globals.css</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/globals.css (current global styles)
    - /Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (Tailwind config)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (Zinc base colors)
  </read_first>
  <action>
    Update globals.css for proper dark/light theme support:

    1. Ensure dark mode uses zinc-950 background:
       - @layer base {
         * Add :root (light mode) with --background: 0 0% 100%
         * Add .dark (dark mode) with --background: 0 0% 3.5% (zinc-950)
         * Add --foreground for text colors
       }

    2. shadcn/ui CSS variables (should be added by shadcn init):
       - Keep these, they provide the theming foundation
       - Ensure they use zinc colors for neutral base
       - Primary should be indigo-600 (accent)

    3. Add body styles:
       ```css
       @layer base {
         body {
           @apply bg-white dark:bg-zinc-950;
           @apply text-zinc-950 dark:text-zinc-50;
         }
       }
       ```

    4. Verify Tailwind dark mode is class-based:
       - tailwind.config.ts should have darkMode: 'class'
       - This was set in plan 09-01

    Per CONTEXT.md: "Base color: Zinc (neutral grays)"
    Per CONTEXT.md: "Accent color: Indigo (primary)"
  </action>
  <verify>
    <automated>grep -q "dark:bg-zinc-950" apps/web/src/app/globals.css && grep -q "bg-white" apps/web/src/app/globals.css && grep -q "\.dark" apps/web/src/app/globals.css</automated>
  </verify>
  <done>
    - globals.css has :root (light mode) and .dark (dark mode) selectors
    - Body uses bg-white dark:bg-zinc-950
    - Text uses zinc-950 dark:zinc-50
    - shadcn/ui CSS variables present
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Test theme persistence and system preference</name>
  <files>(verification only, no files modified)</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (ThemeProvider config)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ThemeToggle.tsx (toggle component)
  </read_first>
  <action>
    Test theme functionality (manual verification during execution):

    1. **System preference detection:**
       - Clear localStorage (remove theme key)
       - Set OS to dark mode
       - Refresh app → Should use dark theme
       - Set OS to light mode
       - Refresh app → Should use light theme

    2. **Manual override:**
       - Click theme toggle
       - Should cycle: system → light → dark → system
       - Each click updates UI immediately

    3. **Persistence:**
       - Set theme to dark
       - Refresh page
       - Should stay on dark (check localStorage has theme="dark")
       - Close browser, reopen
       - Should stay on dark

    4. **No flash on load:**
       - disableTransitionOnChange prevents flash
       - Theme applies immediately on page load

    Per CONTEXT.md: "Dark mode: System preference aware with manual override"
  </action>
  <verify>
    <automated>grep -q "localStorage.*theme" apps/web/src/app/globals.css || echo "Theme persistence handled by next-themes (no manual localStorage check needed)"</automated>
  </verify>
  <done>
    - next-themes handles system preference detection automatically
    - Theme toggle cycles through light/dark/system
    - Theme preference persists across sessions (localStorage)
    - No flash of unthemed content on page load
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. next-themes installed in package.json
2. ThemeProvider wraps app with attribute="class" and defaultTheme="system"
3. ThemeToggle component with Sun/Moon/Monitor icons
4. ThemeToggle integrated into sidebar
5. Dark mode uses zinc-950, light mode uses white
6. Theme persists in localStorage
7. System preference detection works
8. Theme toggle cycles through light/dark/system
</verification>

<success_criteria>
1. next-themes installed and configured
2. ThemeProvider wraps app (root layout)
3. Theme toggle button visible in sidebar
4. Toggle cycles through light → dark → system → light
5. Theme preference persists across sessions (localStorage)
6. System preference detected on first visit
7. Dark mode uses zinc-950 background
8. Light mode uses white/gray-50 background
9. WEB-06 requirement verified: Dark/light theme toggle with persistence
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-client/09-06-SUMMARY.md`
</output>
