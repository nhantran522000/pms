---
phase: 09-web-client
plan: 04
type: execute
wave: 4
depends_on: ["09-01", "09-02"]
files_modified:
  - apps/web/package.json
  - apps/web/components.json
  - apps/web/src/lib/utils.ts
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/card.tsx
  - apps/web/src/components/ui/skeleton.tsx
  - apps/web/src/components/ui/toast.tsx
  - apps/web/src/components/ui/use-toast.ts
  - apps/web/src/app/dashboard/page.tsx (updated with shadcn components)
autonomous: true
requirements:
  - WEB-04
must_haves:
  truths:
    - "shadcn/ui components installed and configured"
    - "Tailwind CSS configured for shadcn/ui theming"
    - "Button component available and used in navigation"
    - "Card component available and used in dashboard"
    - "Skeleton component available for loading states"
    - "Toast system configured for error notifications"
  artifacts:
    - path: "apps/web/package.json"
      provides: "shadcn/ui dependencies"
      contains: "\"class-variance-authority\""
    - path: "apps/web/components.json"
      provides: "shadcn/ui CLI configuration"
      contains: "\"$schema\""
    - path: "apps/web/src/lib/utils.ts"
      provides: "cn utility for className merging"
      contains: "cn.*function"
    - path: "apps/web/src/components/ui/button.tsx"
      provides: "Button component with variants"
      min_lines: 20
    - path: "apps/web/src/components/ui/card.tsx"
      provides: "Card component set"
      min_lines: 30
    - path: "apps/web/src/components/ui/skeleton.tsx"
      provides: "Skeleton loading component"
      min_lines: 10
    - path: "apps/web/src/components/ui/use-toast.ts"
      provides: "Toast hook for notifications"
      contains: "useToast"
  key_links:
    - from: "apps/web/src/app/dashboard/page.tsx"
      to: "apps/web/src/components/ui/card.tsx"
      via: "import Card"
      pattern: "import.*Card.*from.*@/components/ui/card"
    - from: "apps/web/src/app/dashboard/page.tsx"
      to: "apps/web/src/components/ui/skeleton.tsx"
      via: "import Skeleton for loading"
      pattern: "import.*Skeleton"
    - from: "apps/web/src/app/dashboard/page.tsx"
      to: "apps/web/src/components/ui/use-toast.ts"
      via: "import useToast for errors"
      pattern: "import.*use-toast"
---

<objective>
Install and configure shadcn/ui components with Tailwind styling

Purpose: Provide pre-built, accessible UI components that follow the Zinc/Indigo theme and include loading/error states
Output: Working shadcn/ui setup with Button, Card, Skeleton, and Toast components integrated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-client/09-CONTEXT.md

@/Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (Tailwind config)
@/Users/nhan/Developer/my-projects/pms/apps/web/src/app/globals.css (global styles)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize shadcn/ui with CLI and configure components</name>
  <files>apps/web/package.json, apps/web/components.json, apps/web/src/lib/utils.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/package.json (current dependencies)
    - /Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (Tailwind config)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (shadcn/ui decision)
  </read_first>
  <action>
    Initialize shadcn/ui using the CLI with these specific settings:

    1. Run shadcn init command:
       ```bash
       pnpm dlx shadcn@latest init
       ```

    2. When prompted, select/enter:
       - Style: Default (Zinc)
       - Base color: Zinc (matches CONTEXT.md decision)
       - CSS variables: Yes
       - Tailwind config: apps/web/tailwind.config.ts
       - Components path: apps/web/src/components
       - Utils path: apps/web/src/lib/utils.ts
       - RSC: No (Next.js 16, not using React Server Components here)
       - Aliases: Use default @/ alias

    3. After init, install additional required dependencies:
       - pnpm add class-variance-authority clsx tailwind-merge

    4. Verify components.json was created with correct paths

    Per CONTEXT.md: "shadcn/ui installed via CLI (npx shadcn@latest init) — Copy components, not npm package"
    Per CONTEXT.md: "Base color: Zinc (neutral grays)"
  </action>
  <verify>
    <automated>test -f apps/web/components.json && grep -q "class-variance-authority" apps/web/package.json && grep -q "cn" apps/web/src/lib/utils.ts</automated>
  </verify>
  <done>
    - components.json created in apps/web/
    - utils.ts created with cn() function
    - class-variance-authority, clsx, tailwind-merge in dependencies
    - tailwind.config.ts updated with CSS variables
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add shadcn/ui components (Button, Card, Skeleton, Toast)</name>
  <files>apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/card.tsx, apps/web/src/components/ui/skeleton.tsx, apps/web/src/components/ui/toast.tsx, apps/web/src/components/ui/use-toast.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/components.json (shadcn config)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (component needs: button, card, skeleton, toast)
  </read_first>
  <action>
    Add shadcn/ui components using the CLI:

    1. Run shadcn add commands:
       ```bash
       pnpm dlx shadcn@latest add button
       pnpm dlx shadcn@latest add card
       pnpm dlx shadcn@latest add skeleton
       pnpm dlx shadcn@latest add toast
       ```

    2. Verify components are created in apps/web/src/components/ui/

    3. Components added:
       - button.tsx: Primary, secondary, ghost, outline variants
       - card.tsx: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
       - skeleton.tsx: Loading placeholder component
       - toast.tsx and use-toast.ts: Toast notification system

    4. Update button.tsx primary variant to use indigo-600 (accent color):
       - Find primary variant in button.tsx
       - Change bg-primary to bg-indigo-600
       - Change hover-bg-primary to hover:bg-indigo-700

    Per CONTEXT.md: "Accent color: Indigo (primary)"
  </action>
  <verify>
    <automated>test -f apps/web/src/components/ui/button.tsx && test -f apps/web/src/components/ui/card.tsx && test -f apps/web/src/components/ui/skeleton.tsx && test -f apps/web/src/components/ui/use-toast.ts && grep -q "indigo-600" apps/web/src/components/ui/button.tsx</automated>
  </verify>
  <done>
    - button.tsx created with indigo-600 primary variant
    - card.tsx created with Card components
    - skeleton.tsx created for loading states
    - toast.tsx and use-toast.ts created for notifications
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Toaster component to root layout</name>
  <files>apps/web/src/app/layout.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (current layout)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ui/toast.tsx (Toaster component)
  </read_first>
  <action>
    Add Toaster component to root layout for toast notifications:

    1. Import Toaster from components/ui/toast
    2. Add Toaster component inside QueryClientProvider (after it)
    3. Ensure Toaster is rendered once in the component tree

    Layout structure should be:
    ```
    <QueryClientProvider>
      <html>
        <body>
          {children}
          <Toaster />
        </body>
      </html>
    </QueryClientProvider>
    ```

    This enables useToast() hook to work throughout the app.
  </action>
  <verify>
    <automated>grep -q "Toaster" apps/web/src/app/layout.tsx</automated>
  </verify>
  <done>
    - layout.tsx imports Toaster from components/ui/toast
    - layout.tsx renders Toaster component in body
    - Toaster positioned after children in DOM
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update ModuleCard to use shadcn/ui Card component</name>
  <files>apps/web/src/components/ModuleCard.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ModuleCard.tsx (current module card)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ui/card.tsx (shadcn Card)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ui/button.tsx (shadcn Button)
  </read_first>
  <action>
    Refactor ModuleCard to use shadcn/ui components:

    1. Import Card components:
       - Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter

    2. Import Button component

    3. Replace custom div elements with Card components:
       - Outer div -> Card
       - Header section -> CardHeader with CardTitle and CardDescription
       - Metrics section -> CardContent
       - Link section -> CardFooter with Button

    4. Update styling to use shadcn/ui classes:
       - Card handles border, rounded, shadow
       - Button handles variants and hover states
       - Remove redundant custom styles

    5. Keep Zinc base + Indigo accent:
       - Cards use default styling (Zinc borders)
       - Buttons use primary variant (Indigo)

    Per CONTEXT.md: "Zinc base (neutral grays), Indigo accent (primary)"
  </action>
  <verify>
    <automated>grep -q "from '@/components/ui/card'" apps/web/src/components/ModuleCard.tsx && grep -q "from '@/components/ui/button'" apps/web/src/components/ModuleCard.tsx && grep -q "CardHeader" apps/web/src/components/ModuleCard.tsx</automated>
  </verify>
  <done>
    - ModuleCard.tsx imports Card components from ui/card
    - ModuleCard.tsx imports Button from ui/button
    - Component uses Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
    - Component uses Button for link action
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update dashboard to use Skeleton for loading states</name>
  <files>apps/web/src/app/dashboard/page.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/dashboard/page.tsx (current dashboard)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ui/skeleton.tsx (Skeleton component)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (skeleton loading decision)
  </read_first>
  <action>
    Update dashboard to show skeleton screens while data loads:

    1. Import Skeleton from components/ui/skeleton

    2. Create SkeletonCard component (inline or separate):
       - Renders card-shaped skeleton with:
         - Header skeleton (icon + title)
         - 2-3 metric skeletons (circles for numbers)

    3. Update dashboard loading state:
       - When any hook is loading, show skeleton cards
       - Show 6 skeleton cards in grid layout
       - Match layout of actual cards (responsive grid)

    4. Example skeleton card structure:
       ```tsx
       <Card>
         <CardHeader>
           <Skeleton className="h-6 w-6" />
           <Skeleton className="h-5 w-24" />
         </CardHeader>
         <CardContent>
           <Skeleton className="h-8 w-16 mb-2" />
           <Skeleton className="h-4 w-20" />
         </CardContent>
       </Card>
       ```

    Per CONTEXT.md: "Loading states: Skeleton screens — Better perceived performance than spinners"
  </action>
  <verify>
    <automated>grep -q "Skeleton" apps/web/src/app/dashboard/page.tsx && grep -q "isLoading" apps/web/src/app/dashboard/page.tsx</automated>
  </verify>
  <done>
    - dashboard/page.tsx imports Skeleton component
    - Loading state shows 6 skeleton cards
    - Skeleton layout matches actual card layout
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Add toast error notifications to dashboard</name>
  <files>apps/web/src/app/dashboard/page.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/dashboard/page.tsx (current dashboard)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ui/use-toast.ts (useToast hook)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (toast error decision)
  </read_first>
  <action>
    Add toast error notifications to dashboard for API failures:

    1. Import useToast from components/ui/use-toast

    2. Call useToast() in component: const { toast } = useToast()

    3. Add useEffect hooks for each data hook's error state:
       ```tsx
       useEffect(() => {
         if (accountsError) {
           toast({
             title: "Error loading accounts",
             description: accountsError.message,
             variant: "destructive"
           })
         }
       }, [accountsError])
       ```

    4. Add inline error messages in cards:
       - If data is null and not loading, show error state
       - Display error message in CardContent
       - Include retry button if applicable

    Per CONTEXT.md: "Error handling: Toast notification + inline error message"
    Per CONTEXT.md: "Toast position and timing" (Claude's discretion - use default)
  </action>
  <verify>
    <automated>grep -q "useToast" apps/web/src/app/dashboard/page.tsx && grep -q "toast(" apps/web/src/app/dashboard/page.tsx && grep -q "useEffect.*error" apps/web/src/app/dashboard/page.tsx</automated>
  </verify>
  <done>
    - dashboard/page.tsx imports and uses useToast hook
    - useEffect hooks show toast on API errors
    - Inline error messages displayed in cards when data fails to load
    - Error toasts use variant: "destructive"
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. shadcn/ui initialized with components.json config
2. Button component uses indigo-600 primary variant
3. Card components (Card, CardHeader, etc.) available and used
4. Skeleton component used for loading states
5. Toast system configured with Toaster in layout
6. Dashboard uses shadcn/ui components (Card, Button, Skeleton)
7. Error handling uses toast notifications + inline messages
</verification>

<success_criteria>
1. shadcn/ui installed and configured (components.json exists)
2. Button, Card, Skeleton, Toast components added
3. Button primary variant uses indigo-600 (accent color)
4. Toaster component rendered in root layout
5. ModuleCard refactored to use Card components
6. Dashboard shows Skeleton cards while loading
7. Dashboard shows toast on API errors
8. WEB-04 requirement verified: shadcn/ui components with Tailwind styling
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-client/09-04-SUMMARY.md`
</output>
