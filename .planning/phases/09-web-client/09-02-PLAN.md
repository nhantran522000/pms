---
phase: 09-web-client
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/components/DashboardLayout.tsx
  - apps/web/src/components/Sidebar.tsx
  - apps/web/src/components/BottomTabBar.tsx
  - apps/web/src/components/ModuleCard.tsx
  - apps/web/src/app/dashboard/page.tsx
  - apps/web/src/lib/navigation.ts
autonomous: true
requirements:
  - WEB-02
must_haves:
  truths:
    - "Dashboard displays with sidebar on desktop (≥768px width)"
    - "Dashboard displays with bottom tab bar on mobile (<768px width)"
    - "Home screen shows cross-module summary with cards for each domain"
    - "Navigation links to all 6 modules (Financial, Habits, Tasks, Health, Notes, Hobbies)"
    - "Module cards show key metrics per CONTEXT.md specifications"
  artifacts:
    - path: "apps/web/src/components/DashboardLayout.tsx"
      provides: "Responsive layout shell with sidebar/mobile nav"
      min_lines: 50
    - path: "apps/web/src/components/Sidebar.tsx"
      provides: "Desktop sidebar navigation (≥768px)"
      min_lines: 30
    - path: "apps/web/src/components/BottomTabBar.tsx"
      provides: "Mobile bottom tab navigation (<768px)"
      min_lines: 30
    - path: "apps/web/src/components/ModuleCard.tsx"
      provides: "Reusable card for module summary display"
      min_lines: 20
    - path: "apps/web/src/app/dashboard/page.tsx"
      provides: "Home dashboard with cross-module summary"
      min_lines: 40
    - path: "apps/web/src/lib/navigation.ts"
      provides: "Navigation config (modules, routes, icons)"
      min_lines: 20
  key_links:
    - from: "apps/web/src/app/dashboard/page.tsx"
      to: "apps/web/src/components/ModuleCard.tsx"
      via: "import ModuleCard"
      pattern: "import.*ModuleCard"
    - from: "apps/web/src/components/DashboardLayout.tsx"
      to: "apps/web/src/components/Sidebar.tsx"
      via: "responsive render (md:hidden vs hidden md:flex)"
      pattern: "hidden md:flex"
    - from: "apps/web/src/components/DashboardLayout.tsx"
      to: "apps/web/src/components/BottomTabBar.tsx"
      via: "responsive render (md:flex vs hidden md:hidden)"
      pattern: "md:hidden"
---

<objective>
Create responsive dashboard layout with module navigation (sidebar desktop, bottom tab mobile)

Purpose: Provide the primary navigation structure for the PMS web app with mobile-first responsive design
Output: Working dashboard layout with navigation to all 6 modules and cross-module home screen
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-client/09-CONTEXT.md
@.planning/phases/09-web-client/09-01-PLAN.md

@/Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (root layout)
@/Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (Tailwind config)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create navigation configuration with modules and routes</name>
  <files>apps/web/src/lib/navigation.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (module organization by domain)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (existing structure)
  </read_first>
  <action>
    Create navigation configuration in apps/web/src/lib/navigation.ts with:

    1. Export array of navigation items with:
       - name: Display name (Financial, Habits & Tasks, Health, Notes, Hobbies)
       - href: Route path (/financial, /habits, /tasks, /health, /notes, /hobbies)
       - icon: Icon component name (lucide-react icons)
       - description: Brief description for module cards

    2. Organize by domain per CONTEXT.md:
       - Finance: Financial
       - Habits & Tasks: Habits, Tasks
       - Health: Health
       - Productivity: Notes, Hobbies

    3. Export helper functions:
       - getModuleNavItems(): Returns all nav items
       - getModulesByDomain(domain): Returns filtered items

    Per CONTEXT.md: "Module organization: By domain (Finance: Financial, Habits & Tasks, Health: Health, Productivity: Notes, Hobbies)"
  </action>
  <verify>
    <automated>grep -q "export.*getModuleNavItems" apps/web/src/lib/navigation.ts && grep -q "Financial" apps/web/src/lib/navigation.ts && grep -q "Habits" apps/web/src/lib/navigation.ts</automated>
  </verify>
  <done>
    - navigation.ts exports nav items array with 6 modules
    - Each item has name, href, icon, description
    - Helper functions getModuleNavItems and getModulesByDomain exist
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: create Sidebar component for desktop navigation</name>
  <files>apps/web/src/components/Sidebar.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/navigation.ts (nav items config)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (sidebar decision: desktop only, ≥768px)
  </read_first>
  <action>
    Create Sidebar component in apps/web/src/components/Sidebar.tsx:

    1. Import nav items from navigation.ts
    2. Import icons from lucide-react (DollarSign, CheckSquare2, ListTodo, Heart, FileText, Trophy)
    3. Use Next.js Link component for navigation
    4. Apply responsive classes: hidden md:flex (mobile hidden, desktop flex)
    5. Fixed width: w-64 (256px) on desktop
    6. Styling:
       - Zinc-900 background in dark mode
       - Zinc-50 background in light mode
       - Border-r for separator
       - Flex column layout
       - Logo/header at top ("PMS")
       - Nav links with hover states

    Per CONTEXT.md: "Navigation pattern: Sidebar (desktop) + bottom tab bar (mobile)"
    Per CONTEXT.md: "Mobile breakpoint: Bottom tab navigation below 768px width"

    Use lucide-react icons (install via pnpm add lucide-react if not already in deps)
  </action>
  <verify>
    <automated>grep -q "hidden md:flex" apps/web/src/components/Sidebar.tsx && grep -q "from 'lucide-react'" apps/web/src/components/Sidebar.tsx && grep -q "Link.*href" apps/web/src/components/Sidebar.tsx</automated>
  </verify>
  <done>
    - Sidebar.tsx component exports default
    - Component uses lucide-react icons
    - Component has responsive classes (hidden md:flex, w-64)
    - Nav items rendered from navigation.ts config
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create BottomTabBar component for mobile navigation</name>
  <files>apps/web/src/components/BottomTabBar.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/navigation.ts (nav items config)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (mobile bottom tab decision)
  </read_first>
  <action>
    Create BottomTabBar component in apps/web/src/components/BottomTabBar.tsx:

    1. Import nav items from navigation.ts
    2. Import icons from lucide-react
    3. Use Next.js Link component for navigation
    4. Apply responsive classes: md:hidden (mobile only, hidden on desktop)
    5. Fixed bottom position with safe-area-inset-bottom
    6. Styling:
       - Fixed bottom: fixed bottom-0
       - Full width: w-full
       - Border-t for separator
       - Flex row layout
       - Scrollable horizontal if needed (for 6 items)

    7. Each tab:
       - Icon + label (vertical stack)
       - Active state styling (indigo-600)
       - Hover states

    Per CONTEXT.md: "Navigation pattern: Sidebar (desktop) + bottom tab bar (mobile)"
    Per CONTEXT.md: "Mobile breakpoint: Bottom tab navigation below 768px width"
  </action>
  <verify>
    <automated>grep -q "fixed bottom-0" apps/web/src/components/BottomTabBar.tsx && grep -q "md:hidden" apps/web/src/components/BottomTabBar.tsx && grep -q "from 'lucide-react'" apps/web/src/components/BottomTabBar.tsx</automated>
  </verify>
  <done>
    - BottomTabBar.tsx component exports default
    - Component is fixed at bottom (fixed bottom-0)
    - Component has mobile-only classes (md:hidden)
    - Nav items rendered from navigation.ts config
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create ModuleCard component for dashboard summaries</name>
  <files>apps/web/src/components/ModuleCard.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (module cards specifications)
    - /Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (Tailwind config)
  </read_first>
  <action>
    Create ModuleCard component in apps/web/src/components/ModuleCard.tsx:

    1. Accept props:
       - name: string
       - description: string
       - href: string
       - icon: LucideIcon
       - metrics: array of { label, value, trend? }

    2. Styling (Zinc base + Indigo accent):
       - Card background: bg-white dark:bg-zinc-900
       - Border: border border-zinc-200 dark:border-zinc-800
       - Hover: hover:border-indigo-300 dark:hover:border-indigo-700
       - Rounded: rounded-lg
       - Shadow: shadow-sm

    3. Layout:
       - Header: icon + name + description
       - Metrics grid: 2-3 key metrics per card
       - Link button: "View" with arrow icon

    4. Metrics per CONTEXT.md:
       - Financial: balance, monthly spending, budget progress
       - Habits: completion rate, current streak
       - Tasks: overdue count, today's tasks
       - Health: latest weight, sleep quality
       - Notes: recent count, mood trend
       - Hobbies: active count, completion rate

    Per CONTEXT.md module cards specification
  </action>
  <verify>
    <automated>grep -q "interface.*ModuleCardProps" apps/web/src/components/ModuleCard.tsx && grep -q "metrics" apps/web/src/components/ModuleCard.tsx && grep -q "bg-white dark:bg-zinc-900" apps/web/src/components/ModuleCard.tsx</automated>
  </verify>
  <done>
    - ModuleCard.tsx component exports default
    - Component accepts name, description, href, icon, metrics props
    - Component uses Zinc base colors (bg-zinc-900 in dark mode)
    - Component renders metrics grid and link button
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Create DashboardLayout component integrating Sidebar and BottomTabBar</name>
  <files>apps/web/src/components/DashboardLayout.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/Sidebar.tsx (desktop sidebar)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/BottomTabBar.tsx (mobile nav)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (root layout pattern)
  </read_first>
  <action>
    Create DashboardLayout component in apps/web/src/components/DashboardLayout.tsx:

    1. Accept children prop for page content
    2. Layout structure:
       - Sidebar (desktop, left)
       - Main content area (right, with padding for bottom nav on mobile)
       - BottomTabBar (mobile, fixed bottom)

    3. Responsive behavior:
       - Desktop: Sidebar visible, content area expands to fill remaining width
       - Mobile: Sidebar hidden, content area has bottom padding (pb-16 for bottom tab bar)

    4. Main content area:
       - Flex-1 to fill available space
       - Overflow handling for content
       - Max-width container for readability

    5. Use flex row for desktop layout (sidebar + main)
    6. Use relative positioning for main content (absolute bottom nav)

    Per CONTEXT.md: "Home screen: Cross-module summary dashboard"
  </action>
  <verify>
    <automated>grep -q "children" apps/web/src/components/DashboardLayout.tsx && grep -q "Sidebar" apps/web/src/components/DashboardLayout.tsx && grep -q "BottomTabBar" apps/web/src/components/DashboardLayout.tsx && grep -q "flex-row" apps/web/src/components/DashboardLayout.tsx</automated>
  </verify>
  <done>
    - DashboardLayout.tsx component exports default
    - Component accepts and renders children prop
    - Component includes Sidebar (desktop) and BottomTabBar (mobile)
    - Layout uses flex-row for desktop, handles mobile padding
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Create dashboard page with cross-module summary</name>
  <files>apps/web/src/app/dashboard/page.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/DashboardLayout.tsx (layout wrapper)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/components/ModuleCard.tsx (card component)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/lib/navigation.ts (nav config)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (home screen spec)
  </read_first>
  <action>
    Create dashboard page in apps/web/src/app/dashboard/page.tsx:

    1. Wrap content in DashboardLayout component
    2. Page heading: "Dashboard" or "Welcome to PMS"
    3. Grid layout for module cards (1 col mobile, 2 col tablet, 3 col desktop)
    4. Render ModuleCard for each of 6 modules:

    For now, use placeholder/dummy metrics (real data comes in plan 09-03):
    - Financial: { balance: "$0.00", spending: "$0.00", budget: "0%" }
    - Habits: { completion: "0%", streak: "0 days" }
    - Tasks: { overdue: "0", today: "0 tasks" }
    - Health: { weight: "--", sleep: "--" }
    - Notes: { recent: "0 notes", mood: "stable" }
    - Hobbies: { active: "0", completion: "0%" }

    5. Organize cards by domain per CONTEXT.md (optional grouping)

    Per CONTEXT.md: "Home screen: Cross-module summary dashboard — Key metrics from all modules in one view"

    Note: This creates the UI structure. Real metrics will be connected in plan 09-03 (TanStack Query).
  </action>
  <verify>
    <automated>grep -q "DashboardLayout" apps/web/src/app/dashboard/page.tsx && grep -q "ModuleCard" apps/web/src/app/dashboard/page.tsx && grep -q "Financial\|Habits\|Tasks" apps/web/src/app/dashboard/page.tsx</automated>
  </verify>
  <done>
    - dashboard/page.tsx renders DashboardLayout wrapper
    - Page renders 6 ModuleCard components (one per module)
    - Cards use placeholder metrics for now
    - Grid layout responsive (1/2/3 columns)
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Responsive navigation: Sidebar shows on desktop (≥768px), hides on mobile
2. Mobile navigation: Bottom tab bar shows on mobile (<768px), hides on desktop
3. Module cards: All 6 modules rendered with correct icons and labels
4. Navigation links: Each card/nav item links to correct route
5. Layout structure: DashboardLayout properly wraps content with nav elements
</verification>

<success_criteria>
1. Dashboard displays sidebar on desktop (width ≥768px)
2. Dashboard displays bottom tab bar on mobile (width <768px)
3. Home screen (/dashboard) shows cross-module summary with 6 module cards
4. All navigation links work (Financial, Habits, Tasks, Health, Notes, Hobbies)
5. Module cards show placeholder metrics (real data in plan 09-03)
6. Styling follows Zinc base + Indigo accent theme
7. WEB-02 requirement verified: Responsive dashboard with module navigation
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-client/09-02-SUMMARY.md`
</output>
