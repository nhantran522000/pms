---
phase: 09-web-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/project.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
  - apps/web/.eslintrc.json
  - apps/web/tailwind.config.ts
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Next.js 16.2 app generates with App Router structure"
    - "Static export configuration builds successfully without SSR server"
    - "Development server runs on localhost:3000"
    - "TypeScript compilation succeeds without errors"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Dependencies and scripts for Next.js app"
      contains: "\"next\": \"^16.2\""
    - path: "apps/web/next.config.ts"
      provides: "Static export configuration"
      contains: "output: 'export'"
    - path: "apps/web/project.json"
      provides: "Nx project configuration"
      contains: "\"type\": \"app\""
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root layout with metadata"
      min_lines: 10
    - path: "apps/web/src/app/page.tsx"
      provides: "Home page component"
      min_lines: 5
    - path: "apps/web/tailwind.config.ts"
      provides: "Tailwind CSS configuration"
      contains: "content: []"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "dist/apps/web"
      via: "nx build web"
      pattern: "output.*export"
    - from: "apps/web/package.json"
      to: "node_modules/next"
      via: "npm install"
      pattern: "\"next\""
---

<objective>
Initialize Next.js 16.2 web application with static export configuration in Nx monorepo

Purpose: Create the foundation for the PMS web client with static hosting capability (no SSR server required for deployment)
Output: Working Next.js app that builds to static files for serving via Caddy
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-web-client/09-CONTEXT.md

@CLAUDE.md (Nx workspace patterns)
@/Users/nhan/Developer/my-projects/pms/apps/api/project.json (reference for Nx app configuration)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate Next.js 16.2 app using Nx</name>
  <files>apps/web/package.json, apps/web/project.json, apps/web/tsconfig.json, apps/web/.eslintrc.json, apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/app/globals.css</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/CLAUDE.md (Nx workspace patterns, use nx-generate skill)
    - /Users/nhan/Developer/my-projects/pms/package.json (existing workspace dependencies)
    - /Users/nhan/Developer/my-projects/pms/apps/api/project.json (reference for Nx app structure)
  </read_first>
  <action>
    Use the nx-generate skill to invoke the Nx React generator with these specific parameters:
    - Generator: @nx/react:app
    - Name: web
    - Directory: apps/web
    - Style: tailwind
    - Routing: app (App Router, not Pages Router)
    - Builder: @nx/next:build (use Next.js builder, not Vite)
    - No SSR component: false (we need App Router components)
    - Skip format: true

    After generation, verify the created files and update package.json to ensure Next.js 16.2 version.

    Per CONTEXT.md decision: "Use Next.js 16.2 App Router (not Pages Router) — Latest patterns, better data fetching"
  </action>
  <verify>
    <automated>test -f apps/web/package.json && test -f apps/web/project.json && grep -q '"next":' apps/web/package.json</automated>
  </verify>
  <done>
    - apps/web directory exists with Next.js app structure
    - package.json contains next dependency (version ^16.2.x)
    - project.json configured with @nx/next:build executor
    - src/app directory exists with App Router structure (layout.tsx, page.tsx)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure static export in next.config.ts</name>
  <files>apps/web/next.config.ts</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/next.config.ts (current config after generation)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (static export requirement)
  </read_first>
  <action>
    Configure Next.js for static export by setting output mode in next.config.ts:

    1. Read the existing next.config.ts file
    2. Add the following configuration to the NextConfig object:
       ```typescript
       output: 'export'
       ```
    3. Ensure images configuration uses unoptimized: true (static export limitation)
    4. Add trailingSlash: true for consistent URLs

    Per CONTEXT.md decision: "Static export configuration for hosting on Caddy alongside API"

    Per ROADMAP WEB-01: "Static export build (no SSR server process)"
  </action>
  <verify>
    <automated>grep -q "output: 'export'" apps/web/next.config.ts && grep -q "trailingSlash: true" apps/web/next.config.ts</automated>
  </verify>
  <done>
    - next.config.ts contains output: 'export'
    - next.config.ts contains images: { unoptimized: true }
    - next.config.ts contains trailingSlash: true
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Configure Tailwind CSS with Inter font and Zinc/Indigo theme</name>
  <files>apps/web/tailwind.config.ts, apps/web/src/app/globals.css</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/tailwind.config.ts (current Tailwind config)
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/globals.css (current global styles)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (theme decisions: Zinc base, Indigo accent, Inter font)
  </read_first>
  <action>
    Configure Tailwind CSS per CONTEXT.md theme decisions:

    In tailwind.config.ts:
    1. Extend the theme with Inter font family
    2. Set primary color to indigo-600 (accent)
    3. Use default zinc colors (base)
    4. Add darkMode: 'class' for manual theme toggle (plan 09-06)

    Update content array to include:
    - ./src/app/**/*.{ts,tsx}
    - ./src/components/**/*.{ts,tsx}

    In globals.css:
    1. Add @tailwind directives (base, components, utilities)
    2. Add Inter font import via next/font
    3. Add CSS variables for theme colors (optional prep for shadcn/ui)

    Per CONTEXT.md decisions:
    - "Base color: Zinc (neutral grays)"
    - "Accent color: Indigo (primary)"
    - "Typography: Inter (variable font)"
    - "Dark mode: System preference aware with manual override"
  </action>
  <verify>
    <automated>grep -q "inter" apps/web/tailwind.config.ts && grep -q "indigo" apps/web/tailwind.config.ts && grep -q "darkMode.*class" apps/web/tailwind.config.ts</automated>
  </verify>
  <done>
    - tailwind.config.ts includes Inter font in fontFamily
    - tailwind.config.ts has primary: indigo-600 in extend.colors
    - tailwind.config.ts has darkMode: 'class' configured
    - globals.css includes @tailwind directives
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update root layout with metadata and basic structure</name>
  <files>apps/web/src/app/layout.tsx</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/src/app/layout.tsx (current layout)
    - /Users/nhan/Developer/my-projects/pms/.planning/phases/09-web-client/09-CONTEXT.md (app naming: PMS, installable web app)
  </read_first>
  <action>
    Update the root layout.tsx with proper metadata for the PMS web app:

    1. Set metadata:
       - title: "PMS - Personal Management System"
       - description: "Unified personal data platform with AI-powered insights"
       - viewport: "width=device-width, initial-scale=1"
       - themeColor: "#4f46e5" (indigo-600)

    2. Add Inter font via next/font/google:
       ```typescript
       import { Inter } from 'next/font/google'
       const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })
       ```

    3. Apply font variable to body className

    4. Ensure children are rendered within proper HTML structure

    Per CONTEXT.md: "Typography: Inter (variable font)"
  </action>
  <verify>
    <automated>grep -q "PMS.*Personal Management" apps/web/src/app/layout.tsx && grep -q "from 'next/font/google'" apps/web/src/app/layout.tsx</automated>
  </verify>
  <done>
    - layout.tsx exports metadata with title "PMS - Personal Management System"
    - layout.tsx imports Inter from next/font/google
    - layout.tsx applies font variable to root element
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Verify build and development server</name>
  <files>apps/web/dist (build output)</files>
  <read_first>
    - /Users/nhan/Developer/my-projects/pms/apps/web/package.json (available scripts)
    - /Users/nhan/Developer/my-projects/pms/CLAUDE.md (use nx run, not direct npm commands)
  </read_first>
  <action>
    Verify the Next.js app builds correctly as static export:

    1. Run build command: pnpm nx build web
    2. Verify dist/apps/web directory contains static files (index.html, _next/static, etc.)
    3. Check build output contains no SSR-specific errors
    4. Run dev server briefly: pnpm nx serve web (should start on localhost:4200 or 3000)
    5. Stop dev server after verification

    Per CLAUDE.md: "always prefer running the task through nx (i.e. nx run, nx run-many)"
    Per CLAUDE.md: "Prefix nx commands with the workspace's package manager (e.g. pnpm nx build)"

    Success criteria:
    - Build completes without errors
    - Static files generated in dist/apps/web
    - Dev server starts successfully
  </action>
  <verify>
    <automated>test -d dist/apps/web && test -f dist/apps/web/index.html</automated>
  </verify>
  <done>
    - pnpm nx build web completes with exit code 0
    - dist/apps/web/index.html exists
    - dist/apps/web/_next/static directory exists (bundled JS/CSS)
    - Dev server starts without errors (manual verification during execution)
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Static build test: Build produces pure static HTML/CSS/JS files (no server code)
2. Nx integration: Project follows Nx conventions (project.json, proper tags)
3. TypeScript: No type errors in apps/web/src
4. Tailwind: Global styles apply correctly (check globals.css structure)
</verification>

<success_criteria>
1. Next.js 16.2 app generates in apps/web directory with App Router
2. Static export configured (output: 'export' in next.config.ts)
3. Build produces static files in dist/apps/web (index.html, _next/static)
4. Tailwind CSS configured with Zinc base + Indigo accent theme
5. Inter font integrated via next/font/google
6. Development server runs on localhost (port assigned by Nx)
7. WEB-01 requirement verified: Static export build, no SSR server process
</success_criteria>

<output>
After completion, create `.planning/phases/09-web-client/09-01-SUMMARY.md`
</output>
