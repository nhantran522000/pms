---
phase: 06-notes-journal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/data-access/prisma/schema.prisma
  - libs/shared-types/src/notes/index.ts
  - libs/shared-types/src/notes/notes.schema.ts
  - libs/shared-types/src/index.ts
autonomous: true
requirements:
  - NOTE-01
  - NOTE-02
  - NOTE-03
  - NOTE-05
  - NOTE-06
must_haves:
  truths:
    - "Prisma schema contains Note, Folder, Tag, NoteTag, JournalEntry models"
    - "All models have tenantId for RLS isolation"
    - "Note has search_vector tsvector column for full-text search"
    - "Zod schemas exist for all DTOs"
  artifacts:
    - path: "libs/data-access/prisma/schema.prisma"
      provides: "Database models for Notes & Journal module"
      contains: "model Note"
    - path: "libs/shared-types/src/notes/notes.schema.ts"
      provides: "Zod validation schemas"
      exports: ["CreateNoteSchema", "UpdateNoteSchema", "CreateJournalEntrySchema"]
  key_links:
    - from: "schema.prisma"
      to: "Note model"
      via: "tenantId field"
      pattern: "tenantId.*String.*@map"
---

<objective>
Create the Prisma schema and Zod DTOs for the Notes & Journal module.

Purpose: Establish the database foundation and type contracts before implementing business logic.
Output: Updated Prisma schema with Note, Folder, Tag, NoteTag, JournalEntry models; Zod schemas in shared-types.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notes-journal/06-CONTEXT.md
@.planning/phases/06-notes-journal/06-RESEARCH.md
@.planning/phases/06-notes-journal/06-UI-SPEC.md

<interfaces>
<!-- Existing patterns from codebase - executor should follow these -->

From libs/data-access/prisma/schema.prisma:
```prisma
/// Model - Pattern for adding new models
model Example {
  id        String   @id @default(cuid())
  tenantId  String   @map("tenantId")
  // ... fields
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId])
  @@map("table_name")
}
```

From libs/shared-types/src/habits/habits.schema.ts:
```typescript
export const CreateHabitSchema = z.object({
  name: z.string().min(1).max(100),
  description: z.string().max(500).optional(),
  // ...
});
export type CreateHabitDto = z.infer<typeof CreateHabitSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Notes & Journal models to Prisma schema</name>
  <files>libs/data-access/prisma/schema.prisma</files>
  <read_first>
    - libs/data-access/prisma/schema.prisma (current schema structure)
    - .planning/phases/06-notes-journal/06-UI-SPEC.md (data structure contracts)
  </read_first>
  <action>Add the following models to schema.prisma after the HealthLog model. Follow the exact field names and types from the UI-SPEC data contracts:

1. **Folder model** (single-level hierarchy, no parent/child):
```prisma
/// Folder - Single-level folders for note organization
model Folder {
  id        String   @id @default(cuid())
  tenantId  String   @map("tenantId")
  name      String   @db.VarChar(100)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  notes     Note[]

  @@unique([tenantId, name])
  @@index([tenantId])
  @@map("folders")
}
```

2. **Tag model**:
```prisma
/// Tag - Tags for flexible note categorization
model Tag {
  id        String    @id @default(cuid())
  tenantId  String    @map("tenantId")
  name      String    @db.VarChar(50)
  createdAt DateTime  @default(now())
  tenant    Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  noteTags  NoteTag[]

  @@unique([tenantId, name])
  @@index([tenantId])
  @@map("tags")
}
```

3. **Note model** (with tsvector for full-text search):
```prisma
/// Note - Rich text notes with Tiptap content
model Note {
  id            String    @id @default(cuid())
  tenantId      String    @map("tenantId")
  title         String    @db.VarChar(200)
  content       Json      // Tiptap JSON document (max 100KB enforced in app)
  folderId      String?   @map("folderId")
  search_vector Unsupported("tsvector") // Full-text search vector
  deletedAt     DateTime? @map("deletedAt") // Soft delete
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  tenant        Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  folder        Folder?   @relation(fields: [folderId], references: [id], onDelete: SetNull)
  noteTags      NoteTag[]

  @@index([tenantId])
  @@index([folderId])
  @@index([deletedAt])
  @@map("notes")
}
```

4. **NoteTag junction model** (many-to-many):
```prisma
/// NoteTag - Junction table for note-tag relationships
model NoteTag {
  noteId    String
  tagId     String
  note      Note     @relation(fields: [noteId], references: [id], onDelete: Cascade)
  tag       Tag      @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@id([noteId, tagId])
  @@map("note_tags")
}
```

5. **JournalEntry model** (separate from notes, daily with mood):
```prisma
/// JournalEntry - Daily journal entries with mood tracking
model JournalEntry {
  id        String   @id @default(cuid())
  tenantId  String   @map("tenantId")
  entryDate DateTime @map("entryDate") @db.Date // Date only
  content   Json     // Tiptap JSON document
  mood      Int      // 1-5 scale: 1=terrible, 2=bad, 3=neutral, 4=good, 5=excellent
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, entryDate]) // One entry per day per tenant
  @@index([tenantId])
  @@index([entryDate])
  @@map("journal_entries")
}
```

6. **Update Tenant model** to add relations:
```prisma
// Add these lines inside Tenant model:
  folders        Folder[]
  tags           Tag[]
  notes          Note[]
  journalEntries JournalEntry[]
```

IMPORTANT: The `search_vector` column uses `Unsupported("tsvector")` type which Prisma supports for PostgreSQL. The actual tsvector trigger and GIN index will be added via raw SQL migration in plan 06-03.</action>
  <verify>
    <automated>grep -q "model Note" libs/data-access/prisma/schema.prisma && grep -q "model Folder" libs/data-access/prisma/schema.prisma && grep -q "model Tag" libs/data-access/prisma/schema.prisma && grep -q "model NoteTag" libs/data-access/prisma/schema.prisma && grep -q "model JournalEntry" libs/data-access/prisma/schema.prisma && echo "Schema validation passed"</automated>
  </verify>
  <acceptance_criteria>
    - schema.prisma contains "model Note" with id, tenantId, title, content (Json), folderId, search_vector, deletedAt
    - schema.prisma contains "model Folder" with id, tenantId, name
    - schema.prisma contains "model Tag" with id, tenantId, name
    - schema.prisma contains "model NoteTag" with @@id([noteId, tagId])
    - schema.prisma contains "model JournalEntry" with id, tenantId, entryDate, content (Json), mood (Int)
    - All new models have tenantId field with @map("tenantId")
    - Tenant model has relations to folders, tags, notes, journalEntries
    - Note has @@unique constraint on [tenantId, entryDate] for JournalEntry
  </acceptance_criteria>
  <done>Note, Folder, Tag, NoteTag, JournalEntry models exist in schema.prisma with tenantId for RLS and proper relations.</done>
</task>

<task type="auto">
  <name>Task 2: Create Zod schemas for Notes & Journal DTOs</name>
  <files>libs/shared-types/src/notes/notes.schema.ts, libs/shared-types/src/notes/index.ts, libs/shared-types/src/index.ts</files>
  <read_first>
    - libs/shared-types/src/habits/habits.schema.ts (pattern for Zod schemas)
    - libs/shared-types/src/health/health.schema.ts (pattern for Zod schemas)
    - libs/shared-types/src/index.ts (export pattern)
  </read_first>
  <action>Create the Zod schemas file following the established pattern from habits.schema.ts. Create the following schemas:

1. **Create libs/shared-types/src/notes/notes.schema.ts**:

```typescript
import { z } from 'zod';

// Mood scale (1-5)
export const MoodSchema = z.number().int().min(1).max(5);
export type Mood = z.infer<typeof MoodSchema>;

// Create Note
export const CreateNoteSchema = z.object({
  title: z.string().min(1).max(200),
  content: z.record(z.unknown()).optional().default({ type: 'doc', content: [] }), // Tiptap JSON
  folderId: z.string().cuid().optional().nullable(),
  tagIds: z.array(z.string().cuid()).max(20).optional().default([]), // Max 20 tags per NOTE-05
});
export type CreateNoteDto = z.infer<typeof CreateNoteSchema>;

// Update Note
export const UpdateNoteSchema = z.object({
  title: z.string().min(1).max(200).optional(),
  content: z.record(z.unknown()).optional(),
  folderId: z.string().cuid().nullable().optional(),
  tagIds: z.array(z.string().cuid()).max(20).optional(),
});
export type UpdateNoteDto = z.infer<typeof UpdateNoteSchema>;

// Note response
export const NoteResponseSchema = z.object({
  id: z.string(),
  tenantId: z.string(),
  title: z.string(),
  content: z.record(z.unknown()),
  folderId: z.string().nullable(),
  deletedAt: z.date().nullable(),
  createdAt: z.date(),
  updatedAt: z.date(),
  tags: z.array(z.object({
    id: z.string(),
    name: z.string(),
  })),
});
export type NoteResponse = z.infer<typeof NoteResponseSchema>;

// Create Folder
export const CreateFolderSchema = z.object({
  name: z.string().min(1).max(100),
});
export type CreateFolderDto = z.infer<typeof CreateFolderSchema>;

// Update Folder
export const UpdateFolderSchema = z.object({
  name: z.string().min(1).max(100).optional(),
});
export type UpdateFolderDto = z.infer<typeof UpdateFolderSchema>;

// Folder response
export const FolderResponseSchema = z.object({
  id: z.string(),
  tenantId: z.string(),
  name: z.string(),
  noteCount: z.number().optional(),
  createdAt: z.date(),
});
export type FolderResponse = z.infer<typeof FolderResponseSchema>;

// Create Tag
export const CreateTagSchema = z.object({
  name: z.string().min(1).max(50),
});
export type CreateTagDto = z.infer<typeof CreateTagSchema>;

// Tag response
export const TagResponseSchema = z.object({
  id: z.string(),
  tenantId: z.string(),
  name: z.string(),
  createdAt: z.date(),
});
export type TagResponse = z.infer<typeof TagResponseSchema>;

// Create Journal Entry
export const CreateJournalEntrySchema = z.object({
  entryDate: z.string().date(), // YYYY-MM-DD format
  content: z.record(z.unknown()).optional().default({ type: 'doc', content: [] }),
  mood: MoodSchema,
});
export type CreateJournalEntryDto = z.infer<typeof CreateJournalEntrySchema>;

// Update Journal Entry
export const UpdateJournalEntrySchema = z.object({
  content: z.record(z.unknown()).optional(),
  mood: MoodSchema.optional(),
});
export type UpdateJournalEntryDto = z.infer<typeof UpdateJournalEntrySchema>;

// Journal Entry response
export const JournalEntryResponseSchema = z.object({
  id: z.string(),
  tenantId: z.string(),
  entryDate: z.date(),
  content: z.record(z.unknown()),
  mood: MoodSchema,
  createdAt: z.date(),
  updatedAt: z.date(),
});
export type JournalEntryResponse = z.infer<typeof JournalEntryResponseSchema>;

// Search query
export const SearchNotesSchema = z.object({
  q: z.string().min(1).max(200),
  limit: z.coerce.number().int().min(1).max(100).optional().default(20),
});
export type SearchNotesDto = z.infer<typeof SearchNotesSchema>;

// Search results response
export const SearchResultsSchema = z.object({
  notes: z.array(NoteResponseSchema),
  total: z.number(),
  query: z.string(),
});
export type SearchResults = z.infer<typeof SearchResultsSchema>;

// Mood trends response
export const MoodTrendsSchema = z.object({
  entries: z.array(z.object({
    date: z.string(),
    mood: MoodSchema,
  })),
  averageMood: z.number(),
  trend: z.enum(['improving', 'stable', 'declining']),
});
export type MoodTrends = z.infer<typeof MoodTrendsSchema>;

// Mood query params
export const MoodTrendsQuerySchema = z.object({
  days: z.coerce.number().int().min(7).max(365).optional().default(30),
});
export type MoodTrendsQuery = z.infer<typeof MoodTrendsQuerySchema>;
```

2. **Create libs/shared-types/src/notes/index.ts**:
```typescript
export * from './notes.schema';
```

3. **Update libs/shared-types/src/index.ts** to add:
```typescript
export * from './notes';
```</action>
  <verify>
    <automated>grep -q "CreateNoteSchema" libs/shared-types/src/notes/notes.schema.ts && grep -q "CreateJournalEntrySchema" libs/shared-types/src/notes/notes.schema.ts && grep -q "MoodSchema" libs/shared-types/src/notes/notes.schema.ts && echo "Zod schemas created"</automated>
  </verify>
  <acceptance_criteria>
    - libs/shared-types/src/notes/notes.schema.ts exists with CreateNoteSchema, UpdateNoteSchema, NoteResponseSchema
    - libs/shared-types/src/notes/notes.schema.ts contains CreateFolderSchema, UpdateFolderSchema, FolderResponseSchema
    - libs/shared-types/src/notes/notes.schema.ts contains CreateTagSchema, TagResponseSchema
    - libs/shared-types/src/notes/notes.schema.ts contains CreateJournalEntrySchema, UpdateJournalEntrySchema, JournalEntryResponseSchema
    - libs/shared-types/src/notes/notes.schema.ts contains MoodSchema with min(1).max(5)
    - libs/shared-types/src/notes/notes.schema.ts contains SearchNotesSchema, SearchResultsSchema
    - libs/shared-types/src/notes/notes.schema.ts contains MoodTrendsSchema, MoodTrendsQuerySchema
    - libs/shared-types/src/notes/index.ts exports from notes.schema.ts
    - libs/shared-types/src/index.ts exports from notes
  </acceptance_criteria>
  <done>Zod schemas exist for all Notes & Journal DTOs with proper validation rules.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate Prisma client</name>
  <files>libs/data-access/prisma/src/generated/ (generated)</files>
  <read_first>
    - libs/data-access/prisma/schema.prisma (after Task 1 updates)
  </read_first>
  <action>Run Prisma generate to create the TypeScript client with the new models:
```bash
cd libs/data-access/prisma && pnpm prisma generate
```

This regenerates the Prisma client with Note, Folder, Tag, NoteTag, and JournalEntry types.</action>
  <verify>
    <automated>cd /Users/nhan/Developer/my-projects/pms/libs/data-access/prisma && pnpm prisma generate && grep -q "export type Note" src/generated/index.d.ts && echo "Prisma client regenerated"</automated>
  </verify>
  <acceptance_criteria>
    - Prisma generate runs without errors
    - src/generated/index.d.ts contains "export type Note"
    - src/generated/index.d.ts contains "export type Folder"
    - src/generated/index.d.ts contains "export type Tag"
    - src/generated/index.d.ts contains "export type JournalEntry"
  </acceptance_criteria>
  <done>Prisma client regenerated with Note, Folder, Tag, NoteTag, JournalEntry types.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Prisma schema has all 5 new models (Note, Folder, Tag, NoteTag, JournalEntry)
2. All models include tenantId for RLS
3. Zod schemas exist for all DTOs
4. Prisma client regenerated successfully
</verification>

<success_criteria>
- schema.prisma contains Note, Folder, Tag, NoteTag, JournalEntry models
- All models have tenantId field
- Note model has search_vector Unsupported("tsvector") column
- JournalEntry has mood Int field (1-5 scale)
- Zod schemas in shared-types/notes/notes.schema.ts
- Prisma client regenerated
</success_criteria>

<output>
After completion, create `.planning/phases/06-notes-journal/06-01-SUMMARY.md`
</output>
